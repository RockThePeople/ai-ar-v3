# PROGRESS — 국소 3D 편집 시스템 (ai-ar-v3)  · **rev20**

> 🔴 **판본 확인 규칙 (rev8 신설 — 세 웨이브 연속으로 실패한 자리다).**
> 이 머리말의 rev 번호가 정본이다.  각 세션은 첫 동작으로 이 번호를 읽고 보고 첫 줄에 적는다.
> 맥북은 **rev 커밋 해시**를 보고 필수 항목으로 낸다.  커밋 확인이 없으면 다음 웨이브를 열지 않는다.
> (rev5·rev6·rev7 이 연속으로 리포에 안 올라갔고, 그 사이 세 세션이 없는 조항을 근거로 움직였다.
>  Chat 의 프로세스 결함이다 — 원칙 9 를 적어두기만 하고 기계적 확인 절차를 안 만들었다.)

> **이 파일이 계획의 단일 진실이다.**
> 매 Claude Code 세션의 첫 동작은 이 파일을 읽는 것이고, 마지막 동작은 §6 세션 로그에 결과를 적는 것이다.
> 여기 없는 일은 하지 않는다. 여기 있는 순서를 건너뛰지 않는다.
> 리포 `github.com/rockthepeople/ai-ar-v3` · 배치 위치 `docs/PROGRESS.md`
> 작성 2026-08-04 · rev3(관통 우선 재배열 · cbin-delta 지위 강등 · RAM 확정).
> 근거: `docs/INFRA.md` · `docs/DESIGN_INTENT.md` · `cbin-delta/{README,FINDINGS}.md` · `ai-ar-prototype/contract/ROADMAP_STATUS.md`.

---

## 0. 운영 원칙 (매 세션 적용)

```
1. 계획 → 승인 → 구현 → 검증.  코드부터 쓰지 않는다.
2. 한 세션 = 한 게이트.  게이트가 닫히면 /clear 하고 새 세션.
3. 실패한 접근을 계속 고치지 않는다.  §5 의 재시도 상한을 넘으면 되돌리고 폴백으로 간다.
4. "필수적으로 구현하되 최소한으로 구현한다."  스코프 확장은 이 파일을 고친 뒤에만.
5. 결정은 대화가 아니라 파일에 남긴다.  이 파일 또는 docs/adr/ 에.
6. 비밀은 코드·문서·커밋에 절대 넣지 않는다 (§7).
7. **테스트 통과는 동작의 증거가 아니다.**  "그 코드가 떴는가" 와 "그게 실제로
   무엇을 바꿨는가" 는 별개의 사실이다.  육안 산출물이 없으면 미검증으로 적는다.
8. **프롬프트를 작성할 때 세 세션 간 의존성을 고려해 스케줄링한다.**
   보고가 없는 프롬프트는 **아직 실행 중**으로 간주하고, 그것에 의존하는 작업을
   내보내지 않는다.  의존이 없는 작업만 병렬로 내보낸다.
9. 🔴 **Chat 이 이 파일을 고치면, 다음 웨이브 발사 전에 리포에 반영한다.**
   리포의 `docs/PROGRESS.md` 가 Chat 판본보다 뒤처진 채로 웨이브를 발사하지 않는다.
   세션은 **리포 판본만** 근거로 삼는다 — 프롬프트 본문의 조항 인용은 근거가 아니다.
   (W2 에서 세 세션이 리포에 없는 D6 을 근거로 움직였다.  같은 일을 반복하지 않는다.)
10. **Chat 이 쓰는 이 파일도 §7 보안 규칙의 대상이다.**  public 리포로 내려가기 때문이다.
   호스트명·공인 IP·터널 URL 을 본문에 쓰지 않는다. (rev5 에서 실제로 위반했다.)
```

**웨이브 스케줄링 규칙** (원칙 8 의 운용)
```
웨이브   같은 웨이브 = 서로 의존이 없는 작업들.  동시에 내보낸다
전진     한 웨이브의 **모든** 보고가 도착해야 다음 웨이브를 연다
공백     한 세션의 보고가 늦으면, 그 세션에 의존하지 않는 다음 작업만 먼저 내보낸다
기록     내보낸 프롬프트와 도착한 보고를 §6 에 웨이브 단위로 적는다
금지     "아마 끝났을 것" 으로 다음 작업을 내보내는 것.  보고가 곧 완료 신호다
```

**역할 경계** (`claude-code-chat-best-practices.md`)

| 세션 | 담당 | 금지 |
|---|---|---|
| Chat (여기) | 설계·게이트 정의·태스크 분해·이 파일 갱신 | 대규모 코드 생성 |
| Code (맥북, 로컬) | 리포 주인. 계약·오케스트레이터·테스트·main | 전략 결정, 스코프 확장 |
| Code (GPU 서버 SSH) | CUDA·모델 기동·VoxHammer·벤치·로그 | 계약 파일 수정 |

**방법론 5조** (cbin-delta FINDINGS — 이 프로젝트가 여섯 번 물린 것)
```
대리 지표를 의심해라 — 너무 깨끗한 숫자는 항진명제다
"예외가 안 났다" ≠ "안전하다" — 그 경로가 안 돌았을 수 있다
봉쇄와 효능을 둘 다 재라 — 보존만 재면 아무것도 안 하는 구현이 전부 통과한다
규칙만 적고 함수를 안 주면 그 규칙은 안 지켜진다
자기 검사가 자기 환경에서 안 돌면 자기를 보호하지 않는다
```

---

## 1. 대목표 (한 문장)

> **자연어로 만든 3D 오브젝트의 일부를 화면에서 골라 자연어로 바꾸면,
> 바뀐 부분만 실제로 바뀌고 바뀐 바이트만 오간다.**

```
(A) 효능   마스크 안이 지시대로 실제로 변한다        🔴 한 번도 성립한 적 없다
(B) 보존   마스크 밖은 바이트 단위로 불변이다         △ 계측됨 — 단, (A)가 no-op 인 상태에서
(C) 절감   변경 청크만 전송되고 그 자리만 교체된다     △ 계측됨 — 단, (A)가 no-op 인 상태에서
```

🔴 **rev3 의 핵심 인식.** 지난 프로젝트에서 (B)(C)가 "통과"한 것은 **(A)가 아무것도
바꾸지 않은 상태에서 잰 값**이다. 아무것도 안 하는 구현이 보존·절감 검사를 전부
통과하는 것은 당연하다. **따라서 (B)(C)도 재검증 대상이다** — 눈에 보이게 바뀐
콘텐츠 위에서 다시 재기 전까지는 증명이 아니다.

---

## 2. ★ 확정 결정

### D1. 백본을 TRELLIS 1 으로 통일한다 — 생성·편집 동일 백본
```
결정   생성도 편집도 VoxHammer 가 벤더링한 TRELLIS 1 (image-large) 하나만 쓴다.
       TRELLIS.2-4B 는 이번 범위에서 내린다.
이유   ① VoxHammer 가 TRELLIS 1 백본이라 이종 구성은 자산 변환 계층이라는 순수 부채
       ② 편집은 자기가 만든 자산을 inversion 해야 정확하다 — 이종이면 이 전제가 깨진다
       ③ VRAM 여유: 편집 피크 14,180 MiB / 24 GB.  TRELLIS.2 상주 6 GB 회수까지
       ④ 이번 목표는 사실성이 아니라 국소성이다
대가   생성 품질·해상도 하락.  S6 사용자 판정에서 허용 여부를 본다
되돌리는 조건   S6 에서 "데모로 못 쓸 품질" 판정 시에만.  그전엔 재론 금지
```

### D2. `cbin-delta` 는 **계약이지 검증된 시스템이 아니다**  🔴 rev3 강등
```
있는 것   .cbin 인코더/디코더 · 좌표 변환 · 마스크 팽창 · 청크 분할 · assemble
          골든 벡터 200개(바이트 잠김) · conformance 51 · LassoVolume.cs · ChunkBin.cs
없는 것   **실제로 눈에 보이게 바뀐 오브젝트를 이 경로로 통과시킨 적이 없다**

⇒ conformance 51/51 은 인코더의 자기 일관성만 잠근다.  시스템 동작의 증거가 아니다.
⇒ 재구현하지는 않되, **S2 에서 실동작을 처음 증명하기 전까지 "미검증" 으로 취급한다.**
```

### D3. **관통(walking skeleton)을 GPU 앞에 놓는다**  ★ rev3 신설
```
결정   §5-F 폴백이었던 "assemble 경로" 를 폴백이 아니라 **첫 마일스톤(S2)** 으로 올린다.

이유   ① GPU·모델 의존이 0 이다 (numpy 만).  VoxHammer 를 기다릴 이유가 없다
       ② 결과가 **반드시 눈에 보이게 바뀐다** — 효능이 모델 성능에 걸려 있지 않다
       ③ 그래서 (B)(C)를 **no-op 이 아닌 콘텐츠 위에서** 처음으로 정직하게 잴 수 있다
       ④ cbin-delta 의 실동작을 여기서 처음 증명한다 (D2)
       ⑤ best-practices §6: "가짜 구현으로 전 구간을 먼저 관통시킨다.
          프로젝트가 실제로 죽는 지점은 핵심 로직이 아니라 포트·방화벽·인증이다"
       ⑥ 여기까지만 해도 **데모 레벨2 콘텐츠(눈사람+호박머리)가 화면에 뜬다**

결과   VoxHammer 는 do-or-die 게이트가 아니라 **품질 업그레이드**가 된다.
       실패해도 프로젝트는 S2 산출물로 살아 있다.
```

### D4. 인프라 확정 (2026-08-04 W1 실측)
```
리포       github.com/RockThePeople/ai-ar-v3 (public) · 첫 커밋 902cd4f · conformance 51/51 · gitleaks 0
DebugView  <DEBUGVIEW_PORT>=8083 회수 완료 → W2 에서 스켈레톤 bind, 공개 URL **502 → 200 확인**
           터널은 정적으로 이 포트를 겨냥한다.  터널 설정을 손대지 않는다
           ⚠️ 경로는 `/health` 가 아니라 **`/healthz`**
3090       GPU 38 MiB / 24 GB (사실상 전부 가용) · RAM 125 GB(가용 117) · zimage·birefnet Python 3.11.15
A5000      VRAM 여유 확인 · CPU RAM 124 GB
되돌리기   3090 fc5d38e @ 08-03T09:20:15Z / A5000 1a8d89c @ 08-03T08:38:23Z (contract_version=3 일치)
           복구 ai-ar-prototype/server/tools/run-8083.sh
현재 중단  ai-ar-prototype API (VRAM 15.85 GiB 점유로 종료).  복구 run_api.sh
```

### D5. ★ 효능 지표를 재정의한다  🔴 rev5 — W1/A5000 이 게이트의 결함을 찾아냈다
```
문제   G4 의 "전역 24뷰 평균 실루엣 대칭차 > 5%" 는 두 방향으로 눈이 멀었다
       ① 국소 마스크 편집에 둔감 — 마스크가 실루엣의 18%면 나머지 82%가 평균을 희석한다
       ② 시선 방향 돌출에 둔감 — 카메라 쪽으로 튀어나온 주둥이는 외곽선을 안 건드려
          정면 대칭차가 0.16% 로 최저였다.  옆면에서만 14.3% 로 잡혔다
       ⇒ 실측 2.58%(전역) vs 14.32%(마스크·최대뷰).  같은 편집, 5.5배 차이

신 지표 (카메라 무관을 1차로)
  ★ 효능-필수   마스크 영역 **신규 복셀 수 > 0**            ← 레벨2 의 정의 그 자체.
                                                             RePaint 가 원리적으로 못 하던 것
  ★ 효능-정도   마스크 영역 복셀 점유 IoU(before,after) < 0.8
    보조        마스크 한정 실루엣 대칭차 **최대뷰** > 10%   (평균 아님)
  ★ 보존        마스크 밖 복셀 IoU > 0.99  AND  바이트 동일률 100%

폐기   전역 24뷰 평균 실루엣 대칭차 — 판정 지표에서 뺀다.  참고 수치로만 남긴다
교훈   보존만 재면 아무것도 안 하는 구현이 통과하고, **지표를 잘못 고르면
       제대로 도는 구현이 탈락한다.**  둘 다 같은 병이다
```

**D5-a. W2 재계측으로 확정된 보정** — A5000 이 계산 전에 맹점을 등록하고 6건 중 5건이 실제로 발동했다
```
① 신규 복셀 수는 삭제에 눈이 먼다        신규 274 뒤에 제거 245 가 숨어 있었다 (순증 +29)
                                          ⇒ 신규·제거를 **쌍으로** 보고한다
② 신규 복셀은 공간 분포에 눈이 먼다       273/274 가 단일 연결성분.  이게 없으면
                                          "표면 잡음 274개" 와 구분 불가
                                          ⇒ ★ 최대 연결성분을 **필수 지표로 승격**
③ 마스크 밖 IoU 는 1.0 이 될 수 없다      실측 0.853.  리메싱 잡음이다.
                                          ⇒ 절대값 해석 금지.  잡음 바닥값 대비로만 읽는다
④ 표면 복셀화는 신규 수를 부풀린다        마스크 **밖**에서도 신규 533/제거 478 발생
                                          ⇒ ★ **churn 비율**(마스크 안/밖)로 정규화.  실측 4.97배
⑤ 최대뷰는 이상치에 취약                  상위 5뷰 15.3~16.5%, 중앙값 2.01% — 단독 이상치 아님

최종 판정 기준
  ★ 1차(카메라 무관)  마스크 내 신규 복셀 > 0  AND  최대 연결성분 / 신규 ≥ 0.8
  ★ 2차(잡음 정규화)  churn(마스크 안) / churn(마스크 밖) ≥ 3.0
    3차(보조)         마스크 한정 실루엣 **최대뷰** > 10% · 상위 3뷰가 근접
```

**D5-b. 🔴 보존 지표를 고친다 — 바이트 동일률은 승계 청크에만 쓴다**
```
문제   맥북의 합성 디코더는 면 컬링 없음 + 복셀 안쪽 들여쓰기라 기하가 **복셀-국소**다.
       그래서 "마스크 밖 바이트 100%" 가 구조적으로 성립한다.
       진짜 TRELLIS 디코더는 receptive field 때문에 이 성질이 **없다.**
       A5000 의 마스크 밖 IoU 0.853 이 정확히 그 현상이다

수정   보존-A  승계 청크의 바이트 동일률 = 100%      ← 우리가 안 건드렸으니 항진명제. 회귀 검사용
       보존-B  재디코딩 영역의 기하 거리 ≤ 잡음 바닥값   ← 이게 진짜 보존 지표
       보존-C  churn(밖) 이 잡음 바닥값 수준           ← D5-a ④와 짝

🔴 미결   잡음 바닥값이 아직 없다.  마스크 밖 IoU 0.853 의 13% 가 (a) 순수 리메싱 잡음인지
          (b) 일부 실제 누출인지 이 데이터로는 못 가른다.
          ⇒ **편집 없이 인코드→디코드만 왕복시킨 대조군 1회(GPU)** 가 필요하다.  W3-A5000 에 배정
```

### D19. 🔴 육안 게이트의 **기준 그림**을 고정한다  (rev9)
```
문제   실루엣 4분할로는 "호박 얼굴이 있는가" 에 **원리적으로 답할 수 없다.**
       분면은 실루엣 투영이라 깊이를 뭉개고, 파낸 구멍은 뒷면 표면으로 메워져 안 보인다
       ⇒ 기준 그림을 안 정하면 세션마다 다른 그림으로 판정하고, 그때마다 답이 달라진다

결정   ★ 육안 게이트의 정본은 **앞면 깊이맵**이다 (3090 이 W5 에 추가한 ⑤).
       4분할×3뷰는 형태·마스크·변경청크 확인용이고 **오목 디테일 판정에는 쓰지 않는다**
       ⇒ G2 의 "육안으로 보인다" 는 깊이맵 기준임을 §5 S2 에 명시
★ 기록   3090 이 "내 화면으로는 이 질문에 답할 수 없다" 를 먼저 말하고 도구를 추가했다.
          있는 도구로 답을 짜내지 않았다 — 다섯 번째 사례
```

### D20. 얼굴 해상도는 **구조적 한계**다 — 파라미터로 못 고친다  (rev9)
```
실측(3090 W5)   마스크 세로 23복셀 → 호박이 22×22×24 로 들어간다 → 얼굴 요소 하나가 3~4복셀
                비스듬한 삼각눈 → 1~2복셀 가로 슬릿 · 톱니입 → 이빨 구분 사라진 어두운 덩어리 하나
                "잭오랜턴" 이라고 알고 보면 얼굴이 보이지만, 모르고 보면 호박에 팬 자국 몇 개
원인   TRELLIS-image-large 의 SLat 격자가 **64³ 고정**이다.  머리만 편집하는 한 구조적이다
       ⇒ D11 결함이 **아니다.**  형태는 완승했다 — 호박 전체가 올라갔고(used_fill=1.000)
         능선진 몸통 + 꼭지가 세 뷰 모두에서 호박으로 읽힌다
★ 기록   3090 은 파라미터로 돌려 맞추지 않고 "해결할 수 없다" 로 보고했다
선택지   (a) 얼굴이 프레임을 꽉 채우는 호박으로 재생성 — 싸고 국소성을 안 해친다
         (b) 머리 마스크 확대 — 국소성·절감을 희생한다.  핵심 주장을 깎는다
         (c) 현 수준을 레벨2 달성으로 인정 — 이음새·디테일은 M3(VoxHammer)로 넘긴다
         ⇒ **사용자 판정 사안** (S6)
```

### D16-a. 허용 배수 확정 — `max_excess_ratio = 1.0`, **적용 대상은 승계 후 산출물**  (rev9)
```
맥북이 정확히 지적했다: rev8 의 D16 은 **척도만** 정하고 배수를 안 정했다.

확정   max_excess_ratio = 1.0  (rev6 G2 의 "거리 ≤ 잡음 바닥값" 을 배수로 옮긴 것.
       근거 있는 유일한 값이고, 알려진 두 점 (assemble 0.0 / VoxHammer 2.10) 사이
       어떤 값이든 지금 판정은 같다)

★ 그런데 더 중요한 것은 **무엇에 대고 재는가**다
   VoxHammer 원출력의 마스크 밖 2.10배 초과는 **게이트 대상이 아니다.**
   우리가 출하하는 것은 원출력이 아니라 **승계 후 산출물**이고, 거기서는 마스크 밖이
   부모 바이트라 초과가 구조적으로 0 이다 (D15).  모델 원출력으로 보존을 판정하면
   우리가 배송하지 않는 것을 판정하는 셈이다

⚠️ 그러면 보존이 다시 항진명제가 되는가? — 아니다.  VoxHammer 단계의 진짜 위험은
   **halo 가 좁아서 경계에 이음새가 생기는 것**이다.
   ⇒ S5 에서는 별도 지표를 잰다: **halo 바로 바깥 대역(0–2 vox)의 초과배수**
     A5000 실측 참고치 경계 0–2 vox 1.71× / 2–5 vox 1.24× / 원거리 2.21×
     ⇒ 이 대역의 문턱은 **실제로 halo 를 적용해 재본 뒤** 정한다.  지금 정하지 않는다
```

### D21. 바닥값 **단위 규칙** — 무차원과 복셀거리를 섞지 않는다  (rev9)
```
preservation_geometry_distance = 1 − IoU  → **무차원**.  바닥값은 1 − 0.9299 = 0.0701
A5000 이 같이 준 Chamfer(0.097 / 0.177 / 0.610)는 **복셀 단위 거리**다
⇒ 두 값을 섞으면 판정이 조용히 무의미해진다.  3090 이 W5 에서 올바르게 분리했다
```

### D15. ★ 잡음 바닥값 확정 · 0.853 = (b) 누출 · **그러나 승계가 흡수한다**  (rev8)
```
바닥값 (A5000 W4, :8082 를 129초 내리고 측정)
  마스크 밖 왕복 IoU   0.9299    churn 0.0701
  Chamfer(양방향)      평균 0.097 vox · 95% 0.177 · 최대 0.610
  ★ 디코더 분산 IoU    0.9997  — 사실상 결정적.  이게 비교의 유효숫자를 정한다

판정   0.853 vs 바닥값 0.9299 → 격차 0.0768 = 디코더 분산의 **256배** ⇒ **(b) 누출**
       마스크 밖 차이의 약 절반(0.0701/0.1469)만 왕복 잡음이고 나머지는 편집 파이프라인 기인
분포   초과가 마스크 경계가 아니라 **원거리에 더 크다** (원거리 2.21× > 경계 1.71×)
       ⇒ "의미가 마스크를 넘어 번진 누출" 보다 **전역 inversion 불완전성**에 가깝다

★★ 그런데 이것은 제품을 위협하지 않는다 — **아키텍처가 이미 막고 있다**
    우리는 VoxHammer 출력에서 **마스크+halo 청크만** 가져가고 나머지는 **부모 바이트를 승계**한다.
    전역 드리프트는 클라이언트에 도달할 수 없다.  DESIGN_INTENT §3-D 의 승계 설계가
    정확히 이 문제를 위해 있었다는 것이 이제 숫자로 확인됐다
    ⚠️ 단 **halo 폭이 충분한가**는 별개 문제다.  경계 0–2 vox 초과가 1.71× 다 — S5 에서 확인
    ⚠️ 그리고 이 값은 **VoxHammer 경로**의 것이다.  assemble 경로는 strict_containment=True 에서
       마스크 밖 IoU **1.000000** 이다.  D3(관통 우선)의 정당성이 한 번 더 확인됐다

미결   "inversion 불완전성" vs "의미적 누출" 분리 → 마스크=∅ 편집 1회(GPU ~200초) 필요.
       **데모에 불필요하므로 연기한다** (최소 구현 원칙).  S5 VoxHammer 단계에서 처리
```

### D16. 보존 판정은 절대값이 아니라 **바닥값 대비 초과배수**로 한다  (rev8)
```
마스크 밖 IoU 0.853 은 "0.99 미만이라 실패" 가 아니라 "바닥값 0.9299 대비 2.10배 초과라 실패" 다.
절대 IoU 문턱(0.99)은 리메싱 잡음 때문에 **어떤 디코더로도 달성 불가능**하다 — 항진적으로 실패한다.
⇒ 보존 임계는 **왕복 잡음(0.0701) 기준**으로 잡는다.  디코더 분산(0.0003) 기준이 아니다
   (분산 기준으로 잡으면 모든 것이 유의해져 지표가 죽는다)
⇒ 이 역시 D5·D12·D11 과 같은 병이다 — 문턱이 측정 대상의 물리를 반영하지 않으면 거짓말한다
```

### D17. `churn_ratio` 정의 확정 — 합집합 정규화 ≡ 1−IoU  (rev8)
```
churn_rate(region) = (|new| + |removed|) / |(before ∪ after) ∩ region|  ≡  1 − IoU(region)
churn_ratio        = (1 − IoU_in) / (1 − IoU_out) = 0.7300 / 0.1469 = 4.97

채택 이유   ① 항등적으로 1−IoU 라 IoU 만 있으면 재현되고 별도 상태 불필요
            ② 유계 [0,1]   ③ before/after 대칭
기각        맥북 제안(before 점유 정규화) = 7.459 — 무계다.  마스크 안에서 1.188 > 1 이라
            "비율" 로 해석 불가.  before 가 빈 영역에서 발산
오답        원시 개수비 0.5134 — 영역 크기를 무시한다 (맥북 진단 정확)
★ 두 정의가 결론을 바꾸지 않음을 확인했다 (4.97 vs 7.46, 둘 다 ≫1)
```

### D9-b. ⚠️ 좌표 함정 **두 번째** — `to_glb` 의 z-up→y-up 회전  (rev8)
```
to_glb 말미:  vertices @ [[1,0,0],[0,0,-1],[0,1,0]]
디코더 native 프레임은 **z-up 이고 그것이 복셀 격자 프레임과 같다.**
⇒ to_glb 를 **거치지 않은** 메시를 복셀과 비교할 때는 이 회전을 직접 걸어야 한다
근거   A5000 이 기하 전용 export 에서 이걸 빠뜨려 왕복 메시만 z-up 이었다.
       소스를 읽고 적용 후, 왕복이 D9 와 같은 축 변환을 IoU 0.9473 으로 선택하는 것으로 검증
       (before.glb 는 0.9365).  놓쳤으면 **바닥값이 통째로 허수가 됐다**
⇒ 같은 파이프라인에 같은 종류의 함정이 둘이다.  frames.py 에 이것도 상수로 넣는다
```

### D18. 운영 — VoxHammer 계열 디코드는 `torch.no_grad()` 필수  (rev8)
```
res-256 메시 디코더 실 peak   no_grad 시 6,204 MiB / 없으면 8 GB 초과
:8082 정지↔복구 절차 검증됨   다운타임 40~49초.  trap EXIT 로 실패해도 자동 복구 (3회 중 2회 실패했으나 무사)
:8082 와 VoxHammer 는 상호 배타 (D14).  정지 전 RECOVERY 절차를 먼저 파일로 기록한다
```

### D11. 🔴 기증자 스케일 — 마스크 범위에 맞춰 **메시에서 재복셀화**한다  (rev7, 최우선)
```
문제(W3/3090 실측)   base 와 donor 가 **각자 독립적으로** NORMALIZED 격자를 꽉 채우게 정규화된다
                     base span 49×50×64 · donor 60×60×64 · 머리 마스크 span 49×50×23
                     ⇒ crop ≤ 0.30 에서만 들어간다.  지금 화면의 기증자는 호박의 위쪽 30%
                       (뚜껑+꼭지)뿐이고 삼각눈·톱니입은 잘려 나갔다.
                       **게이트는 통과하는데 "할로윈 호박 머리" 로는 안 보인다**

기각   좌표 스케일

🔴 **근거 보정 (rev8 — 맥북이 D11 의 근거가 D11 이 쓰는 방향에서 무력함을 실측했다)**
```
FINDINGS 의 "6-이웃 유지율 s=1.5 → 50%, s=2.0 → 0%" 는 **업스케일** 실측이다.
맥북 독립 재현: 업스케일 s=2.0 → 좌표스케일 쌍유지 0.0%·고립 100% / 재복셀화 고립 0.0% ✅

그런데 D11 이 실제로 쓰는 방향은 **다운스케일**(기증자 60×60×64 → 마스크 18³)이고,
다운스케일에서는 **두 경로 모두 고립복셀 0.0%** 다.  이 지표로는 구분이 안 된다.
다운스케일이 셀을 찢지 않고 뭉치기 때문이다.

⇒ 다운스케일에서 두 경로가 갈리는 것은 **연결성이 아니라 내용**이다:
   같은 자리로 옮겨 비교 시 IoU 0.332 · 셀 수 1352(좌표스케일) vs 1538(재복셀화)
   — 좌표 스케일이 표면 셀 12% 를 잃는다
⇒ 재복셀화를 쓰는 진짜 이유: **양방향에서 안전하고 양자화 오차를 겹치지 않는다.**
   업스케일 실측을 인용해 다운스케일을 정당화하지 않는다
★ 네 번째 사례 — 세션이 "이 지표가 내 주장을 실제로 지지하는가" 를 물어 반증을 냈다
```

결정   ★ 기증자 **메시**를 마스크의 복셀 범위에 맞는 cell_size 로 **다시 복셀화**한다
         cell_size' = donor_mesh_extent / mask_extent
       이것은 좌표 스케일이 **아니다.** 연속 메시를 다른 해상도로 새로 복셀화하는 것이라
       인접성이 처음부터 올바르게 재구성된다.  FINDINGS §2 가 금지한 것은
       "이미 만들어진 희소 좌표를 곱하는 것" 이지 재복셀화가 아니다
       ⇒ 기증자 메시는 .cbin 의 POSITION/INDEX 로 재조립한다 (.cbin 은 이미 메시다)

부수   마스크도 좁힌다.  top_region_cells 가 가로로 점유 bbox **전체**를 덮어서
       마스크가 격자의 21%(56,350셀)다 — 눈사람은 머리가 몸통보다 훨씬 좁다.
       ⇒ Y-슬라이스별 bbox 로 바꾼다.  작은 변경이고 "국소 편집" 전제를 되살린다
```

### D12. G2 효능 문턱을 (신규 ∪ 제거)의 연결성분으로 바꾼다  (rev7)
```
문제(W3/맥북 실측)  "최대 연결성분 / 신규 ≥ 0.8" 은 **가산 편집 전용**이다
                    배치된 기증자 껍질 1.000 · 결과 점유 전체 1.000 · **신규만 0.731** ← 미달
                    원인은 구조적이다 — 기증자 껍질이 옛 껍질과 교차하고, 교차한 308셀은
                    before 에도 있어 "신규" 가 아니다.  그 링이 빠지며 한 덩어리가 두 조각으로 갈린다
                    A5000 의 273/274 는 **빈 공간에** 주둥이를 더한 가산 편집이라 이 현상이 없었다

결정   ★ 지표를 **(신규 ∪ 제거)의 최대 연결성분 비율**로 바꾼다.  문턱 0.8 단일 유지
       근거: 편집은 더하든 치환하든 **하나의 응집된 변화**여야 한다.
       가산(VoxHammer)이든 치환(assemble)이든 같은 정의로 잡힌다
기각   경로별 문턱 분리(assemble 0.7 / VoxHammer 0.8) — 그게 대리 지표를 만드는 길이다

⚠️ 단서 (rev8)   0.731 은 **"넓은 bbox 마스크 + 크롭"** 구성의 값이다.  D11 적용 후에는
                 구 정의(신규만)도 **0.9924**, 신 정의는 **1.0000** 이다.  파편화가 자연 소멸했다.
                 D12 변경은 여전히 옳지만(가산·치환을 한 정의로 통일한다),
                 "assemble 경로가 원리적으로 0.8 을 못 넘는다" 로 읽으면 과장이다.
                 옛 구성은 맥북의 `test_d12_fixes_the_assemble_fragmentation_that_w3_found` 가 보존한다

★ 기록   맥북은 crop 0.15 로 줄이면 0.826 으로 넘길 수 있었으나 **하지 않았다.**
         "문턱을 넘기려 픽스처를 깎는 것이 바로 대리 지표를 만드는 짓" 이라고 적고
         실측을 그대로 박아 판정을 올렸다.  이 프로젝트가 여섯 번 물린 함정을 피한 두 번째 사례다
```

### D13. `strict_containment=True` 는 옵션이 아니라 전제다  (rev7)
```
끄면 보존이 조용히 무너진다 — W3/3090 실측 preservation_iou_out 0.345 · 절감 14.05%
켜면 1.000000.  splice docstring 이 이미 경고하고 있었고 모듈이 맞았다
⇒ §5 S2 게이트 문구에 명시한다.  게이트를 재는 모든 실험에서 True 고정
```

### D14. 잡음 바닥값 — `:8082` 를 잠깐 내려서 얻는다  (rev7)
```
상황   A5000 24 GB 에서 :8082 상주 18.02 GiB + TRELLIS 메시 디코더(res 256) >5.4 GB → 공존 불가
       encode 는 끝났다(w3/slat.npz).  decode 만 남았고 ~40초 · ≥8 GB
결정   ★ (i) :8082 를 잠깐 내린다.  3090 의 자산은 **이미 다 확보됐다**(base 181 · donor 294 청크,
         해시 전건 대조 통과).  지금이 내려도 되는 유일한 창이다.  끝나면 즉시 복구
기각   (ii) 3090 에서 왕복 — TRELLIS 가중치가 반쪽이라 불가(D6)
       (iii) 연기 — 바닥값 없이는 실자산 보존을 영원히 판정할 수 없다.  맥북의
             "baseline 없으면 판정 거부" 설계는 유지가 맞다

★ 기록   A5000 은 4회 시도 끝에 못 재고 **(c) 못 가름**으로 보고했다.  사전 등록한 맹점 5번
         ("왕복이 VRAM 에 안 들어가면 (c)로 보고, 추정치 금지")이 실제로 발동해
         "0.85쯤이니 잡음일 것" 이라고 쓰고 넘어가는 것을 막았다.  세 번째 사례다

운영 제약(신규)   :8082 와 VoxHammer 계열 작업은 **상호 배타**다.  스케줄링에 반영한다
사고 원인 기록    :8082 는 W1 에서 OOM 해소를 위해 승인받고 종료한 프로세스였는데
                  복구 필요가 명시되지 않았다.  W1 포트 스캔이 8082 를 빠뜨린 것이 근본 원인.
                  ⇒ GPU 정리 시 포트 스캔은 **8080–8090 전 범위**로
```

### D8. 산출물은 `.cbin` 세트다 — glTF 는 파생물이다  🔴 rev6 신설
```
발견   A5000 :8082 계약에 glTF 가 **없다**.  노출 라우트는 generate/edit/assemble/
       jobs/chunks/*.cbin/commit/health 뿐이고 산출물은 .cbin 청크 세트다.
       .glb 를 내는 경로는 레거시 :8081 하나인데 그건 **텍스트 조건**이라 우리 RGBA 를 안 쓴다

결정   ★ 사용자 요구("glb 대신 gltf")의 **의도는 통짜 재로딩 회피**였다
       (DESIGN_INTENT §3-E: 통짜 GLB 재로딩이 GameObject 파괴·앵커 리셋·화면 pop 을 냈다).
       `.cbin` 청크 세트는 그 요구를 **더 강하게** 만족한다 — 청크 1개 = GameObject 1개, 부분 교체.
       ⇒ 전송·교체의 단위는 `.cbin` 으로 확정한다.  A5000 계약을 그대로 쓴다
       ⇒ glTF/GLB 는 **DebugView 육안 확인용 파생물**로만 서버 내부에서 생성한다.
         Unity 로 나가는 와이어에는 등장하지 않는다
       ⇒ 레거시 :8081(.glb·텍스트 조건)은 D1 위반이므로 쓰지 않는다
```

### D9. ⚠️ 좌표 프레임 규약 — 축 순열  🔴 rev6 신설 · 다음 웨이브 전 필수
```
GLB       Y-up   (±0.50)
복셀 격자  Z-up   (±0.49)
변환      voxel = (x, -z, y)      즉 perm=(0,2,1) sign=(1,-1,1)

근거   A5000 이 가정하지 않고 48개 부호付 순열을 **전수 탐색**해 확정.
       정답 IoU 0.9365 vs 차점 0.1943 — 4.8배 격차로 모호하지 않다
🔴 항등 변환을 쓰면 IoU 0.19 대가 나오고 **조용히 전부 무의미해진다.**
   복셀을 비교하는 모든 코드는 이 규약을 상수로 참조해야 한다 (문서만으로는 안 지켜진다 — 방법론 4조)
부수 소득   이 값이 우리 복셀화기의 검증이기도 하다 (자체 6,787 vs TRELLIS 7,125)
```

### D10. 절차 — VoxHammer 는 래퍼 경유가 필수  (W2/A5000 확립)
```
tools/run_voxhammer_checked.sh   VoxHammer 소스 무수정, 바깥에서 감싼다
  실행 전  GPU 여유 확인 (기본 16,000 MiB = W1 실측 피크 14,180 + 여유).
           부족하면 **거부하되 아무것도 죽이지 않는다** (:8082 보호)
  실행 후  features.npz mtime 검증 · "Error happened during processing." 마커 검사
검증     5개 경로 실증 완료 (A 낡은캐시=FAIL / B 마커=FAIL / C 정상=PASS / D VRAM부족=거부 / E exit3=FAIL)
         ★ 테스트 A 가 W1 1회차(7월자 features 로 돌며 "completed successfully" 출력)를 정확히 잡는다
```

### D6. 호스트 배치 확정 — 3090 은 TRELLIS 가중치를 받지 않는다
```
발견   3090 의 TRELLIS-image-large 는 **반쪽이다** — 디코더 4개(642 M)뿐.
       pipeline.json · ss_flow_img_dit_L · slat_flow_img_dit_L 이 없다 (.incomplete 도 없음
       = 중단이 아니라 애초에 안 받은 것).  3090 로컬 image-to-3D 는 불가
       ⇒ 그러나 **다운로드하지 않는다.** 우리 배치에서 3090 은 TRELLIS 가 필요 없다

배치   3090   Z-Image-Turbo → BiRefNet → RGBA · 오케스트레이터 · DebugView(:8083) · 델타 부기
       A5000  TRELLIS-image-large(:8082, 정상) **생성** + VoxHammer **편집** — D1 백본 통일 유지
       ⇒ D1 은 살아 있다.  단 생성·편집이 **같은 기기**에서 돈다

🔴 스케줄링 제약   VoxHammer 는 GPU 전용(exclusive)을 요구한다 — 16 GB 상주 프로세스와 공존 불가.
                   ⇒ 편집 실행 중에는 A5000 의 생성 서비스를 내려야 한다.
                     같은 웨이브에 "A5000 생성" 과 "A5000 편집" 을 같이 넣지 않는다
```

### D7. 절차 보강 — 조용한 실패를 막는다
```
extract_feature.py:125 의 맨 except 가 OOM 을 삼키고 "completed successfully" 를 찍는다.
1회차가 7월자 낡은 features.npz 로 돌았다.  원본 개조는 금지이므로 **바깥에서 검증**한다.
  ⇒ VoxHammer 실행 절차에 features.npz 의 mtime 검증을 필수 단계로 넣는다
  ⇒ 실행 전 nvidia-smi 로 GPU 를 비우고, 실행 후 산출물 타임스탬프를 확인한다
서비스 기동 후 ps -o pid,ppid,cmd -p <PID> 로 PPID=1 을 눈으로 확인한다 (setsid --fork)
```

---

## 3. 중목표

| ID | 중목표 | 상태 | 리스크 |
|---|---|---|---|
| **M0** | 리포 부트스트랩 · 인프라 라이브 확인 · `:8083` 회수 | ☐ | 낮음 |
| **M1** | **관통: 고정 자산 → 마스크 → assemble → 델타 → DebugView 육안** | ☐ | 낮음 · **최우선** |
| **M2** | 생성: 자연어 → Z-Image → BiRefNet → TRELLIS 1 → glTF + `.cbin` | ☐ | 낮음 |
| **M3** | **편집 엔진: VoxHammer — 품질 업그레이드** | ☐ | 🔴 높음 (실패해도 M1 로 산다) |
| **M4** | 사용자 최종 판정 (레벨1·레벨2) | ☐ | — |
| **M5** | Unity AR: 라쏘 → 편집 → in-place 교체 (앵커 손실·다운타임 0) | ☐ | 중간 |
| **M6** | 멀티유저: 겹침 없는 가정 하에 `.cbin` 만 브로드캐스트 | ☐ | 중간 |

### D22. ★★ 레벨2 정의 확정 = **용 오브젝트의 머리를 3개로** (rev10 · 사용자 확정)
```
레벨1   흰 눈사람 2단 → 머리를 주황 할로윈 호박으로        ✅ **W5 에서 사용자 인정 (G2 육안 통과)**
레벨2   용(Dragon) 생성 → **머리를 3개로 자연스럽게**       ← 새로 확정. 원문이 잘려 있었다

★ 이 정의가 계획을 바꾼다
① **assemble 경로로는 원리적으로 불가능하다.**
   assemble 은 "마스크 자리를 비우고 도너를 정수 이동으로 끼운다" 라서,
   목이 몸통에서 **갈라져 나오게** 만들 수 없다.  머리 하나를 다른 머리로 바꾸는 건 되지만
   없던 가지를 자연스럽게 뻗게 하는 건 다른 종류의 연산이다
   ⇒ **VoxHammer 가 옵션이 아니라 필수 경로가 된다.**  M3 의 지위가 올라간다
   ⇒ D3(관통 우선)은 여전히 옳았다 — 인프라를 먼저 뚫어서 지금 VoxHammer 만 남았다

② **마스크가 빈 공간을 포함해야 한다.**  머리 옆 빈 자리를 마스크로 잡고 "머리를 셋으로" 를 지시한다.
   지금까지 우리는 **점유 셀 교집합만** 해봤다 (assemble 경로)
   ⚠️ 근거는 있다 — W2/A5000 실측에서 VoxHammer 샘플의 `voxels_delete` 2,346셀 중 **81.4%가
      before 에서 빈 공간**이었다.  체적 마스크가 맞고, 그래서 신규 274 가 나올 수 있었다

③ **"자연스럽게" = 목 연결부 품질 = halo 폭 문제**다.  D15 의 미결(halo 가 충분한가)이
   이제 데모의 합격 조건 그 자체가 된다.  A5000 참고치: 경계 0–2 vox 초과 1.71×

④ 해상도 리스크는 **낮다.**  D20(얼굴 3~4복셀)이 여기선 재발하지 않는다 —
   머리 3개는 얼굴 디테일이 아니라 큰 구조라 64³ 에 오히려 잘 맞는다

🔴 새 리스크   **용 자체가 64³ 에서 생성되는가.**  눈사람·호박은 단순한 덩어리였다.
               용은 가늘고 긴 목·꼬리·날개를 가져서 뭉개질 수 있다.
               ⇒ 이것이 레벨2 전체의 전제다.  **가장 먼저 확인한다** (W6-3090)
```

### D22-검증. ★ 레벨2 전제 통과 (W6/3090)
```
용은 64³ 에서 형체를 유지하고 **목이 구조로 남는다**
  dragon-c(primary)  10,264복셀 · 124청크 · 정점 230,332
  ★ 목 단면 극소     z=42:111 → z=45: **32** → z=51:94    (뚜렷하다)
  머리 마스크 1.41%  자산 1,381복셀     ← 눈사람 8.65% 보다 **더 국소적**
  날개도 살아남았다 (얇은 판) — 예상보다 훨씬 잘 버텼다
⇒ D20(해상도) 은 용에서 **재발하지 않는다.**  D22 ④ 예측이 맞았다

★ D22 ② 체적 마스크 확보   head3mask 11,628셀(4.44%) · 자산 998 · **빈 공간 10,630 = 91.4%**
   기존 headmask 는 "머리를 다른 것으로 교체" 용이라 머리에 딱 붙어 있어 새 가지를 뻗을 자리가 없다.
   head3mask 는 목 잘록함(z=45) 위를 머리로 잡고 좌우로 머리 폭만큼 넓힌 것이다
```

### 🔴 W10 결과 — **머리 3개 실패. 머리 1개.**  그러나 원인이 거의 특정됐다  (rev17)
```
실측   마스크 11,628셀(4.436%) · 자산 955 · 빈공간 91.79% · X대칭 True · 절단 z=46 (동점 45/47, 45 선택)
       머리 개수  정본 z=46 에서 6/18/26 **전부 1개.**  3-plateau 가 어디에도 없다
       신규/제거  v1 304/730 · v2 395/725   ★ **제거가 신규의 2배**
       전체 복셀  8,000 → 8,352(v1) / 6,744(v2)
       육안       목·머리가 **소실**되고 뿔/날개형 구조 한 쌍이 그 자리를 차지
       비용       편집 116~118초 · VRAM 12.8 GB · 예산 여유 (렌더 재사용 80초 절약)
```

### 🔴 W11 결과 — 머리 여전히 1개.  **그러나 D40 이 절반 맞았고 진짜 병목이 드러났다**  (rev20)
```
                    W10 (1머리 조건)        W11 Run A (3머리 조건)   Run B (+앵커 18.1%)
  신규 / 제거        304 / 730  ❌           532 / 471  ✅            545 / 483  ✅
  direction_ok       FALSE                  **TRUE**                 **TRUE**
  총 복셀            8,352 / 6,744          9,562                    9,488
  최대 연결성분      1.000                  0.986                    0.981
  머리 개수          1                      **1**                    **1**

★ **조건 교체는 실제로 효과가 있었다 — 방향에만.**  파괴가 생성으로 바뀌었다.
  D40 은 "제거 > 신규" 를 정확히 설명했고 조건을 고치자 그 증상이 사라졌다.
  ⇒ **D40 은 필요조건이지 충분조건이 아니다**
⚠️ D39(앵커)는 **이번 범위에서 효과 없음.**  잔존율 18.1% 로 Run A 와 결과 동일 →
   앵커가 병목이 아니라는 증거이며, 아래 z 예산 가설을 지지한다
```

### D41. ★★★ 진짜 병목 — **공간이 없다.  자산이 격자를 꽉 채운다**  (rev20 · 최상위 가설)
```
증거   결과 구조의 z 범위가 **[46,63] — 격자 천장 z=63 에 닿았다**
       마스크가 허용한 세로 공간은 **z 46–62 = 17칸뿐**인데 조건 이미지의 목 셋은 몸통에서 올라온다
       ⇒ **머리 셋을 17칸에 넣으라는 요구 자체가 무리다**
       ★ A5000 이 **계산 전에 등록한 맹점 §2** 가 정확히 이것이었고 발동했다

더 근본적으로   dragon-c 의 bbox 는 **z 0–62**.  용이 격자를 **세로로 꽉 채우고 있다.**
                위로 뻗을 공간이 애초에 없다
                ⚠️ 대조: W1 소년상의 주둥이는 **앞으로(깊이축)** 뻗었고 거기엔 공간이 있었다.
                  그래서 성공했다 — 성공 사례가 "여유가 있던 축" 을 썼다는 것을 이제 안다

⇒ **add 계열 편집은 격자 여유(headroom)를 요구한다.**  자산이 격자를 꽉 채우면 원리적으로 제약된다
⇒ 남은 여유는 **가로**다.  마스크 box_x 14–49 = **36칸**.  세로 17칸의 두 배가 넘는다

W12 대응 (같은 뿌리를 두 방향에서 친다)
  ① 조건 이미지를 **머리 셋이 좌우로 벌어진(부채꼴)** 것으로.  수직으로 세우지 않는다 — 공간이 없다
  ② 마스크 z 하한을 낮춰 **목 전체를 포함**한다 (분기가 목에서 일어나야 하므로)
```

### D25-c. 🔴 D25-a 를 레벨2 에 적용한 것이 잘못이었다  (rev20)
```
rev16 에서 긴장 관계로 적어 뒀던 것이 **데이터로 확정됐다**
  D25-a("목 극소 **위**에서 자른다")는 **레벨1(부위 교체)** 에서 도출된 규칙이다 —
  목을 물지 않아야 이음새가 산다
  **레벨2(가지 뻗기)는 요구가 반대다** — 분기가 목에서 일어나야 하므로 **목 재료가 필요하다**
⇒ 레벨2 의 마스크는 "부위 교체 영역" 이 아니라 **"분기 영역"** 이다.  z 하한을 목 시작점까지 낮춘다
⇒ 마스크 정의를 **op 별로 다르게** 한다.  replace_region 은 D25-a, add 는 D41
```

### D40. ★★★ **조건 이미지가 편집 내용과 모순되면 이미지가 이긴다**  (rev17 · 최상위 원인 가설)
```
🔴 **RePaint 실패의 원인 ② 가 VoxHammer 에서도 그대로 살아 있었다.**
   DESIGN_INTENT §6: "조건이 parent/input.png 하나뿐 → prompt 가 no-op"
   우리는 편집 엔진을 RePaint → VoxHammer 로 바꿨지만 **조건 문제는 안 고쳤다.**

논리   TRELLIS-image-large 는 **이미지 조건** 백본이다.  조건 이미지(dragon-c/source.png)에는
       **머리가 하나**다.  마스크를 비우고 다시 채울 때 모델이 참조하는 유일한 강한 신호가
       "머리 하나인 용" 이므로, 텍스트로 "머리 3개" 를 요구해도 **이미지가 이긴다**
정황   ① 결과가 머리 1개로 **정확히 원본 구조로 회귀**했다
       ② 제거 > 신규 — 지운 자리를 이미지대로 되돌리려다 부피가 줄었다
       ③ A5000 이 브리핑에 없던 TRELLIS-text-large 를 스스로 설치했다.  같은 직감이다

⇒ **W11 의 1순위 실험: 조건 이미지를 "머리 3개인 용" 으로 바꾼다.**
  t2i 한 번(~40초)이면 되고, 마스크를 통제한 채 조건만 바꾸므로 **효과가 격리된다**
⇒ 실패하면 2순위: 마스크를 좁혀 앵커를 남긴다 (D39)
```

### D40-a. 조건 이미지 확보 (W11/3090) — 그리고 **새 교란 변수**  (rev18)
```
채택   dragon3-c.  목 셋이 나란히 서고 얼굴 셋이 다 보인다 (입·눈·뿔 뚜렷, 목 사이 빈 공간 있음)
       sha256 97056e22… (source.png, RGBA 원격 판독: alpha_min 0 / max 255 · 투명 74.9% · 전경 24.3%)

★ 프롬프트 교훈   **"three heads" 로는 안 나온다.**  dragon3-a 가 그것만으로 2개+모호를 냈다
   갈린 문구   "**exactly three separate thick vertical necks rising side by side … wide visible gaps**"
   그리고 "hydra" 로 불러야 나왔다.  "three-headed dragon"(b)은 머리를 **날개 관절에 붙였다**
   ⚠️ 단어 선택이 결과를 갈랐지만 **1회 관측**이라 재현 가능한 규칙인지는 모른다

구도 일치   자세·카메라·전경비율(24.6→24.3%)·세로 bbox 모두 맞다.  잘리지 않았다

🔴 **새 교란 변수 — 날개가 사라졌다**
   dragon-c 는 날개를 **펼치고** 있는데 dragon3-c 는 날개가 **안 보인다**.
   프롬프트에 "wings folded flat against the back" 이 들어 있는데도 그렇다
   (W6 에선 정반대로 "folded 를 넣어도 3/3 폈다" 였다 — Z-Image 의 날개 거동은 불안정하다)
   가로 bbox 도 좁다 (x 230–800 vs 112–913)

   ⇒ **조건 이미지와 편집 대상 자산이 목 아래에서 다르다.**
     마스크는 목 위라 직접 영향은 적을 것으로 보이지만, **마스크 밖 변화를 W10 과 직접 비교할 수 없다.**
     W10 은 dragon-c 조건, W11 은 dragon3-c 조건 — 조건이 다르면 마스크 밖 churn 의 기준선도 다르다
   ⇒ 마스크 밖 변화가 관측되면 **(a) 편집 파이프라인 불완전성 vs (b) 조건 이미지 차이**를
     구분해야 한다.  이번 런에서 이 둘은 **분리되지 않는다** — 그 사실을 판정에 실어야 한다
```

### D39. 마스크가 **앵커를 전부 지우면** 편집이 아니라 재생성이 된다  (rev17)
```
A5000 가설   마스크가 머리 영역 전체 + 좌우 3배 폭을 비운다.  목과 머리가 통째로 지워진 뒤
             모델이 그 큰 빈 공간을 자유롭게 채웠다.  **남은 앵커가 없다**
대조   W1 성공 사례(주둥이 추가)는 **얼굴이 남아 있는 상태에서** 그 앞으로 뻗은 것이다.
       마스크 안에 앵커가 있었다.  이번엔 지운 것이 "구조적으로 의미 있는 전체 부위" 다

🔴 **D22 ② 를 정정한다.**  "빈 공간 91.4% 가 필요하다" 를 좋은 신호로 적었는데,
   빈 공간은 **필요조건이지 충분조건이 아니다.**  앵커가 남아야 한다는 조건이 빠져 있었다
   ⇒ 지표 후보: 마스크 안 자산 셀 / 원 부위 자산 셀 = **앵커 잔존율**.  이번엔 0 에 가깝다

★ **D39-a. 문턱을 언제 정할지 미리 정한다 (rev19)** — 안 적으면 다음 세션이 한 점으로 만든다
```
조건   앵커 잔존율 문턱은 **성공 사례 ≥2점 AND 실패 사례 ≥2점**이 모이기 전에는 정하지 않는다
       그전까지는 값을 **병기만** 한다.  verdict/passes 메서드를 두지 않는 것으로 이미 구조에 남겼다
근거   이 프로젝트가 한 점으로 문턱을 만들어 물린 것이 네 번이다
       (D5 전역실루엣 · D12 연결성분 0.8 · D16 절대IoU 0.99 · D37 halo 비율)
       전부 "측정 대상의 물리를 반영하지 않는 문턱" 이었고, 전부 **정상 동작을 탈락시키거나
       파괴를 통과시켰다**
```

### D38. 🔴 효능 게이트에 **방향 조건**을 넣는다 — 지금 게이트는 파괴를 통과시킨다  (rev17)
```
사실   최대 연결성분 1.000 ≥ 0.8 **통과**.  그런데 제거 730 > 신규 304 로 부피가 줄었다.
       "최대 연결성분" 은 **"한 덩어리인가" 이지 "옳은 방향인가" 가 아니다** (A5000 이 등록해 둔 그대로)

결정   op 별 방향 조건을 게이트에 추가한다.  D26 매핑표와 짝을 이룬다
         add             신규 > 제거          ← "머리를 3개로" 가 이것이다.  이번 결과는 여기서 탈락
         remove          제거 > 신규
         replace_region  방향 무관 (부피 비율만 참고)
         recolor         기하 불변 (신규 = 제거 = 0)
⇒ D5-a ① 이 "신규·제거를 **쌍으로 보고**하라" 였는데 **보고 요구였을 뿐 게이트 조건이 아니었다.**
  이번에 그 틈으로 실패가 통과했다.  게이트로 승격한다
★ 이 프로젝트에서 게이트를 고치는 네 번째 경우다 (D5 전역실루엣 · D12 연결성분정의 · D37 halo비율 · D38 방향)

🔴 **D38-a. `op` 를 넘겨야 검사된다 (rev19 · 최우선 운영 항목)**
```
gate_g2() 에 op 를 안 주면 `direction_ok=None` — **"검사되지 않았다"** 가 된다.
**W10 이 정확히 그 상태였다.**  게이트를 재는 **모든 경로**가 op 를 넘겨야 한다
⇒ 3090·A5000 프롬프트 체크리스트에 넣는다.  안 넣으면 W10 이 그대로 반복된다
⚠️ 그리고 D38 이 W10 을 잡는 것은 **사후 재현**이다.  실제 W10 실행에는 이 검사가 없었다 —
   다음 실행에서 실제로 막는지는 gate_g2(op="add") 가 호출돼야 확인된다
```

🔴 **D38-b. `replace_region` 의 방향 구멍 — 부피 하한을 두지 않는다 (rev19 확정)**
```
맥북 지적   방향 무관이라 W10 같은 파괴가 replace_region 으로 오면 아무것도 안 막는다.
            "부피 비율은 참고값" 인데 참고값에는 문턱이 없다 — D5-a ① 과 같은 모양의 틈이다

결정   ★ **하한을 두지 않는다.**  데이터가 반례를 준다
       W5  레벨1 **성공**(호박 머리, 사용자 육안 인정)   신규 3,185 / 제거 4,069 → 순증 **−884**
       W10 레벨2 **실패**(머리·목 소실)                   신규   304 / 제거   730 → 순증 **−426**
       ⇒ **실패가 성공보다 순증이 크다.**  순증 부호·크기로는 성공/실패를 못 가른다.
         하한을 두면 이미 통과한 레벨1 이 탈락한다

그러면 무엇이 가르나 — **육안이 갈랐다.**  W5 는 호박이 보였고 W10 은 목·머리가 사라졌다
⇒ replace_region 은 방향 조건 대신 **`visual_confirmed` 를 필수로 유지**한다.  이미 G2 에 있다
⇒ 즉 이 구멍은 "메우지 못하는 구멍" 이 아니라 **"수치로 메울 수 없어서 육안이 담당하는 자리"** 다.
  그 사실을 적어 두지 않으면 다음 세션이 한 점으로 문턱을 만든다
```

### D36-a. 🔴 halo 분모는 **실마스크로만** — 의사 마스크와 **10배** 차이  (rev17)
```
의사(W9)   0.0222 / 0.0889 / 0.1458
실마스크   **0.2222 / 0.2100 / 0.1681**       ← halo 1/2/3.  halo-1 이 **10배**다
⇒ W9 잠정치는 **폐기**.  A5000 이 등록한 맹점 D 가 크게 발동했다 — 그대로 썼으면 10배 틀렸다
관측(v2)   halo1 신규189/제거37 · halo2 155/63 · halo3 146/83.  참고 비율 ~4.2× / ~4.3× / ~5.4×
⚠️ 다만 D37 대로 이 비율은 참고값이다.  판정은 깊이맵이었고 **목 자체가 남아 있지 않다** —
   "목 연결부 품질" 을 논할 대상이 없다
```

### D36. ★★ halo 분모는 **halo 대역 전용 바닥값**이다 — 목 z대역이 아니다  (rev15)
```
🔴 rev14 의 "분모는 목 대역 0.1538" 이 **틀렸다.**  목 z44–47 과 "halo 바깥 0–2 vox 대역" 은
   서로 **다른 공간**이다.  A5000 이 마스크 없이 W8 왕복으로 대역 전용 바닥값을 미리 쟀다

  halo   대역 셀   before   신규/제거   합집합   **대역 전용 바닥값**
   1     2,516      44       1 / 0       45      **0.0222**
   2     2,694      41       4 / 0       45      **0.0889**
   3     2,876      43       5 / 2       48      **0.1458**
  (참고) 목 z44–47   100     17 / 1      100       0.1538

⇒ 0.1538 을 halo-1 분모로 쓰면 **약 7배 과소평가**한다.
   W8 의 "0.0701 → 0.2229 (3.2배 과대평가)" 에 이어 **분모 오류 두 번째**이고, 이번엔 반대 방향이다
⚠️ 위 값은 **의사(pseudo) 마스크** 기준(`provisional=True`)이다.
   ★ **실마스크로 재산출하는 것이 W11 의 선행조건이다.  잠정값으로 게이트를 닫지 않는다**

★ **neck 바닥값 0.1538 의 용도 확정 (rev16)** — 안 적으면 다음 세션이 또 분모로 쓴다 (rev14 가 그랬다)
```
용도    편집 후 **목 대역(z44–47) 자체가 얼마나 흔들렸나**의 참고 기준선.  그뿐이다
금지    halo 분모 (D36 이 코드로 거부한다) · 마스크 안 보존 (마스크는 목만 덮지 않는다)
지위    **게이트에 쓰지 않는다.**  100복셀이라 이것도 small sample 이다 — 개수 병기 필수
```

### D37. ⚠️ halo 지표가 **정밀도 한계**에 도달했다 — 비율을 버리고 개수+육안으로  (rev15)
```
사실   대역 합집합이 **45~48복셀**뿐이고 바닥값 분자가 **신규 1개**(halo 1)다.
       ⇒ **복셀 하나가 대역의 2%** 다.  초과배수를 소수점으로 논할 수 없다

결정   ★ halo 판정을 **초과배수 → (원시 개수 + 깊이맵 육안)** 으로 바꾼다
       · 원시 개수를 **반드시 병기**한다.  비율은 "약 N배" 수준의 참고값으로만 쓴다
       · "목이 자연스럽게 이어지는가" 는 근본적으로 육안 질문이다 — D19 의 선례를 따른다.
         비율은 대리 지표였고, 이 표본 크기에서는 **해상도가 없다**
⇒ 이 프로젝트에서 지표를 버리는 두 번째 경우다 (첫 번째는 D5 전역 실루엣).
  둘 다 "지표가 측정 대상의 물리를 반영하지 못한다" 는 같은 병이다

★ **D37-a 확장 (rev16 · 맥북 지적 채택).  halo-1 만의 문제가 아니다**
```
세 대역 바닥값이 **전부** is_small_sample 이다 — 45 · 45 · 48복셀 (문턱 200).
⇒ 문구를 고친다: "halo-1 은 해상도가 없다" ✗ → **"대역 비율 지표 자체가 이 자산에서 해상도가 없다"** ✓
⇒ W11 이 halo-2·3 에서 비율을 **다시 시도하지 않게** 한다.  폭을 넓혀도 안 된다
실증   1/45 = 0.022222 vs 기록된 0.0222 → 초과배수 **1.001배**.
       문턱 1.0 의 어느 쪽인지가 **반올림으로 갈린다.**  신규가 하나만 늘면 2.0배다
```

★ **D37-b 우회 플래그 처분 (rev16 확정)**
```
결정   `allow_ratio_only` 를 **남기되 게이트 경로에서 금지**한다
       · 이름을 `diagnostic_only_ratio` 로 바꿔 오용이 이름에서 보이게 한다
       · `gate_g2()` / halo 판정 경로가 이 플래그를 **절대 세우지 않는지** 테스트로 잠근다
근거   없애면 진단으로도 못 쓰고, 그냥 두면 누군가 게이트에 쓴다.
       이 프로젝트에서 통한 방식은 "규칙" 이 아니라 **함수로 강제** 하는 것이다 (방법론 4조)
```

### D25-b. 🔴 절단 z 정정 — 진짜 극소는 **z=45(28복셀)**, 절단은 **z=46**  (rev15)
```
A5000 W7 원문   z=42:108 → 43:60 → **44:32 → 45:28** → 46:30 → 47:28 → 48:40 → 49:75
                그리고 "z=44 에서 32, **z=45 는 28(진짜 극소)**" 라고 이미 명시했다
🔴 **Chat 이 rev14 에 "극소 z=44(32복셀)" 로 잘못 요약했다.**  A5000 이 W9 에서 다시 잡았다
⇒ D25-a(극소 위 = neck+1)를 적용하면 절단은 **z=46** 이다.  rev14 전제대로면 z=45 —
  **한 칸 차이로 목을 문다.**  마스크 생성 전 확정
★ Chat 의 요약 오류가 실제 결정을 바꾼 세 번째 사례다 (인계 경로 · rev 미도달 · 극소 z)

⚠️ **동점 처리 (rev16)** — z=45 와 z=47 이 **둘 다 28복셀**이다.  코드는 낮은 z 를 고른다
```
근거를 바꾼다.  맥북의 "47 은 머리로 올라가는 구간" 은 프로파일만으로는 못 가린다 (본인도 그렇게 적었다)
★ 더 강한 근거는 **과제에서 나온다: 머리 3개는 목에서 갈라져야 한다.**
  절단이 낮을수록(z=46) 마스크가 목을 더 포함하고, VoxHammer 가 분기를 만들 재료가 생긴다
  z=48 로 자르면 머리 밑동에서만 잘려 가지를 뻗을 자리가 없다

🔴 그런데 이것은 **D25-a 와 긴장 관계**다
   D25-a("극소 포함은 목을 문다")는 **"머리를 다른 것으로 교체"**(레벨1) 에서 도출됐다.
   레벨2 는 **"가지를 뻗기"** 라 요구가 반대다 — 목 재료가 필요하다
   ⇒ 두 과제는 마스크 요구가 다르다.  D25-a 를 레벨2 에 그대로 적용하지 않는다
   ⇒ 지금은 z=46 으로 간다 (A5000 실행 중).  **결과를 보고 재조정한다** — 이음새가 깨지면
     더 높게, 가지가 안 갈라지면 더 낮게.  지금 확정하지 않는다
```

### D27-a. 🔴 인계 **4연속 실패** — 확인은 반드시 **원격에서 읽는 것**이어야 한다  (rev15)
```
W7  경로가 3090 홈이라 A5000 에 존재할 수 없었다 (Chat 실수)
W8  공개 URL pull — **수신자에게 도달 0.**  3090 의 "전송 경로 실증" 은 자기가 자기를 상대로
    당겨 본 것이었고 수신자 확인이 아니었다.  D27④ 의 정확한 사례다
W9  scp 인증까지 성공(sshd 로그: publickey accepted ×2)했으나 **목적지 경로 부재로 0바이트.**
    세션이 같은 초에 열리고 닫혔다 — 인증 후 경로 없음의 전형적 서명

규약 보강
  ① 목적지 디렉터리를 **먼저 만든다** (A5000 이 이번에 둘 다 생성해 뒀다)
  ② 송신자는 `ssh <host> "sha256sum <path>"` 로 **원격에서 읽어** 보고한다.  자기 로컬 확인 금지
  ③ 바이트 일치 ≠ 동작.  **원격 스모크**(import·실행)까지 해야 "됐다" 다
  ④ 그래도 최종 확인은 **수신 세션의 보고**다 (D27④ 불변)

★ **D27-b. 코드도 인계 대상이고, 확인은 바이트가 아니라 API 다 (rev19 · 맥북 제안 채택)**
```
🔴 이번 틈   sha256 대조(D27②)는 "보낸 것과 받은 것이 같은가" 만 본다.
             **"보낸 것이 정본인가" 는 안 본다.**  A5000 이 받은 slatmask.py 에는
             D28-a 구조 강제(grid_source= · require_slat_grid · is_x_symmetric)가 **없었다**
⇒ D27④ 를 강화한다: 수신 세션은 "받았다" 가 아니라 **`assert_required_api()` 통과**를 보고한다
   정본 위치: `server/provenance.py` (repo_manifest/verify_handoff · check_required_api/assert_required_api)
   REQUIRED_API 에 **조항 번호를 함께 싣는다** — "왜 필요한가" 를 다시 조사하지 않게
   ⚠️ 클래스 필드까지 본다.  grid_source 는 dataclass 필드라 최상위만 보면 오탐한다
★ **W11 실증** — A5000 이 동등 검사를 직접 수행: `require_slat_grid()` ABSENT ·
  `is_x_symmetric()` ABSENT · `build_head3_mask(grid_source=)` ABSENT.
  **sha256 은 일치하는데 API 는 없다.**  D27-b 가 잡으라는 상황이 그대로 재현됐다
  실제 시그니처: `build_head3_mask(cells, *, source, width_multiple=1.0, manifest=None)`
  ⇒ 맥북의 D28-a 작업분이 인계본에 반영되지 않았다.  브랜치 확인 필요

### D43. ⚠️ 브리핑 정정 — `rembg` 는 **건너뛰지 않는다**.  BiRefNet 알파가 안 쓰인다  (rev20)
```
실측(A5000)   `image_rgb()` 가 RGBA 를 **흰 배경에 합성해 RGB 로** 만들고
              `rembg.remove()` 가 **무조건** 돈다.  우리가 보낸 BiRefNet 알파는 **u2net 알파로 덮어써진다**
⇒ 편집 경로에서 3090 의 BiRefNet 단계는 **낭비다**.  조건 이미지는 RGB 로 보내도 같은 결과다
⚠️ 단 **생성 경로**에서도 그런지는 미확인이다 — 생성은 :8082 의 다른 진입점을 탄다.  섞지 마라
⇒ 브리핑에 "알파가 있으면 rembg 를 건너뛴다" 고 적었던 것은 **Chat 의 오류**다
```

★ **결정: `slatmask.py` 를 리포에 편입한다.**  지금은 리포 밖이라 **정본이 없고 버전 추적이
  원리적으로 불가능하다.**  3090 담당 파일이지만 리포에 있어야 한다 (PR 로)
  ⚠️ 단 A5000 은 리포 클론이 아니라 scp 로 받으므로, slatmask.py 는 **단독 실행 가능**해야 한다
     (deltacontract + numpy 만).  이 제약은 유지한다
```
⚠️ 현재 3090 과 A5000 의 보고가 엇갈린다 — 3090 은 sha256 일치+원격 스모크 성공을 보고했고
   A5000 은 파일 부재를 보고했다.  **타이밍 문제로 보이나 다음 웨이브에서 수신측이 확정한다**
```

### D31-b. features 재사용 확정 — 검사를 **끄지 않는다**  (rev15)
```
A5000 이 소스를 읽고 결론냈다 (inference.py run_complete_pipeline)
  Step 1 렌더    transforms.json + mesh.ply 존재 시 **스킵** → 80초 절약
  Step 2 피처    **무조건 호출**된다.  존재 검사가 없다 → features.npz 가 매번 새로 생성되고 mtime 갱신
⇒ `EXPECT_FEATURES_REGEN=1`(엄격) **그대로 둔다.**  래퍼 mtime 검증이 정상 PASS 한다
⇒ W1 사고를 다시 여는 것이 아니다.  W1 의 위험은 "추출이 실패했는데 성공으로 위장하고 다른 자산의
  낡은 캐시로 도는 것" 이었고 mtime 검사가 그걸 그대로 잡는다.  **안전을 거래하지 않는다**
편집 1회 예산   ~270초 (렌더 80초 절약 반영)
```

### D33. ★★ **dragon-c 전용 잡음 바닥값** — 소년상 값을 용에 쓰면 3배 과대평가한다  (rev14)
```
표 A — 무차원 (1−IoU).  ★ 영역 분해가 필수다
  영역              before복셀   IoU      1−IoU     신규/제거
  전체               8,000      0.7771   0.2229    1064 / 956
  몸통 z<44          7,181      0.7775   0.2225     939 / 868
  ★ 목 z44–47          100      0.8462   **0.1538**   17 / 1     ← W9 halo 의 분모
  머리 z≥48            719      0.7642   0.2358     108 / 87
  디코더 분산                   0.9983   0.0017
표 B — 길이 (표 A 와 같은 칸에 놓지 않는다, D21)
  before↔왕복  평균 0.218 vox · 95% 0.438 · 최대 1.091
  디코더 분산  평균 0.074 vox · 95% 0.168 · 최대 0.353

★★ 용 0.2229 는 소년상 0.0701 의 **3.2배**다.  0.0701 을 용에 쓰면 halo 초과배수가
   3배 이상 과대평가되고 **"누출 심각" 이라는 오판**이 나온다.  안 재고 W9 를 돌렸으면 그랬다
★ W9 halo 분모는 **목 대역 0.1538** 이다.  전역 0.2229 를 쓰면 과소평가, 0.0701 은 3배 과대평가
⚠️ 단 목 대역은 **100복셀뿐**이라 분산이 크다.  판정에 반드시 반영한다
⚠️ 디코더 분산도 소년상 0.0003 → 용 0.0017 로 **5.7배**다 (렌더 잡음이 W8 엔 있고 W4 엔 없었다).
   두 자산의 수치를 나란히 놓고 비교하지 마라
```

### D31-a. 예산 정정 — 편집 1회 실비용 **≈350초** (rev14)
```
실측(W8/A5000)   slat→GLB 46.4s · 150뷰 렌더 80.4s · features 24.5s · 편집 ~200s
⇒ 렌더·피처 없는 상태에서 편집 1회 = **~305초**, slat 에서 시작하면 **~350초**
⚠️ W1 의 200초는 **캐시 상태** 수치였다.  G4 의 "1회 < 300초" 문턱을 **400초**로 정정한다
★ 절약   W9 는 w8/render/ 를 재사용하면 렌더 80s·피처 25s 를 아낀다
         단 D10 래퍼의 EXPECT_FEATURES_REGEN 판단이 필요하다 (재사용은 mtime 검증과 충돌한다)
```

### D35. ⚠️ 좌표 함정 **다섯 번째** — 좌우 대칭 자산은 X 반전을 IoU 로 못 가린다  (rev14)
```
실측   dragon-c 는 좌우 대칭(날개 둘)이라 전수 탐색 1·2위 격차가 **1.01배**다 (0.8231 vs 0.8146).
       2위는 같은 순열의 **X 반전**이다.  소년상은 4.8배 격차였다 — 그땐 안 보였다
       VoxHammer 자체 복셀화로 교차 확인해도 identity 0.7486 vs mirror-X 0.7463 — 여전히 못 가린다
       Y·Z 는 명확하다 (0.052 / 0.044 로 확실히 오답)

★ 해법(A5000 제안 채택, 최소)   **head3 마스크를 X 대칭으로 만든다.**
  그러면 X 반전이 마스크를 바꾸지 않으므로 모호성이 **무해해진다.**
  D22② 의 "좌우로 머리 폭만큼 넓힌다" 를 그대로 따르면 자연히 대칭이 된다
⚠️ 비대칭 마스크가 필요해지면 X 를 특정할 **fiducial** 을 함께 보내야 한다.  그때 다시 정한다
바닥값(D33)은 영향 없다 — 양쪽에 같은 변환을 걸었고 전역 미러는 IoU 를 보존하며 영역 분해가 z밴드다
```

### D34. 🔴 **`.cbin` 은 slat coords 를 담지 않는다** — 인계에 반드시 동봉한다  (rev14)
```
포맷 실측(3090)   본문 = positions + normals + colors + uvs + indices.  헤더에 voxel_count 하나뿐
복원 시도 2회 실패   정점 비닝 10,154 · 삼각형 무게중심 10,071   (정본 9,591)
                     청크별 일치 0/124 · 26/124
매니페스트로 아는 한계   **청크-z 단위까지**.  목이 cz=5(z 40–47) 에 있다는 것까지는 맞다
                         (A5000 의 z=44/45 와 일치).  복셀 단위는 **A5000 만 낼 수 있다**

⇒ 인계 계약에 **slat coords (N,3) int 동봉**을 필수로 넣는다 (D27 ③).  안 넣으면 매 웨이브 재발한다
⇒ 그리고 이번 웨이브부터는 **마스크를 A5000 에서 산출한다** — 데이터가 거기 있기 때문이다.
  산출기(3090 의 slatmask.py)를 보내고 A5000 이 실행한다.  소유권은 3090, 실행 위치는 A5000

★ 표면 복셀화 오차 정량 (D28 근거)   총 **+673 (+7.0%)** · 청크-z 최대 +300 (cz=1, +12.9%) ·
  목이 있는 cz=5 는 +29.  **경계에서 한 칸 밀리기에 충분하다**
★ 3090 이 방어를 규칙이 아니라 **함수**로 걸었다: `source=` 를 기본값 없이 요구(기본값이 있으면
  다음 세션이 안 적고 지나간다) · `source="surface"` → NotSlatCoords · `"slat"` 이라 적어도
  매니페스트 총계와 어긋나면 거부 — 10,264 ≠ 9,591 에서 **실제로 잡혔다.**  라벨만 바꾸는 우회가 막힌다
```

### D25-a. 절단은 **목 극소 위(neck+1)** 에서 한다  (rev14)
```
극소점 **포함**은 목을 물고 들어가 "자연스럽게"(= 이음새) 를 직접 깎는다.
W6 이 그 실수를 했고 원인은 격자 shift 였다 — **정의와 격자를 같이 적어야 한다**
⇒ W7 인계물은 폐기됐다 (`_handoff-w7-INVALID/`).  그 마스크는 slat 기준 극소점 자리에서 잘렸다.  재사용 금지
```

### D28. 🔴 **복셀 격자의 정본은 `slat coords`(manifest)다** — 오프바이원의 근본 원인  (rev12)
```
증상   같은 dragon-c 를 두 세션이 다르게 봤다
       3090   복셀 10,264 · 목 극소 **z=45**:32          (자기 표면 복셀화)
       A5000  복셀  9,591 · 목 극소 **z=44**:32, z=45:28  (manifest 합계·slat coords 일치)
       청크 수는 124 로 일치.  형상도 일치.  **인덱스가 한 칸 어긋난다**

원인   두 세션이 서로 다른 복셀화를 쓴다.  격자가 다르면 z 인덱스가 밀린다 — 증상 둘은 한 원인이다

결정   ★ **정본은 slat coords / manifest 다.**  VoxHammer 가 그 공간에서 동작하기 때문이다
       3090 의 표면 복셀화는 **진단용**이고 **마스크 좌표의 근거가 될 수 없다**
       ⇒ 마스크는 반드시 slat 격자에서 만든다.  마스크 지문도 그 격자 기준으로 낸다

⚠️ 직접 영향   head3mask 가 "z=45 위" 로 잘렸다면 실제로는 극소점 **위**가 아니라
              극소점 **에서** 잘린 것이다.  이번 게이트의 "자연스럽게" = 목 연결부 품질에 직결된다
★ 좌표 함정 **세 번째**다 (D9 축순열 · D9-b to_glb 회전 · D28 격자 정본).
  같은 파이프라인에서 계속 나온다 — frames.py 에 격자 정본도 명시한다
```

### D32. ★ 서버 간 전송 = **SSH**.  포트는 22 도 80 도 아니다  (rev13 · 사용자 제공)
```
전송 수단   두 GPU 서버는 같은 서브넷(1홉)이고 **SSH 로 직접 오간다.**  scp/rsync 를 쓴다
            ⇒ D27 의 "명시적 전송" 이 이걸로 실현된다.  파일을 손으로 옮기지 않는다

🔴 SSH 포트가 **비표준**이다.  22 가 아니고 **80 도 아니다.**  양 서버 공통
   ⇒ 값은 리포에 적지 않는다 (§7 · 원칙 10).  아래 변수로만 참조한다

환경변수 (실제 값은 각 기기의 리포 **밖** 0600 파일에 둔다)
   GEN_HOST      3090 (오케스트레이터 · t2i · DebugView)
   EDIT_HOST     A5000 (TRELLIS :8082 · VoxHammer)
   SSH_PORT      양 서버 공통.  **비표준 포트다 — 22 를 가정하지 마라**
   값 보관       ~/.ai-ar-v3-hosts  (chmod 600, .gitignore 됨).  세션은 여기서 읽는다
   사용 예       scp -P "$SSH_PORT" <file> "$EDIT_HOST:<path>"

★★ 이것이 **열려 있던 미확인 두 건을 동시에 해결한다**
   ① INFRA.md §1  "A5000 sshd 없음 (SSH:22 refused 는 정상)"
      → 틀린 기록이었다.  sshd 는 있고 **비표준 포트**에 있다
   ② 3090 W1 보고  ":8080 타 UID 소유, 정체 미확인, sudo 불가, HTTP 아님"
      → 그게 sshd 였다.  HTTP 가 아니었던 이유가 이것이다
   ⇒ INFRA.md 의 해당 줄을 정정한다.  "sshd 없음" 을 근거로 판단하던 것이 있으면 전부 재검토
   ⚠️ 인바운드 가용 포트 계산에도 영향이 있다 — 그 자리는 애초에 우리 것이 아니었다
```

### D27. 자산·마스크 **인계 규약** — 경로를 가정하지 않는다  (rev12)
```
🔴 사고   W7 에서 A5000 이 편집을 못 돌렸다.  `~/ai-ar-v3-assets/_handoff-w7/` 는
          **3090 의 홈**인데 Chat 이 그 경로를 A5000 프롬프트에 그대로 줬다.
          다른 기기에서 찾힐 리가 없다.  **Chat 의 실수다** (rev8 의 rev 확인 규칙과 같은 종류)

규약   ① 인계는 **명시적 전송**이다 — SSH/scp (D32).  "그 기기에 있을 것" 을 가정하지 않는다
       ② 수령 확인은 **sha256 대조**로 한다.  파일 존재 확인이 아니다
       ③ 인계물의 **격자 정본(D28)** 과 자산 식별자를 매니페스트에 함께 적는다
       ④ 보내는 세션이 "보냈다" 가 아니라 **받는 세션이 "받았다"** 를 보고해야 인계 완료다
★ 기록   A5000 은 마스크를 스스로 만들 수 있었지만 **만들지 않았다.**
          "브리핑 수치(11,628셀·빈공간 91.4%)가 9,591복셀 자산과 정합하지 않으므로
          내가 만든 마스크는 그 수치와 대응하지 않는다 — 게이트 숫자가 가리키는 대상이 달라진다."
          **아홉 번째 사례.**  일을 진행시키는 대신 멈추고 보고한 것이 옳다
```

### D29. 머리 개수 판정 절차 — 단일 절단·문턱 없음은 **오판한다**  (rev12 · A5000 확정)
```
🔴 실증   **편집 전** dragon-c 가 z=43 절단·문턱 없음에서 **머리 3개로 나왔다.**
          실체는 [1031, 21, 21] — 머리 1개 + 21복셀 파편 2개.
          문턱 없이 셌으면 편집도 안 한 용을 "머리 3개 달성" 으로 보고할 뻔했다

절차   목 위 절단 후 연결성분.  다음 셋을 **전부** 한다
       (a) 최소부피 문턱 스윕      (b) 절단 z 스윕      (c) 연결정의 6/18/26 병기
★ **D29-a. 성분의 z 범위·x 중심을 반드시 함께 본다 (rev20 · W11 실증)**
```
W11 에서 **z=44 · 문턱>20 에서 3개가 나왔다.**  성분 상세를 안 봤으면 "머리 3개 성공" 으로 보고할 뻔했다
  실체   중앙 949 (xc=31.5, z 44→63)  +  좌우 71/69 (xc=18.0/45.0, **z 44–46 뿐**)
  좌우 것은 **위로 안 뻗는** 절단면에 걸린 날개·어깨 조각이다.  문턱>100 에서 사라진다
⇒ 개수만으로는 못 가른다.  **성분마다 z 범위와 x 중심을 병기**하고,
  "머리" 는 **절단면 위로 실제로 뻗는 것** 으로 정의한다
★ 스윕 없이 세면 오판하는 사례가 이걸로 **두 번째**다 (W7 편집 전 용 [1031,21,21])
```

유효 구간   z=44~50 · 문턱 >20복셀.  이 구간에서 6/18/26 전부 **1개** (1007→782복셀) 로 안정
★ 편집 전 기준선 = **머리 1개, ~1007복셀.**  편집 후는 이 기준선과 비교한다
```

### D31. VoxHammer × 우리 자산의 **선행 비용** — 200초는 캐시 수치다  (rev12)
```
VoxHammer 는 --input_model 로 **GLB 메시**를 받는데 dragon-c 는 **청크 + slat** 형태다
선행 작업   ① slat → GLB 디코드 (GPU ~6.2 GB · torch.no_grad() 필수, D18)
            ② 150뷰 렌더 (bpy)
            ③ features.npz 추출 (GPU) ← 맨 except 가 OOM 을 삼키는 그 단계.  D10 래퍼가 잡는다
            ④ .npy 마스크 → voxels_delete.ply 변환
⚠️ 현재 outputs/render/features.npz 는 **소년상 샘플** 것이라 재사용 불가
⚠️ W1 의 200초는 렌더·피처가 **캐시된 상태** 수치다.  실소요는 "편집 1회" 가 아니라 전체 파이프라인 1회
⚠️ 잡음 바닥값 0.0701 도 **소년상** 값이다.  용(9,591복셀)은 형상·표면적이 달라
   **dragon-c 전용 왕복 바닥값**을 따로 재야 halo 초과배수를 낼 수 있다
★ ①②③은 **마스크가 없어도 할 수 있다** → 마스크 재생성과 병렬 진행 가능
```

### D25. "머리 영역" 은 **목 잘록함** 기준으로 정의한다 — z 분위수는 틀린다  (rev11)
```
3090 이 실제로 밟았다: top_region_cells(0.35) 의 z 하한(z=41)에서 XY 를 재니 **날개가 섞여**
폭 46, 마스크가 격자의 14.5% 가 됐다.  목 잘록함 위(z=46)로 다시 잡아 4.44% 로 줄였다
⇒ 부속지(날개·팔·꼬리)를 가진 자산에서 "상단 x%" 는 부위 정의가 아니다.
  단면 극소(잘록함)를 경계로 쓴다
```

### D23. 🔴 **LLM 은 좌표를 만들지 않는다** — 모듈 규약에서 **결정**으로 승격  (rev11)
```
D9(축 순열)·D13(strict_containment) 과 같은 급의 불변식이다
  좌표의 유일한 진실은 **클라가 준 마스크**다.  LLM 은 {op, target_prompt} 만 낸다
  마스크 밖 불변은 **서버 코드가 강제**한다 — 프롬프트로 부탁하지 않는다
  근거: LLM 의 3D 좌표 직접 예측은 경계 이탈이 잦다 (LayoutVLM 계열 지적)
코드 정본   server/llm.py 의 CoordinateLeak.  bbox·voxel_coords·offset·mask_cells·position·region
            6종 전부 거부.  문자열 자리에 숫자 배열이 와도 거부.  plan_edit 수준에서도 거부
★ 기록   맥북의 좌표 검사가 `\b` 단어경계를 써서 `_` 를 단어문자로 취급했고, 그래서
          **가장 그럴듯한 유출 이름 두 개(voxel_coords·mask_cells)가 통과하고 있었다.**
          테스트가 그걸 잡았고 토큰 분해로 고쳤다 — **일곱 번째 사례**
```

### D24. 색 편집은 **별도 경로**로 정식화한다 — 복셀 격자를 경유하지 않는다  (rev11)
```
발견(W6/3090)   레벨1(색만)은 **된다.**  단 S2 관통 경로 밖이다
                hue 58.3° → 27.0° (이동 **31.3°**, D5 기준 >30° 충족) · 기하 바이트 **53/53 보존** ·
                절감 70.46% · 색 바꾼 청크 53 / 부모 승계 128
원인   S2 는 .cbin → 표면복셀화 → occupancy → occupancy_to_mesh → 재인코딩 인데
       **occupancy_to_mesh 가 정점·면만 낸다.** 색 채널이 없어 그 경로를 타면 색이 통째로 사라진다
       ⚠️ 색은 자산에 **실제로 있다** — base flags=0b0011(COLOR|NORMAL),
          눈사람 (148,147,147) · 호박 (209,148,71).  **버리는 건 우리 경로다** (버그 아니라 설계 선택)

결정   ★ (a) 채택 — 색 편집을 별도 경로로 정식화한다
       마스크에 걸린 청크만 디코드 → 정점 색 교체 → 재인코드, 나머지는 부모 바이트 승계
       이유 ① 이미 동작한다  ② **복셀 격자를 아예 안 거쳐서 기하가 원본 그대로다**(53/53)
            ③ GPU 불필요  ④ 레벨1 의 진짜 최소 구현
🔴 **D24-a. 이 경로에서 `canonicalize()` 를 다시 부르면 안 된다.**
   정렬 키의 tie-break 가 **속성 raw 바이트**라, 색을 바꾼 뒤 재정규화하면 정점 순서가 재배열되고
   `positions`·`indices` 가 **전부 달라진다** — D24 의 근거(기하 100% 보존)가 조용히 사라진다
   ⚠️ 골든 청크는 정점이 3~35개뿐이라 이 함정이 **재현되지 않는다.**  맥북이 정점 48개짜리
      합성 청크를 따로 만들어 재배열을 재현한 뒤 테스트로 박았다 — **여덟 번째 사례**
   정본: `server/pipeline/recolor.py` · 부수: `decode()` 반환은 읽기 전용 뷰라 복사해서 쓴다

🔴 **D24-b. "hue 58.3°" 를 실측치로 인용하지 마라.**
   눈사람 (148,147,147)은 **채도 0.05 = 사실상 무채색**이고 그 색상각은 잡음이 정한다.
   같은 색이 3090 에선 58.3°, 맥북 픽스처에선 359.5° 로 나왔다 — 둘 다 맞는 값이 아니라 **둘 다 무의미**하다.
   판정 자체는 성립한다(after 가 채도 0.67 주황이라 이동량이 크다).  지표가 `is_achromatic` 로 그 사실을 낸다
   실측 확정치: 맥북 골든 24청크 — 기하 바이트 **14/14 100% 보존** · hue 이동 **34.3°** · 절감 40.84%

⇒ op 집합에 **"recolor"** 를 추가한다.  3090 의 _tools/level1_recolor.py 를 맥북이
  server/pipeline/recolor.py 로 승격한다 (지금은 진단 스크립트지 파이프라인이 아니다)
```

### D26. `op` → 소비자 매핑표.  방어는 **소비자 쪽**에 둔다  (rev11)
```
op                소비자              비고
replace_region    assemble / VoxHammer  레벨1(호박 머리) 는 assemble 로 달성됨
recolor           recolor 경로 (D24)    복셀 격자 미경유. GPU 불필요
add               **VoxHammer 전용**    ★ assemble 로 보내면 **조용히 아무 일도 안 일어난다** (D22 ①)
remove            미정                  아직 0회

**D26-a. recolor 행 확정 (rev14 · 맥북 판단 채택)** — 능력표 정본은 `server/dispatch.py` 의 `CONSUMERS`
```
op \ 소비자        assemble   recolor   voxhammer
replace_region        ✅        ✗          ✅
recolor            🔴 거부      ✅      🔴 거부
add                🔴 거부      ✗          ✅
remove                ✅        ✗          ✅   ← ⚠️ 실행 0회. "시도 가능" 이지 "검증됨" 이 아니다

recolor → voxhammer 거부 근거   레벨1 의 정의는 **기하 불변**인데 VoxHammer 는 재디코딩하므로
                                기하가 흔들린다 (마스크 밖 0.853 = 바닥값 대비 2.10배).
                                색만 바꾸자고 그 경로를 타면 **레벨1 의 판정 조건 자체가 무너지고** GPU 까지 쓴다
recolor → assemble 거부 근거    occupancy_to_mesh 가 색을 안 낸다 (D24 원인 진단 그대로).
                                add→assemble 과 **같은 종류의 조용한 실패**다
★ 자동 강등 없음이 전수 확인됐다.  미지원이면 예외로 멈춘다.  check_supported 가 bool 이 아닌 것도 잠갔다
★ D30 은 세 곳을 고치는 것만으로는 다음에 또 어긋난다 — **테스트가 ①②③ 결속을 잠근다**
  (enum==OPS · 모든 op 가 프롬프트에 · replace_region 설명에 "색" 이 남으면 실패 · 소비자표가 스키마의 모든 op 를 아는가)
```

**D28-a. 격자 출처를 구조적으로 강제한다 (rev14 승인)**
```
현재   assert_slat_grid(grid_source, ...) 가 **문자열**을 받아서, 호출부가 자발적으로 부를 때만 검사가 돈다
승인   `MaskResult` 에 `grid_source` 필드를 넣어 **구조로 강제**한다.  mask.py 시그니처가 바뀐다
근거   방법론 4조 — 규칙만 적고 함수를 안 주면 그 규칙은 안 지켜진다.  좌표 함정이 이미 다섯이다
       3090 의 slatmask.py 가 지금 새로 만들어지는 중이라 배선 영향이 가장 작은 시점이다
```

🔴 **D30. `recolor` op 는 llm.py 의 세 곳을 동시에 고쳐야 한다.**  소유자 = **맥북**
   ① `OPS` 상수  ② `EDIT_SPEC_SCHEMA.op.enum`  ③ 시스템 프롬프트의 `replace_region … (색·재질 변경 포함)` 줄
   하나만 고치면 스키마가 거부하거나 프롬프트가 반대로 유도한다
   ⇒ W7 에서 "머리만 빨갛게" 가 `replace_region` 을 낸 것은 **모델 실패가 아니다.**
     `additionalProperties:false` + enum 이라 API 가 recolor 를 낼 수 **구조적으로 없었다**.
     확률 문제가 아니다.  3090 이 프롬프트를 돌려 맞추기 전에 원인을 먼저 특정했다 — **열 번째 사례**

★ **D19-a. 레벨1 판정 그림은 색 렌더가 정본이다.**  레벨1 은 기하가 불변이라
  **실루엣도 깊이맵도 before/after 가 똑같이 나온다** — 둘 다 색 채널을 안 본다.
  D19 가 오목 디테일에 깊이맵을 지정한 것과 정확히 같은 논리다.  판정 그림을 레벨별로 지정한다
  ⚠️ 운영: `/debug/*.png` 아래에 점 구분 경로를 만들지 마라 (422 충돌. FastAPI 는 먼저 등록된 라우트가 이긴다)

🔴 방어를 llm.py 안에 넣지 않는다 — 분기가 안으로 들어오면 게이트가 LLM 출력에 의존하게 된다.
   소비자(3090 배선)가 op 를 받고 자기가 처리 못 하는 op 면 **거부**한다
★ 폴백은 op 를 추측하지 않는다 (맥북의 의도적 설계).  "머리를 3개로" 는 명백히 add 지만
  폴백은 replace_region 을 낸다.  키워드 매칭을 넣으면 "LLM 없이도 대충 된다" 는 착시가 생기고
  그 착시가 자연어 경로의 실제 상태(실호출 0회)를 가린다.  source 필드로 항상 구분된다
```

### 편집 레벨 정의
```
레벨 1  색·재질 변경     흰 눈사람 → 머리만 주황색       기하 불변
레벨 2  형태 변경        머리(구) → 할로윈 호박          복셀 추가·제거 필요

M1(관통)  레벨2 를 assemble 로 달성 — 경계 이음새 품질은 포기
M3(VoxHammer)  같은 레벨2 를 이음새까지 자연스럽게 — 이게 업그레이드분
```

---

## 4. 현재 진척

### ⚠️ "증명됨" 재분류 — rev3 에서 등급을 내렸다

| 항목 | 계측치 | 등급 | 이유 |
|---|---|---|---|
| `.cbin` 포맷·계약 | 골든 200 · 51/51 | **계약 잠김** | 자기 일관성만. 실동작 아님 |
| 라쏘 → 부피 마스크 | 정점 357,773 · 188셀 (1.9%) | **동작 확인** | 실기 제스처로 확인됨 |
| 마스크 밖 보존 (B) | 바이트 128/128 동일 | **🔴 재검증 필요** | 편집이 no-op 인 상태에서 잰 값 |
| 부분 전송 (C) | 2.72/17.22 MiB (16%) | **🔴 재검증 필요** | 위와 같음 |
| Unity in-place | EntityId 152/152 · 10.35 ms | **동작 확인** | 교체 자체는 실기 확인. 단 내용이 안 바뀐 상태 |
| `assemble.py` | 조립 실루엣차 6.1~8.0% | **동작 확인** | 형태가 실제로 바뀐 유일한 경로 |
| 델타 부기 | 독립 검증 5/5 | **계약 잠김** | 배치 유도 규칙은 코드로 강제됨 |

> **⚠️ 원안 조사순서 2번(Unity in-place 가능 여부)은 재조사하지 않는다** — 교체 메커니즘 자체는 확인됐다.
> 다만 **"바뀐 내용을 교체한다"** 는 M1 에서 처음 증명된다.

### 🔴 실패했음 — 다시 시도하지 않는다
```
RePaint-lite 로 형태 변경        활성 복셀 100% 재샘플링해도 실루엣 대칭차 0.46%/0.70%
  원인 ① masked_sampler Phase 1 이 coords 고정 → 복셀 추가·제거 불가
  원인 ② 조건이 parent/input.png 하나뿐 → prompt 가 no-op
  ⇒ 원안 5번의 "RePaint 로 난이도를 줄인다" 는 실패한 길로 되돌아가는 것이다

해시 비교로 델타 결정             재디코딩하면 152/152 전부 다른 해시 → 절감률 0%. 부기로 정한다
좌표 스케일로 크기 조절           6-이웃 유지율 s=1.5 → 50%, s=2.0 → 0%
소수 평행이동                     +0.5 에서 4,110 → 914복셀 (78% 소실).  정수 이동만
부기를 diff 로 유도               비워진 청크 8개가 목록에서 사라지고 옛 기하가 남았다
Dilate 를 클램프보다 먼저         합성 입력엔 안 나오고 실제 제스처로만 터진다
solidify(부피 채움)               SLat 은 껍질 표현. 빈 내부에 latent 이 없다 (버그 아님)
```

### ✅ W1 에서 새로 확정된 사실

| 항목 | 실측 | 의미 |
|---|---|---|
| **VoxHammer 가 복셀을 추가한다** | 얼굴 앞 **주둥이(snout) 생성** · 마스크 한정 최대뷰 14.32% | 🔴 **가장 중요.** RePaint-lite 가 `coords` 고정으로 원리적으로 못 하던 것이 된다. 레벨2 의 전제가 성립한다 |
| VoxHammer 예산 | VRAM 13.85 GiB · CPU RSS 73.68 GiB · 200초 | README 요구 40 GB 를 24 GB 에서 완주. KV 의 CPU 오프로드가 실제로 작동 |
| 리포·계약 | 51/51 · gitleaks 0 · 커밋 902cd4f | G1 맥북분 닫힘 |
| `:8083` | 회수 완료 · ngrok 502 대기 중 | 새 서버 bind 시 즉시 인계 |
| 3090 GPU | 38 MiB / 24 GB | 전부 가용 |

> 🔴 **W1/A5000 의 G4 는 숫자로는 FAIL(2.58% < 5%)이지만, 세션의 질문에 대한 답은 YES 다.**
> 게이트가 틀렸지 엔진이 틀린 게 아니다 (D5). **"VoxHammer 는 형태를 못 바꾼다" 로 기록하지 않는다.**
> 그리고 그 세션은 자기 계측 스크립트의 허수(`fillPoly` even-odd 상쇄로 40.66%)를 보고 전에
> 스스로 잡아냈다 — 이 프로젝트가 여섯 번 물린 "대리 지표" 함정을 처음으로 피한 사례다.

### ❓ 미검증
```
- cbin-delta 전 구간이 실제 콘텐츠 변화를 통과시키는가          → M1(S2) 이 답한다.  최우선
- 우리 자산·큰 마스크에서 신 지표(D5)가 무엇을 내는가           → W2-A5000 재계측
- TRELLIS 1 image-large 생성 품질 (D1 의 대가)                  → S3·S6
- glTF(분리형) 전환이 gltfast 로딩·청킹에 미치는 영향
- A5000 워킹트리 미커밋 파일 1건(build_untracked=1)의 정체       → W2-A5000
- :8084 레거시의 build/started_at (인증 키 미확보) · :8080 정체  → 보류. 우리 경로와 무관
```

---

## 5. Plan — 세션 단위

> 각 세션은 **하나의 게이트**를 닫는다. 판정은 숫자 + 육안 산출물이다.

### 웨이브 배치 (원칙 8)
```
W1  ☑ [맥북] 리포 부트스트랩          PASS
    ☑ [3090] 인프라 확인 · :8083 회수  PASS
    ☑ [A5000] VoxHammer 원본 데모      게이트 FAIL / 실질 PASS (D5 로 게이트 재정의)

W2  [맥북]  S2 순수 파이프라인 + pytest   의존: W1-맥북(리포) ✅        ─┐
    [3090]  자산 확보 + :8083 스켈레톤     의존: W1-3090 ✅ · A5000:8082 ─┼─ 동시 발사
    [A5000] D5 신 지표로 W1 산출물 재계측  의존: W1-A5000 ✅            ─┘
    ★ 셋 다 서로 의존이 없다.  맥북은 네트워크·GPU 를 안 쓰고,
      3090 은 맥북 모듈을 안 쓰며, A5000 은 이미 있는 before/after.glb 만 본다

W3  ⚠️ **A5000 가용성이 단일 장애점이다.** W1(03:00) 200 → W2(03:30~) 전 포트 refused.
      호스트는 살아 있다(ping 0.2 ms).  A5000 세션은 :8082 미접촉·GPU 34 MiB 를 보고했다
      ⇒ 웨이브 발사 전 `/v2/trellis/health` 확인을 절차에 넣는다

    [A5000] :8082 복구 → 인코드·디코드 왕복 대조군(잡음 바닥값)   의존 없음  ─┐
    [맥북]  PROGRESS rev6 커밋 · D9 좌표규약 코드화 · D5-b 지표 수정  의존 없음 ─┼─ 동시
    [3090]  DebugView 먼저 → watcher 가 잡은 자산으로 .cbin 확보    의존 완충  ─┘
    ★ 3090 은 W2 에서 이미 watcher(90분·15초 재시도)를 걸어놨다.  A5000 이 올라오면
      자동 제출된다.  그래서 3090 프롬프트는 **DebugView 부터** 시켜 의존을 뒤로 민다

W4  ☑ 지표·바닥값·계약 정리
W5  ☑ **재관통 → G2 닫힘 (레벨1 달성, 사용자 인정)**

W6  [맥북]  자연어 편집 스펙 모듈 (LLM)   GPU·네트워크 0   ─┐
    [3090]  🔴 **용 생성 시험** + 레벨1(색만) 1회  :8082 사용 ─┼─ 동시
    [A5000] 쉼 — :8082 유지만                                ─┘
    ⚠️ D6/D14: :8082(생성)와 VoxHammer(편집)는 **상호 배타**다.
       그래서 A5000 의 VoxHammer 작업을 이 웨이브에 넣지 않는다

W6  ☑ [맥북] llm.py (좌표 거부 음성대조) · ☑ [3090] **용 생성 성공 · head3mask · 레벨1 색 확인**

W7  ☑ [맥북] recolor 정식화 · ☑ [3090] **자연어 실호출 6/6 성공** · ❌ [A5000] **미실행**(인계 실패, D27)

W8  ☑ [맥북] D30 결속 테스트 · test_dispatch 23건 · D28 frames.py
    ☑ [A5000] **선행 파이프라인 완주 + dragon-c 바닥값 확정(D33)**
    ◐ [3090] 마스크 재생성 **불가**(D34: .cbin 에 slat coords 없음).  산출기 slatmask.py 는 완성

🔴 **W8 에서 rev13(D32 SSH 8080)이 세션에 도달하지 않았다.**  3090 이 SSH **22** 를 보고
   "push 불가" 로 결론내고 공개 URL pull 우회로를 만들었다.  원래 필요 없던 작업이다.
   판본 미도달이 **또** 실제 결정을 바꿨다 — 원칙 9 의 세 번째 위반이다

W9  ☑ [맥북] D28-a 구조강제 · D33 NoiseFloor 타입 · D35 · D31-a · pytest 166
    ◐ [3090] slatmask.py 전송(sha256+원격스모크 보고) · test_slatmask 12건
    ❌ [A5000] **미수령 → 편집 미실행.**  단 **D36/D37/D25-b/D31-b 를 마스크 없이 확정**

W10 ☑ 인계 닫힘(SSH_PORT=8080 확정) · ☑ 마스크 산출 · ☑ 맥북 D36/D37
    🔴 **편집 FAIL — 머리 1개.**  원인 가설 D40(조건 이미지) · D39(앵커 소실)

W11 ☑ 조건 교체로 **방향 역전 성공**(파괴→생성).  🔴 머리는 여전히 1개
    ⇒ **D41: 진짜 병목은 공간이다.**  결과가 격자 천장 z=63 에 닿았다

W12 ★ 순차.  **같은 뿌리(공간 부족)를 두 방향에서 친다**
    ① [3090]  머리 셋이 **좌우로 벌어진(부채꼴)** 조건 이미지 — 수직으로 세우지 않는다.
              남은 여유는 가로다 (마스크 box_x 36칸 vs 세로 17칸)
              ★ D43: rembg 가 무조건 도니 **BiRefNet 단계를 편집용 조건에는 쓰지 마라**
    ② [A5000] 마스크 **z 하한을 낮춰 목 전체 포함**(D25-c) + 새 조건.  gate_g2(op="add") 필수
    ③ [맥북]  slatmask 리포 편입 · gate_g2/provenance 를 A5000 이 쓸 수 있게 · D29-a

W13 S6 레벨2 육안 판정 → M5 Unity → M6 멀티유저
```

### 🔴 W7 선행조건 — 사용자 조치 필요
```
ANTHROPIC_API_KEY 가 없다.  그래서 자연어 경로는 코드·계약이 섰는데도 **실호출 0회**다.
폴백만으로 게이트를 닫으면 안 된다 (원칙 7).
⇒ 키를 **3090 의 .env**(리포 밖, 0600)에 둔다.  3090 이 오케스트레이터이므로 그쪽이 자연스럽다.
  맥북은 오케스트레이터가 아니다.  키가 없으면 W7-3090 은 배선까지만 하고 실호출은 다시 미검증으로 남는다
```

### S1 — 부트스트랩 · 인프라 확정  `M0`
```
할 일
  1. INFRA §8 확인 명령 전부 실행 → docs/INFRA_LIVE.md 에 기록
  2. :8083 회수 — ai-ar-prototype 오케스트레이터 내리고 새 서버 인계, ngrok URL 승계
  3. gh repo create rockthepeople/ai-ar-v3 --public
     .gitignore + gitleaks pre-commit + .env.example 을 **첫 커밋에**
  4. cbin-delta 편입 (복사).  재구현하지 않는다
  5. CLAUDE.md — 기기별 경계 · 개발 명령 · 계약 변경 규칙 · §7 보안 규칙

게이트 G1   run_conformance.py 51/51 · gitleaks 0건 · :8083 새 서버 /health 200
비용 상한   1세션
```

### S2 — ★★ 관통: GPU 없이 눈사람에 호박 머리를 붙인다  `M1`
```
목적  전 구간을 **눈에 보이는 변화**로 한 번 통과시킨다.  이게 이 프로젝트 최초의 실동작 증명이다.
      모델·GPU 를 쓰지 않는다.  자산은 고정본 2개를 미리 확보해 파일로 둔다.

준비  눈사람(base) — ai-ar-samples/sample-snowman 재사용
      호박(donor)  — 1회만 생성해 파일로 고정.  이후 S2 는 GPU 를 쓰지 않는다

할 일
  1. base 를 64³ 복셀화 → 8³ 청크 → .cbin 세트 (partition.py)
  2. 머리 마스크를 서버측에서 산출 (find_head_bbox 이식).  halo 팽창은 **클램프 뒤에**
  3. assemble.py — 마스크 자리를 비우고 donor 를 크롭·정수 평행이동으로 삽입
     ⚠️ 스케일 인자 없음. 크기는 donor_crop_fraction 으로만
  4. 부기 = 배치에서 유도.  전칭 규칙 ∀c ∈ 부기 : c ∈ changed ∨ c ∈ removed
  5. 마스크 밖은 **부모 바이트 승계**.  재디코딩본을 쓰지 않는다
  6. DebugView 를 :8083 에 — 원본 / 결과 / 마스크 / 변경청크 하이라이트 4분할
  7. 점유 교집합이 몇 셀을 지웠는지 로그 (그 단계가 밥값 하는지 자증)

게이트 G2  — **D5/D5-a/D5-b 로 갱신됨 (rev6)**
   ★ 전제   **strict_containment=True** (D13).  끄면 보존이 조용히 0.345 로 무너진다
   ★ 효능   DebugView 에서 **호박 머리가 육안으로 보인다** (사용자 확인) ← 아직 미달
            AND 마스크 내 신규 복셀 > 0
            AND **(신규 ∪ 제거)의 최대 연결성분 ≥ 0.8** (D12 로 변경)
            AND churn(안)/churn(밖) ≥ 3.0
   ★ 보존   승계 청크 바이트 동일률 100%  AND  재디코딩 영역 기하 거리 ≤ 잡음 바닥값(D14)
   ★ 절감   전송 절감 > 40%

W3 실자산 실측 (3090, 눈사람 2단 + 호박)   신규 4,079 / 제거 3,658 · 마스크안 IoU 0.0565 ·
   마스크밖 IoU 1.000000 · 바이트 1.000000 · 절감 67.61% · 부기 192청크(changed 40 / removed 152)
   ⇒ **(A)(B)(C)가 실자산 위에서 처음으로 동시에 성립했다**
   🔴 단 **육안 판정은 미달** — 기증자가 호박 위쪽 30%(뚜껑+꼭지)뿐이라 얼굴이 없다 (D11)
   🔴 그리고 보존 판정의 잡음 바닥값이 아직 없다 (D14)
   ⇒ 이 셋이 동시에 나오면 (A)(B)(C)가 **처음으로 함께** 성립한다

W2 합성 픽스처 실측 (맥북, GPU 0)   신규 668 / 제거 928 · 마스크안 IoU 0.1618 ·
   마스크밖 IoU 1.000000 · 바이트동일 100% · 절감 68.36% · pytest 26/26
   ★ 음성 대조 통과 — **no-op 이 보존·절감을 만점 통과하고 효능에서만 떨어진다**
   ⚠️ 단 합성 디코더는 복셀-국소라 마스크밖 1.000000 이 구조적이다 (D5-b).  실자산에서 재측정
비용 상한   2세션
```

### S3 — 생성 파이프라인 실연결  `M2`
```
S2 의 고정 자산을 실제 생성으로 교체한다.  경로는 이미 뚫려 있다.
  자연어 → Z-Image-Turbo → BiRefNet → TRELLIS 1 image-large → glTF + .cbin
  ★ A5000 에서 TRELLIS.2-4B 를 내린다 (D1, 상주 6 GB 회수)
  ★ 중간 RGBA 이미지를 보관한다 — M3 편집의 조건 이미지로 재사용

게이트 G3   "흰 눈사람" 자연어 → S2 와 같은 관통이 끝까지 돈다 · 육안 확인
비용 상한   1세션
```

### S4 — 🔴 VoxHammer GO/NO-GO 스파이크  `M3`
```
목적  "우리 GPU 에서 형태를 실제로 바꾸는가" 만 가른다.
      원본 repo 를 원본 샘플로 그대로 돌린다.  우리 코드·자산과 섞지 않는다.
      (백본은 D1 로 확정 · RAM 은 D4 로 해소 — 둘 다 재론하지 않는다)

게이트 G4   ~~실루엣 대칭차 > 5%~~ → **D5 로 폐기·재정의**
   W1 결과   VRAM 13.85 GiB ✅ · CPU RSS 73.68 GiB ✅ · 200초 ✅ · 전역 실루엣 2.58% ✗
   ★ 실질 판정 **PASS** — 마스크 영역에 **없던 복셀(주둥이)이 추가됐다.**
     이것이 레벨2 의 정의이며 RePaint 가 원리적으로 못 하던 것이다
   ⇒ VoxHammer 는 계속 간다.  단 **S2 관통이 먼저 출하된다** (D3 불변)
   ⇒ 신 지표 재계측은 W2-A5000 에서. 우리 자산 효능은 S5
```

### S5 — VoxHammer 효능 게이트  `M3`
```
우리 자산 + S3 의 중간 이미지로 조건화.
  레벨1  "머리를 주황색으로"
  레벨2  "머리를 할로윈 호박으로"

게이트 G5   레벨1  마스크 안 평균 hue 이동 > 30°   AND  마스크 밖 기하 거리 < 잡음+1%
            레벨2  마스크 안 실루엣 대칭차 > 15%   AND  마스크 밖 기하 거리 < 잡음+1%
            AND  이음새가 S2(assemble) 보다 개선됐다 (사용자 육안 비교)
   FAIL → S2 경로 출하.  되돌리는 비용 0
비용 상한   2세션
```

### S6 — ★ 사용자 최종 판정  `M4`
```
DebugView 에서 S2 결과와 S5 결과를 나란히 본다.
  · 레벨1·레벨2 가 요구를 만족하는가
  · D1 의 생성 품질 대가가 허용 가능한가
  · 출하 경로를 assemble 로 갈지 VoxHammer 로 갈지 확정
**여기까지 Unity 는 한 줄도 건드리지 않는다.**
```

### S7 — Unity 이식  `M5`
```
LassoVolume.cs + ChunkBin.cs + ChunkContracts.cs 를 그대로 쓴다.
ai-ar-v2 의 AR 배치/앵커 + ai-ar-prototype 의 청크 GameObject/in-place 교체를 합친다.
게이트 G7   실기 라쏘 → 편집 → 교체 · EntityId 유지율 100% · apply < 50 ms · 앵커 손실 0
            ★ 그리고 **화면에서 실제로 머리가 바뀌는 것이 보인다**
```

### S8 — 멀티유저  `M6`
```
겹치는 수정 범위 없음 가정.  변경 .cbin 만 브로드캐스트.
게이트 G8   두 클라이언트가 서로의 편집을 다운타임 없이 반영
```

---

## 6. 세션 로그 (append only)

| 날짜 | 세션 | 게이트 | 결과 | 다음 |
|---|---|---|---|---|
| 2026-08-04 | Chat | — | 계획 확정. 조사순서 2번 삭제, 폴백을 RePaint→assemble 로 교체 | S1 |
| 2026-08-04 | Chat | — | rev2: D1 백본 TRELLIS 1 통일 확정 · D2 cbin-delta 채택 | S1 |
| 2026-08-04 | Chat | — | rev3: **D3 관통 우선 재배열** — assemble 경로를 폴백에서 S2 로 승격. D2 강등(계약≠동작). (B)(C)를 재검증 대상으로 재분류. D4 RAM 125/124 GB 확정, 리포 확정 | S1 |
| 2026-08-04 | Chat | — | rev4: 원칙 8(웨이브 스케줄링) 추가. W1 3발 발사 — 맥북/3090/A5000 | W1 보고 대기 |

| 2026-08-04 | 3090 | — | W10: **인계 닫힘** — A5000 기기 출력으로 확인(sha256 3b20d566… · 원격 스모크 exit=0 · A5000 매니페스트 voxel_total=9591 sum_ok). ~/.ai-ar-v3-hosts 전달 | A5000 실행 승인 |
| 2026-08-04 | 맥북 | — | W10: D36 region 구분(목→halo 거부) · D37 비율 단독 거부 · D25-b z=46 · pytest 177/177 · db6906a·2645df9 | — |
| 2026-08-04 | A5000 | **W10** | 🔴 **FAIL — 머리 1개.** 마스크 11,628셀·빈공간 91.79%·z=46. 신규304/제거730. 목·머리 소실 후 뿔형 대체. halo 실마스크 바닥값이 의사 대비 **10배**. SSH_PORT=8080 자기 오류 정정 | W11 |
| 2026-08-04 | A5000 | **W11** | ★ **조건 교체로 방향 역전**(제거730→471, 신규304→532, direction_ok F→T). 🔴 머리 여전히 1개. **결과가 격자 천장 z=63 에 닿았다** → D41. 앵커 18.1%는 무효과. rembg 브리핑 오류 적발 | W12 |
| 2026-08-04 | Chat | — | rev20: **D41 진짜 병목 = 공간 부족**(자산이 격자를 세로로 꽉 채운다. W1 성공은 여유 있던 축을 썼다) · **D25-c D25-a 를 레벨2 에 적용한 것이 잘못** · D29-a 성분 z범위·x중심 병기 · D43 rembg 정정(Chat 오류) · D27-b 실증 | W12 |
| 2026-08-04 | 맥북 | — | W11③: D38 방향조건(W10 탈락 재현) · D39 문턱없음 구조화 · D36-a 실측 교체(10.0배) · D35-a "우연" 정량화 · provenance API 검사 · pytest 211/211 · 099f19b·9009589 | — |
| 2026-08-04 | Chat | — | rev19: **D38-a op 미전달 시 미검사**(W10 이 그 상태였다) · **D38-b replace_region 하한 거부** — W5 성공이 순증 −884, W10 실패가 −426. **실패가 성공보다 순증이 크다** → 수치로 못 가른다, 육안이 담당 · **D39-a 문턱 조건 사전 확정**(성공2+실패2) · **D27-b API 검사 + slatmask 리포 편입** | A5000 대기 |
| 2026-08-04 | 3090 | — | W11①: 조건 이미지 **dragon3-c 확보**(목 셋·얼굴 셋). "three heads"론 실패, "exactly three separate necks…wide gaps"+"hydra"가 갈랐다. 구도 일치. 🔴 **날개 소실 = 새 교란 변수** | A5000 |
| 2026-08-04 | Chat | — | rev18: **D40-a 조건 이미지 확보 + 교란 변수 기록.** 마스크 밖 변화를 W10 과 직접 비교 불가 — 조건이 다르다. (a)파이프라인 불완전성 vs (b)조건 차이가 이번 런에선 분리되지 않는다 | A5000 실행 |
| 2026-08-04 | Chat | — | rev17: **D40 조건 이미지가 이긴다 — RePaint 실패 원인 ②가 VoxHammer 에서도 살아 있었다**(최상위 가설) · **D38 게이트에 방향 조건**(파괴가 통과했다) · **D39 앵커 잔존율**(D22② 정정) · D36-a 실마스크 10배. W11 = 조건만 바꿔 격리 | W11 |
| 2026-08-04 | Chat | — | rev16: 맥북 지적 4건 판단. **D37-a 전 폭 확장**(세 대역 모두 small sample — 폭을 넓혀도 안 된다) · **D37-b 우회 플래그 존치+게이트 금지** · **neck 0.1538 용도 확정**(게이트 비사용) · **D25-b 동점 근거를 과제에서 재도출**(분기는 목에서 일어나야 한다 — D25-a 와 긴장 관계, 결과 보고 재조정) | A5000 보고 대기 |
| 2026-08-04 | Chat | — | rev15: W9. **★★ D36 halo 분모가 또 틀렸다** — 목 z대역 0.1538 은 다른 공간, 대역 전용은 0.0222/0.0889/0.1458 (약 7배 과소평가). **D37 halo 지표 정밀도 한계 → 비율 폐기, 개수+육안으로.** **D25-b 절단 z 정정(Chat 요약 오류)** · D27-a 인계 4연속 실패 · D31-b features 재사용 확정. W10 순차 실행 | W10 |
| 2026-08-04 | Chat | — | rev14: W8. **★ dragon-c 바닥값 확정(D33) — 소년상의 3.2배, 안 쟀으면 W9 오판.** **D34 .cbin 에 slat coords 없음 → 마스크 산출을 A5000 으로.** D35 X 대칭 모호성 · D25-a neck+1 · D31-a 예산 350초 · D26-a recolor 거부 승인 · D28-a 구조적 강제 승인. 🔴 rev13 미도달로 3090 이 SSH 22 를 보고 우회로를 만듦 | W9 3발 |
| 2026-08-04 | Chat | — | rev13: **D32 서버 간 전송 = SSH(비표준 포트).** 사용자 제공. 값은 리포 밖 0600 파일, 문서엔 변수명만(§7·원칙 10). ★ INFRA 의 "A5000 sshd 없음" 오기와 3090 의 ":8080 정체 미확인" 이 **같은 것**이었음이 밝혀짐 — 미확인 2건 동시 해결 | W8 진행 |
| 2026-08-04 | Chat | — | rev12: W7. **★ 자연어 경로 실호출 0→6회 성공** (add+factor=3.0, 좌표 유출 0). recolor 정식화(기하 100% 보존). **A5000 미실행 — Chat 의 인계 경로 실수(D27).** **D28 격자 정본 = slat coords**(수치·오프바이원의 단일 원인) · D29 머리 판정 절차 · D30 recolor 3곳 · D31 선행비용 · D24-a/b · D19-a. W8 3발 발사 | W8 보고 대기 |
| 2026-08-04 | Chat | — | rev11: W6 2발 PASS. **용 64³ 생존·목 극소 32 확인 → 레벨2 전제 통과.** head3mask 빈공간 91.4%. **D23 좌표금지 승격 · D24 색 별도경로 · D25 목잘록함 기준 · D26 op 매핑표.** 🔴 자연어 실호출은 키 부재로 여전히 0회. W7 3발 발사 | W7 보고 대기 |
| 2026-08-04 | 3090 | — | W6: dragon-c 124청크 · head3mask 4.44%(빈공간 91.4%) · 레벨1 색 hue 31.3°·기하 53/53 보존 | W7 |
| 2026-08-04 | 맥북 | — | W6: llm.py · 좌표거부 6종 · pytest 108/108 · rev10 8427bb0 · PR#2 머지 b3f5a09 | W7 |
| 2026-08-04 | ★사용자 | **G2** | **육안 인정 — 레벨1 달성. G2 닫힘.** 다음 우선순위 = 자연어 편집 경로 | W6 |
| 2026-08-04 | Chat | — | rev10: **D22 레벨2 = 용 머리 3개 확정.** assemble 로는 원리적 불가 → **VoxHammer 가 필수 경로로 승격.** 마스크가 빈 공간을 포함해야 함. halo 폭이 데모 합격조건이 됨. 새 리스크 = 용이 64³ 에서 생성되는가. W6 2발 발사 | W6 보고 대기 |
| 2026-08-04 | Chat | — | rev9: W5 2발 PASS. **G2 숫자 3항목 실자산 성립** — 절감 73.76%·초과배수 0.0000×·change_component 1.0000. **D16-a 배수 확정(승계 후 산출물 대상)** · **D19 육안 기준=깊이맵** · **D20 얼굴 해상도 구조적 한계** · D21 단위 규칙 | ★ 사용자 육안 판정 |
| 2026-08-04 | Chat | — | rev8: W4 3발 보고 전부 PASS. **바닥값 확정(D15)** · 0.853=(b)누출이나 **승계가 흡수** · **D16 보존 문턱 전환** · **D17 churn 정의** · **D9-b 두 번째 좌표 함정** · D18. D11·D12 근거 보정. **머리말 rev 확인 규칙 신설**(3연속 미도달 대응). W5 2발 = 재관통 + 육안 판정 | ★ 사용자 판정 |
| 2026-08-04 | Chat | — | rev7: W3 3발 보고. **실자산에서 (A)(B)(C) 최초 동시 성립** — 단 육안 미달·바닥값 미확보. **D11 기증자 재복셀화**(데모 성패 직결) · **D12 연결성분 정의 변경** · **D13 strict_containment 전제** · **D14 바닥값 획득 경로**. W4 3발 발사 | W4 보고 대기 |
| 2026-08-04 | Chat | — | rev6: W2 3발 보고 도착. **원칙 9·10 신설**(리포 동기화·이 파일도 보안 대상 — rev5 가 실제로 위반). **D8 산출물 .cbin 확정** · **D9 축 순열** · **D5-a 연결성분/churn** · **D5-b 보존지표 수정** · D10 래퍼. G2 갱신. W3 3발 발사 | W3 보고 대기 |
| 2026-08-04 | Chat | — | rev5: W1 3발 보고 도착. **D5 지표 재정의**(전역 실루엣 폐기 → 복셀 IoU·신규복셀), **D6 호스트 배치**(3090 은 TRELLIS 불필요, 생성·편집 모두 A5000, 편집은 GPU 전용), D7 절차 보강. W2 3발 발사 | W2 보고 대기 |

### 웨이브 대장
| 웨이브 | 세션 | 발사 | 보고 | 결과 |
|---|---|---|---|---|
| W1 | 맥북 (리포 부트스트랩) | 08-04 | ☑ | **PASS** — 51/51 · gitleaks 0 · 902cd4f · CI 추가(채택) |
| W1 | 3090 (인프라·:8083) | 08-04 | ☑ | **PASS** — 8083 회수·ngrok 502 대기·GPU 전부 가용. TRELLIS-image-large 반쪽 발견(D6) |
| W1 | A5000 (VoxHammer) | 08-04 | ☑ | **게이트 FAIL / 실질 PASS** — 주둥이 추가 확인. 게이트를 D5 로 재정의 |
| W2 | 맥북 (S2 순수 파이프라인) | 08-04 | ☑ | **PASS** — pytest 26/26 · 음성대조 통과 · G2 합성 3조건 성립 · 84557b6·469876b |
| W2 | 3090 (자산·bind) | 08-04 | ◐ | **부분** — bind ✅ 공개 502→200 · RGBA 2개 ✅ · **3D 0개**(A5000 다운). watcher 대기 중 |
| W2 | A5000 (D5 재계측) | 08-04 | ☑ | **PASS** — 신규복셀 274(연결성분 273) · 마스크 IoU 0.270 vs 밖 0.853 · churn 4.97배 · **축 순열 발견(D9)** |
| W3 | A5000 (:8082 복구·대조군) | 08-04 | ◐ | **부분** — :8082 복구 ✅(04:01:24, PID 1680668/PPID=1). 바닥값 **미측정 → (c) 못 가름**. encode 완료(slat.npz), decode 만 남음(~40s, ≥8GB) |
| W3 | 맥북 (rev6 커밋·D9·D5-b) | 08-04 | ☑ | **PASS** — rev6 17c6cd0 · D9 코드화(항등 회귀 0.0784 vs 1.0000) · 지표 개명·분리 · pytest 44/44 · 569a2ac |
| W3 | 3090 (DebugView·자산) | 08-04 | ☑ | **PASS** — DebugView 4분할×3뷰 공개 200(합성·실자산) · 자산 base 181/donor 294 청크 · **실자산 관통 (A)(B)(C) 동시 성립** |
| W4 | 맥북 (D11·D12·마스크) | 08-04 | ☑ | **PASS** — 크롭 없이 적합(fill 1.00, 유출 0) · 마스크 4.81%→1.31% · D12 1.0000 · pytest 62/62 · 31d8858. **D11 근거의 반증도 함께 보고** |
| W4 | A5000 (D14 바닥값) | 08-04 | ☑ | **PASS** — 바닥값 0.9299 확정 · **0.853 = (b) 누출** · churn 정의 확정 · **D9-b 두 번째 좌표 함정 발견** · 다운타임 129s |
| W4 | 3090 (편입·계약보강) | 08-04 | ☑ | **PASS** — PR #1 · gitleaks 0(트리+히스토리) · 계약 보강 943817b · 게이트 판정을 `gate_g2()` 단일 정본으로 이관 |
| W5 | 맥북 (rev8 커밋·PR 머지) | 08-04 | ☑ | **PASS** — rev8 **3f01be3** 푸시 · PR#1 머지 8aae63d(스키마 무변경 AST 대조) · D9-b/D16/D17 반영 · pytest 73/73 · 1a4fd26 |
| W5 | 3090 (★재관통) | 08-04 | ☑ | **PASS(숫자)** — 마스크 21.49%→8.65% · used_fill 1.000 · change_component 1.0000 · 초과배수 0.0000× · 절감 **73.76%** · 16.2초. 육안은 사용자 대기 |

### ★★ S2 / G2 실자산 최종 실측 (W5, 3090)
```
효능   신규 3,185 / 제거 4,069 (순증 −884)      change_component 1.0000 ≥ 0.80  ✅
       churn_ratio inf ≥ 3.0 ✅                  편집영역 밖 신규·제거 **0 / 0** ✅
보존   inherited_byte_identity 1.000000 ✅       excess_ratio **0.0000×** (바닥 0.0701) ✅
절감   3,089,520 → 810,840 B = **73.76%** ✅      parent 182 → child 162 청크 · 부기 108(chg 33 / rm 75)
시간   16.2초
육안   ✅ **사용자 인정 (2026-08-04)** — 레벨1 달성.  얼굴 디테일은 D20 구조적 한계로 수용

⇒ ★★ **G2 가 닫혔다. (A)(B)(C)가 실자산에서 동시에 성립한다.**
   이 프로젝트가 처음으로 "부분만 바뀌고, 바뀐 부분만 오간다" 를 증명했다
   ⇒ M1(관통) 완료 · S2 완료 · **S6 레벨1 통과**
```

### ⚠️ 그러나 지금 증명된 명제를 정확히 쓰면
```
"미리 준비된 다른 오브젝트의 일부를, 서버가 정한 마스크 자리에,
 나머지를 바이트 단위로 보존한 채, 26.2% 의 바이트만 보내서 끼워 넣을 수 있다."

원래 목표와의 차이 = **자연어**와 **편집 모델**이다.
전송·보존·in-place 라는 어려운 인프라는 뚫렸고 남은 것은 그 앞단이다.
⇒ 다음 우선순위: **자연어 편집 경로** (사용자 확정 2026-08-04)
```

### W5 미결 사항 처리 (Chat 판정)
```
D16 배수 미확정 (맥북)        ⇒ **D16-a.** 1.0 채택. 단 **적용 대상은 승계 후 산출물**임을 명시.
                              VoxHammer 원출력 2.10× 는 게이트 대상이 아니다(우리가 배송 안 한다).
                              대신 S5 에서 halo 인접 대역을 별도로 잰다 — 문턱은 재본 뒤 정한다
육안 기준 그림 (3090)         ⇒ **D19.** 앞면 깊이맵이 정본. 실루엣으론 원리적으로 못 닫는다
얼굴 해상도 (3090)            ⇒ **D20.** 구조적 한계(64³ 고정). 사용자 판정 사안
단위 혼용 위험 (3090)         ⇒ **D21.**
churn_ratio = inf             편집영역 밖 churn 이 0 이라 발산. JSON 에서 null.
                              **지표가 죽은 게 아니라 절감의 증거다** — 다만 소비자가 생기면 처리 필요
efficacy_net_voxels −884      "머리가 작아졌다" 는 사실. 게이트 항목 아님. 호박 표면 복셀이 더 적다
맥북이 자기 W3 진단을 뒤집음   before-점유 정규화 제안이 오답이었다고 스스로 보고했다(무계, 1.188>1).
                              원시 개수비를 오답으로 본 부분만 맞았다 — **여섯 번째 사례**
contract/ 를 3090 이 수정      1회 예외로 머지. 커밋 메시지에 "상시 예외로 굳히지 않는다" 명시.
                              다음엔 맥북이 대신 커밋하는 경로를 쓴다
PR #2 (3090)                  맥북 리뷰·머지 필요
```

### W4 미결 사항 처리 (Chat 판정)
```
🔴 rev 가 3연속 미도달           **내 프로세스 결함이다.**  원칙 9 를 적어두기만 하고 기계적
                                 확인을 안 만들었다 ⇒ 머리말 rev 번호 + 맥북 보고의 커밋 해시 필수화
D11 근거가 무력 (맥북)           ⇒ 본문 보정 완료.  결정은 유지, 근거를 내용 기반으로 교체
D12 근거의 과장 (맥북)           ⇒ 단서 추가 완료
0.853 = (b) 누출 (A5000)         ⇒ **D15.** 승계가 흡수한다.  제품 위협 아님.  halo 폭만 S5 에서 확인
보존 문턱 (A5000)                ⇒ **D16.** 절대 IoU 0.99 는 항진적으로 실패한다.  초과배수로 전환
churn 정의 (A5000 vs 맥북)       ⇒ **D17.** 합집합 정규화 채택.  맥북 제안은 무계라 기각
to_glb 회전 (A5000)              ⇒ **D9-b.** frames.py 에 상수로 추가
no_grad (A5000)                  ⇒ **D18.**
efficacy 0.7305 미달 (3090)      타이밍 — 3090 이 rev6 코드로 쟀다.  맥북 31d8858 의 D12 로 1.0000
inversion vs 의미적 누출 분리     **연기.** 데모에 불필요하다 (최소 구현 원칙).  S5 후보
contract/ 를 3090 이 만짐         PR 로 처리했고 맥북이 리뷰한다.  **상시 예외로 굳히지 않는다**
```

### W3 미결 사항 처리 (Chat 판정)
```
호박 얼굴이 잘림 (3090)          ⇒ **D11.** 메시에서 재복셀화. 이번 웨이브 최우선. 데모 성패 직결
연결성분 0.731 미달 (맥북)        ⇒ **D12.** (신규 ∪ 제거)로 변경. 경로별 문턱 분리는 기각
strict_containment (3090)         ⇒ **D13.** 전제로 승격. 게이트 문구에 명시
바닥값 못 잼 (A5000)              ⇒ **D14.** (i) :8082 잠깐 내림. 자산은 이미 다 확보됐다
churn_ratio 정규화 정의 (맥북)    ⇒ W4-A5000 이 자기 계산(4.97배)과 대조. 다르면 metrics.py 수정
frames.py 부재 (3090 지적)        타이밍이다 — 맥북 569a2ac 가 그 뒤다. W4 에서 3090 이 확인
rev6 미푸시 (3090 지적)           타이밍이다 — 맥북 17c6cd0 이 그 뒤다. W4 에서 3090 이 pull
베어 포트 정책 (맥북)             ⇒ 베어 포트는 허용(호스트 없이는 식별 불가).
                                  단 D4 의 `<DEBUGVIEW_PORT>=8083` 은 치환 키를 노출하므로 그 줄만 수정
3090 산출물 4개 미커밋            ⇒ W4-3090 이 PR 로 올리고 맥북이 리뷰 후 편입
계약 보강 (3090)                  BChunkResponse.chunks[] 는 chunk_id(≠ ChunkEntry.uri) ·
                                  미커밋 바이트는 staging 경로(버전 없음). 손으로 유추하면 404만 나온다
                                  ⇒ contract/ 문서에 반영
gitleaks 미설치 (3090)            ⇒ W4-3090 에서 설치·스캔
```

### W2 미결 사항 처리 (Chat 판정)
```
🔴 리포 PROGRESS 가 rev4 에 정체     내 잘못이다.  rev5 본문에 터널 호스트를 평문으로 남겼고
                                     맥북이 정당하게 커밋을 보류했다.  ⇒ 원칙 9·10 신설,
                                     rev6 은 레닥션된 상태로 나간다.  W3-맥북이 커밋한다
D6 이 리포에 없다 (3090 지적)        ⇒ rev6 에 D1~D10 전부 포함.  이후 원칙 9 로 재발 방지
glTF 산출 경로 (3090 지적)           ⇒ **D8 신설.** .cbin 으로 확정.  glTF 는 DebugView 파생물
축 순열 (A5000 발견)                 ⇒ **D9 신설.** 코드 상수로 못박는다 — 문서만으론 안 지켜진다
보존 지표 (맥북 경고 + A5000 0.853)  ⇒ **D5-b 신설.** 바이트 동일률은 승계 청크에만
G2 문구가 D5 와 불일치 (맥북 지적)   ✅ 갱신 완료
splice() 의 int(offset) 강제변환      맥북이 자기 래퍼 결함을 테스트로 잡았다.  계약의 소수-offset
                                     거부를 우회하고 있었다 — 제거 확정.  이게 검증 루프의 값이다
sample-snowman 경로 오류             ⇒ §5 S2 수정.  ai-ar-data/assets/… 이고 **3구**다.
                                     2단이 필요하면 W2 에서 새로 만든 base/ 를 쓴다
HANDOFF.md (build_untracked=1)       무해.  단 스키마 부재로 와이어 드롭 중 → RUNBOOK 에 명시
:8084 레거시 · :8080 정체            보류.  우리 경로와 무관하다
```

### W1 미결 사항 처리 (Chat 판정)
```
PROGRESS.md 파일 확인       ✅ 맞다. rev3/rev4 판본이 그 파일이다. 이후 rev5 로 갱신됨
CI workflow 추가            ✅ 채택.  D2 의 "계약 잠김" 을 CI 로 강제하는 것은 원칙 7 에 부합
pre-commit 미설치           → W2-맥북에서 pre-commit install 1줄로 마감
INFRA.md·DESIGN_INTENT.md   → 편입한다.  단 **레닥션 후**.  두 문서에는 공인 IP·ngrok URL·
                              홈 경로·키 이름이 그대로 있다.  public 리포다 (§7)
G1 판정 분리                ✅ 반영.  게이트가 두 세션에 걸치면 담당을 표기한다
3090 TRELLIS 가중치 다운로드 ❌ 하지 않는다 (D6).  3090 은 TRELLIS 가 필요 없다
ai-ar-prototype API 중단     그대로 둔다.  :8083 은 우리가 인계했고 복구 경로는 기록됨
```

---

## 7. 보안 — 커밋 전 매번

```
금지   API 키 · 공인 IP · 포트 조합 · ngrok URL · 공유 시크릿 · 실제 호스트명 · 홈 경로 · **SSH 포트**
방법   전부 환경변수.  GEN_HOST / EDIT_HOST / **SSH_PORT** / ASSETGEN_URL / DEBUGVIEW_PORT /
       T2I_OUT_DIR / ANTHROPIC_API_KEY / LLM_MODEL(기본 claude-opus-5)
값 보관   ~/.ai-ar-v3-hosts (chmod 600, gitignore) · ~/.env (키, 0600).  둘 다 리포 **밖**
       .env.example 에는 키 이름만.  문서에는 <EDIT_HOST>:<PORT> 로만 쓴다
검사   gitleaks pre-commit hook — 첫 커밋에 포함한다
경고   ai-ar-v2 의 StreamingAssets/blockedit_local_key.txt 에 실제 64-hex 키가 있다
       → 이 리포로 복사하지 않는다
```

## 8. 비목표

```
- TRELLIS.2-4B (D1 로 제외)
- 마스크의 voxel 단위 부피 채움 (SLat 껍질 표현 한계. 추후 협의)
- 겹치는 편집 범위의 충돌 해소
- 영속 저장·재시작 복원 (잡 인메모리 유지)
- 본격 인증 (키 회전·다중 사용자)
- 델타의 저장 절감 (승계 청크 중복 45%. 전송 절감이지 저장 절감이 아니다)
```
