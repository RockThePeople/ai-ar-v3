# PROGRESS — 국소 3D 편집 시스템 (ai-ar-v3) · **rev67**

> **이 파일이 계획의 단일 진실이다.** 매 세션의 첫 동작은 이것을 읽는 것, 마지막은 §6 에 결과를 적는 것.
> 결정 원문·웨이브 기록·종결 갈래는 `DECISIONS_ARCHIVE.md` 에 있다. 여기는 얇게 유지한다.
> 리포 `github.com/RockThePeople/ai-ar-v3` · 배치 `docs/PROGRESS.md`

> ✅ **언블록 완료. main 이 계약 v4 다** (PR #8 `54bdf5e` · PR #7 `f0c39d3`, 둘 다 머지·브랜치 삭제).
> 🔴 **rev45~49 가 다섯 판 연속 미도달했다.** 첨부 경로가 샌다 —
> 앞으로 rev 는 **프롬프트 본문 인라인**으로 온다.
> 🔴 **남은 최대 미지수는 서버 배선이다.** Unity 가 서버를 **한 번도 안 불렀다** —
> 생성 0회 · 편집 요청 0회 · 델타 수신 0회. 각각은 다 되는데 **이어진 적이 없다**.

---

## 0. 운영 원칙

```
 1. 계획 → 승인 → 구현 → 검증.  코드부터 쓰지 않는다
 2. 한 세션 = 한 게이트.  닫히면 /clear
 3. 실패한 접근을 계속 고치지 않는다.  재시도 상한을 넘으면 되돌린다
 4. "필수적으로 구현하되 최소한으로."  스코프 확장은 이 파일을 고친 뒤에만
 5. 결정은 대화가 아니라 파일에 남긴다
 6. 비밀은 코드·문서·커밋에 절대 넣지 않는다 (§5)
 7. 테스트 통과는 동작의 증거가 아니다.  육안 산출물이 없으면 미검증으로 적는다
 8. 세션 간 의존성을 고려해 스케줄링한다.  보고가 없으면 실행 중으로 간주한다
8-a. 웨이브가 끝나면 웨이브 대장과 세션 로그 둘 다 갱신한다 (ARCHIVE)
 9. Chat 이 이 파일을 고치면 다음 웨이브 발사 전에 리포에 반영한다.
    세션은 리포 판본만 근거로 삼는다 — 프롬프트 본문의 조항 인용은 근거가 아니다
9-a. **PROGRESS 전달은 사용자가 `docs/` 디렉터리째 수동 복사한다** (사용자 확정).
    ⇒ 세션은 리포 경로 **`docs/PROGRESS.md`** 를 읽는다.  git pull 이 실패해도 작업이 안 터진다.
    ⚠️ clone/pull 이 로컬 rev 를 덮을 수 있다 — `M docs/PROGRESS.md` 는 정상이다(로컬이 더 새것).
      **덮였으면 사용자에게 알려라.**  조용히 낡은 판본으로 일하지 마라
    ⚠️ 그러므로 **리포 main 의 rev 와 로컬 rev 가 다른 것은 사고가 아니다.**
      "main 은 rev51 이다" 를 미도달로 단정하지 말고 **로컬 `docs/PROGRESS.md` 를 먼저 열어라**
      (A5000 이 rev50 을 읽고 "rev53 미도달" 이라 단정한 실제 사례)
9-b. 엔드포인트·경로를 적을 때 **`<GEN_HOST>` / `<EDIT_HOST>` 한정사를 반드시 붙인다.**
    한정사를 떼면 다른 호스트를 잰 두 보고가 서로 모순처럼 보인다 (W26b 실제 발생)
9-c. "내 담당이 아니다" 로 보고를 끝내지 마라.  **그 파일이 누구 디스크에 있는지,
    `contract/` 인지**를 적어라.  그 두 줄이 없으면 소유권 공백이 생긴다 (W26b CHUNK_RE)
9-d. 🔴 **종결된 갈래의 산출물은 삭제한다.**  남겨 두면 닫힌 항목이 열린 항목처럼
    웨이브마다 "미확인" 으로 보고된다 (dragon-c — D56 으로 종결된 Dragon 갈래의 자산이
    다섯 웨이브 동안 미확인 줄을 만들었다.  사용자 지시로 폐기)
10. 이 파일도 §5 보안 규칙의 대상이다 (public 리포로 내려간다)
```

**방법론 6조** — 이 프로젝트가 실제로 물린 것

```
1. 대리 지표를 의심하라.  너무 깨끗한 숫자는 항진명제다
2. "예외가 안 났다" ≠ "안전하다".  그 경로가 안 돌았을 수 있다
3. 봉쇄와 효능을 둘 다 재라.  보존만 재면 아무것도 안 하는 구현이 전부 통과한다
4. 규칙만 적고 함수를 안 주면 그 규칙은 안 지켜진다
5. 자기 검사가 자기 환경에서 안 돌면 자기를 보호하지 않는다
6. 검사 도구의 거짓 양성이 검사 부재보다 나쁠 수 있다.
   실패를 잡는 테스트만 있으면 전부 실패시키는 도구가 만점을 받는다

★ 문턱을 한 점으로 정하지 마라.  그렇게 만든 문턱에 네 번 물렸고 전부 정상 동작을 탈락시켰다
★ 계산 전에 지표의 맹점을 등록하라.  일곱 번 전부 값을 지켰다
★ 수치(실측)와 원인 진단(추론)을 같은 신뢰도로 다루지 마라.  오진은 문서를 타고 되돌아온다 (D71)
```

**역할 경계**

| 세션 | 담당 | 금지 |
|---|---|---|
| Chat | 설계·게이트 정의·태스크 분해·이 파일 갱신 | 대규모 코드 생성 |
| 맥북 | 리포 주인 · 계약 · 서버 로직 · 테스트 · **Unity** | 전략 결정, 스코프 확장 |
| 3090 | 오케스트레이터 · t2i · DebugView(:8083) · 자산 | 계약 파일 수정 |
| A5000 | TRELLIS(:8082) · VoxHammer · GPU | 계약 파일 수정 |

---

## 1. 대목표

> **자연어로 만든 3D 오브젝트를 AR 공간에 띄우고, 그 일부를 화면에서 골라 자연어로 바꾸면,
> 바뀐 부분만 실제로 바뀌고 그 자리만 교체된다.**

```
(A) 효능      마스크 안이 지시대로 실제로 변한다
(B) 보존      마스크 밖은 바이트 단위로 불변이다
(C) in-place  오브젝트를 내리지 않고 그 자리만 교체된다
(D) AR        실제 AR 공간에 배치되고, 편집 중에도 앵커가 유지된다
```

🔴 **(D) 를 잊지 마라 (사용자 지시).** 지금 앱은 **화면 고정 배치**이고 AR 은 **0회**다.
plane 감지 → 탭 배치 → 앵커 유지가 최종 형태이며, **"오브젝트를 내리지 않는다"(C)의 의미도
AR 앵커 위에서라야 완전해진다** — 앵커가 리셋되면 사용자에겐 오브젝트가 사라졌다 나타난 것이다.
⚠️ `ai-ar-prototype/AR/ARPlacement.cs` 의 교훈: **자동 폴백을 기본 경로에서 빼라.**
평면이 없을 때 카메라 앞 고정 거리에 놓으면 그건 정의상 **화면에 붙은 물체**다.

🔴 **(C) 는 "절감" 이 아니다 (D70).** 전송량 절감은 부산물이고 목표가 아니다.
실측: `changed 76/152`(절반 변경)에서도 `IN_PLACE=True` · EntityId 152/152 · apply 10.35 ms.
⇒ **절감 0% 여도 in-place 는 성립한다.** 진짜 위험은 **`removed` 청크**(GameObject 파괴)다.

---

## 2. 현재 위치

```
✅ 생성        자연어 → Z-Image → BiRefNet → TRELLIS-image-large → .cbin 청크
✅ 라쏘        실기 S22 에서 slat 격자 위로 동작.  moto-b 뒷바퀴 1,386셀 · 오염 0셀
✅ 서버 편집   recolor / assemble / remove(미실행) / VoxHammer
✅ 델타        승계 + overflow 부기.  절감 43.2~73.8% (참고값)
✅ 보존        기하 바이트 100% (전 청크 디코드 직접 대조)
✅ **앱 화면**  실기에서 **곡면·색·복셀 단위 하이라이트** 성립 (W25)
🔴 서버 배선   0회        ← Unity 가 서버를 한 번도 안 불렀다
✅ **AR (D)**   실기 성립 — cam_travel **2.94m** · 앵커 델타 **8.92mm / 0.000°** (W26c)
🔴 in-place    실기 0회 (Editor 에서는 통과: EntityId 89/89 · recreated 0 · apply 7.68ms)
🔴 멀티유저    0회
```

### ★★★ W26c — (A)(B)(C)(D) 가 **한 화면에 같이 선 첫 장면** (사용자 판정 "매우 잘 됨")

```
책상 위에 오토바이가 놓이고, 그 상태에서 라쏘 1,386셀/71청크 + 표면 하이라이트 +
자연어 "recolor to blue" 입력까지 동작한다.  handoff/w26-ar/4-anchored-real-scene.png

★★ 누끼가 숫자로 확인됐다 — 원인 1(TrackedPoseDriver 부재) 확정
      시점          dev   posControls   world                       travel
      세션 시작 전   22        0        (0.000, 0.000, 0.000)       0.000m
      세션 시작 후   23        1        (0.009, 1.271, -0.139)      0.508m
   장치가 1개 늘고 posControls 가 0→1 이 되는 **그 순간부터** 카메라가 움직인다
★★ world vs camRel — **다르다** (45초 내내 동일하던 게 아니다.  세션 공간이 살아 있다)
      배치 전  world=(0.009,1.271,-0.139)  camRel=(0,0,0)            ← 앵커 없음
      배치 후  world=(0.051,1.126, 0.001)  camRel=(-0.001,0.191,0.309)  travel=2.936m
합격 판정  cam_travel 2.94m (합격선 2m 초과) · 앵커 델타 8.92mm / 0.000°
          TAP (509,726) planes=1 hits=1 ok=True → AttachAnchor ok · pose=(0.059,0.934,0.265)

원인 2~5 처리
  2 감지모드  Horizontal|Vertical — 생성 시 + 세션 활성 후 **두 곳에서** 명시
  3 회전      히트 pose 회전 버리고 yaw-toward-camera (v2·prototype 선례)
  4 앵커      AddComponent<ARAnchor>() → **ARAnchorManager.AttachAnchor(plane, pose)** · 실기 ok
  5 프레임    Application.targetFrameRate = 60
  부수        평면 프리팹은 MeshRenderer 를 **안 붙이고** MeshFilter 는 남김 (prototype 검증 경로)
              그래픽 API 자동 끄고 **OpenGLES3 고정** · activeInputHandler 는 이미 2 였으나
              빌드가 매번 확인·강제하게 함

🔴 세션 중 잡은 결함 셋 — 전부 "예외 없이 조용히" 계열
 ① 탭 배치가 **아예 안 먹었다.**  조준점 UI 를 걷어낼 때 Update() 의 배치 모드 분기가
    통째로 함께 날아가 TryPlace 가 **호출조차 안 됐다**
    ★★ **로그가 갈랐다 — `TAP` 로그 0건 = 입력 미도달이지 레이캐스트 실패가 아니다.**
       그 구분이 없었으면 멀쩡한 레이캐스트를 고치기 시작했을 것이다 (rev54 경고 그대로)
    복원하면서 레이캐스트를 PlaneWithinPolygon → WithinBounds → Estimated 로 넓혔다
 ② 재배치를 켜면 **눈앞에 아무렇게나 배치**됐다.  토글을 누른 그 탭이 TryPlace 로도 흘러들어
    토글 자리 TAP (765,103) 에 raycast 가 걸렸다
    ⇒ 토글 사각형을 터치 좌표계로 환산해 제외 **+ 직후 0.45초 무시.  둘 다 필요했다**
 ③ 테두리가 오브젝트를 **가렸다.**  ZTest Always 라 선이 자산을 뚫고 앞에 그려졌다
    ⇒ ZTest LEqual + Offset -1,-1 (바닥 z-fighting 은 피하면서 오브젝트가 앞에)
```

### ★★ W25 — 앱 화면이 완성됐다 (실기 S22 · moto-b 376청크)

```
✅ 곡면        큐브가 완전히 사라졌다.  라이더·헬멧·타이어 트레드·서스펜션·머플러가 매끈한 표면으로
✅ 색          피부톤 · 빨간 테일라이트 · 노란 부품 · 검정 차체
✅ 하이라이트   평소엔 복셀이 전혀 안 보이고, 뒷바퀴를 라쏘하니 **1,259셀 / 55청크**가
               바퀴 표면에서만 주황으로 빛난다.  경계가 청크가 아니라 **복셀 단위**로 선다
⇒ 사용자 명세 "평소엔 복셀이 안 보이고, 선택하면 그 볼륨이 반투명/하이라이트로 보인다" 성립

★ 원인 ②(빌드 스크립트)를 안 고쳤으면 자산을 올려도 안 바뀌었다
  `V3_CHUNK_DIR` → `ASSET_DIR` 개명(v3 시절 이름이 v4 자산과 어긋나 있었다) ·
  디렉터리 없으면 **경고가 아니라 종료** · 매 빌드마다 chunks/ 비우고 복사 ·
  ★ **APK 안 청크를 SHA256SUMS 와 대조** — "경로만 맞고 내용이 옛것이면
    '빌드 성공' 인 채 아무도 못 잡는다" (§7-A 계열)

미결 (사용자 판단 대기)
  · 하이라이트 경계가 **계단처럼 각진다** — 마스크가 64³ Point 샘플링이라 곡면 위에 격자가 보인다.
    정확한 표시이지만 부드럽게 하려면 Bilinear 또는 SDF 부풀리기.  어느 쪽이 나은지 미정
  · 불투명 혼합(85%) 유지 — 알파 블렌딩은 청크 376개 정렬 문제로 안 썼다
  · ~~dragon-c 미확인~~ → 🔴 **dragon-c 는 폐기한다 (사용자 지시).**  Dragon 갈래는 D56 으로
    이미 종결됐는데 자산만 남아 웨이브마다 "미확인" 줄을 만들고 있었다.
    `assets/dragon-c` (9.9MB) 삭제 · 이 문서·보고·빌드 스크립트에서 전부 제거.
    ⇒ 종결된 갈래의 산출물을 남겨 두면 **닫힌 항목이 열린 항목처럼 계속 보고된다**
```

**작업 현황** (레벨 개념은 D56 으로 폐기 · 사전 고정도 D62 로 해제)

```
작업 1 (교체)  눈사람 머리 → 호박    ✅ 완료 — 육안 인정 · 절감 73.76% · 마스크 밖 바이트 100%
작업 2 (색)    moto-b 뒷바퀴 → 주황   ✅ 완료 — 기하 100% · in-place 24/0/0 · 육안 인정
작업 3 (제거)  미정 — 후보: 옥상 햄버거 → 콜라 (D63) · remove op 첫 실행
⇒ 편집은 그때그때 정한다.  라쏘가 있으니 각 편집은 몇 분이다
```

---

## 3. 핵심 결정

### D74. 앱을 만든다 — 검사 하네스가 아니라

```
증상   실기 화면이 복셀 큐브 · 디버그 텍스트 · 확정 버튼 없음.  앱이 아니라 검사 하네스였다
원인   Chat 의 프롬프트가 W17~W22 내내 검증만 요구했다.  시킨 대로 됐고 시킨 게 틀렸다

★ 표현을 둘로 나눈다
   판정용   slat coords (N,3) — 라쏘가 투영해 고르는 대상.  화면에 안 그린다
   표시용   .cbin 청크 메시   — 사람이 보는 것.  청크 1개 = GameObject 1개
   ⇒ .cbin 은 이미 POSITION/NORMAL/COLOR/UV/INDEX 를 담는다.  큐브를 세울 이유가 없다
   ⇒ 두 표현을 다른 프레임에 두면 화면과 판정이 갈린다

★ 기존 프로젝트는 "원리" 만 가져온다.  하네스는 버린다 (사용자 지시)
   가져올 것  ChunkBin 디코드→Mesh · 청크당 GameObject · changed 만 교체 ·
              removed 는 파괴+사전 제거 · ARPlacement 의 자동 폴백 제거 판단
   버릴 것    GoldenIndex · PatchInPlaceHarness 계측 루프 · ARChainRunner · REPLAY 케이스
   ⇒ 계측을 버리지 말고 개발자 토글 뒤로 옮긴다
```

### 앱 기능 명세 (사용자 원문 · **정본**)

```
· 자연어로 희망 오브젝트를 생성하는 기능
· 오브젝트가 생성되면 편집 기능 사용 가능 (연필 모양 이모티콘으로 on/off 토글)
   · 편집 범위는 화면을 Lasso 로 드래그 → 볼륨을 자동으로 인식
   · 볼륨 마스킹이 된 상태에서 지우개 기능 사용 가능
     (드래그하면 범위가 아니라 지나간 궤적에 해당하는 마스킹이 제거됨), 지우개도 on/off
   · 편집 범위 확정 버튼을 누르면 어떻게 수정을 희망하는지 자연어로 입력받음

🔴 지우개는 궤적 기반(브러시)이다.  라쏘처럼 영역을 그리는 게 아니라
   손가락이 지나간 자리만 지운다.  라쏘 = 영역 추가 · 지우개 = 궤적 제거

구현 세부 (Chat 판단 — 명세에 없는 것.  다르면 정정 요청)
  라쏘 누적    다시 그리면 더한다 (지우개가 빼는 도구이므로 대칭)
  범위 초기화  명세에 없어서 뺐다 — 맥북이 "크게 잘못 잡으면 지우개로 다 지워야 한다" 고 지적.  추가 검토
  표시        선택 셀 수.  지문·계수·REPLAY 는 개발자 토글 뒤로
```

### 표시 방식 (사용자 확정)

```
평소   오브젝트를 완전한 메시 표면으로.  복셀은 안 보인다
선택   그 볼륨이 반투명 / 하이라이트로 보인다
방식   ai-ar-v2 MeshObject.cs 선례 — 월드공간 영역 셰이더 + Texture3D 마스크.
       원본 메시 표면에 직접, 블록 없이.  정점 색보다 해상도가 정확하다
⚠️ 선택된 복셀을 큐브로 겹쳐 그리지 마라 — 큐브가 다시 나타나면 지금 불만의 재발이다
⚠️ 셰이더는 IL2CPP 에서 스트립된다 → Always Included
```

### 🔴 AR 표시·조작 (사용자 확정 · W26 추가)

```
① 감지된 평면을 **테두리(윤곽선)로 하이라이트**한다
   ⚠️ "평면 2개 감지" 라는 글자만으로는 안 된다 — 사용자가 어디를 탭해야 할지 모른다
   ⚠️ **면을 색으로 채우지 마라.** ai-ar-v2 의 접지 평면 색 채우기는 참조 대상이 아니다
      (v2 의 나머지는 좋은 선례다 — 그 부분만 빼라)

② 🔴 **손가락으로 오브젝트를 돌리는 기능을 제거한다**
   오브젝트는 앵커에 고정되고, **사람이 움직여서 다른 각도를 본다.**  그것이 AR 이다
   손가락 회전이 있으면 앵커링이 됐는지 안 됐는지 화면에서 구분이 안 된다 —
   회전을 없애는 것 자체가 (D)의 검증 장치다
   ⇒ 회전이 없어도 반대편이 보이면 앵커링이 진짜다
```

### D75. 청크 격자 8³ → 16³ (완료 · main v4)

```
변경   CHUNK_SIZE 8 → 4 ⇒ 청크 슬롯 512 → 4,096 · 청크당 복셀 512 → 64 · CONTRACT_VERSION 3 → 4
결과   conformance 53/53 재통과 · 골든 200 → 816개 · DEFAULT_HALO_VOXELS = 2 신설

★ 목적이 수치로 확인됐다 (moto-b recolor, 실제 변경 15.1% 대비)
    8³ (v3)   89청크 · 변경 24 (27.0%) · 과대계상 1.79배
    4³ (v4)  375청크 · 변경 72 (19.2%) · 과대계상 1.27배
  ⇒ 개선됐다.  다만 1.27배는 여전히 과대계상이다

🔴 격자를 바꾸니 드러난 낡은 전제 넷 — 이것이 실제 수확이다
 ① halo=1 로는 부족하다.  해시가 바뀐 114청크 중 6개를 놓친다.  halo=2 면 0
    ⚠️ 픽스처 한 건에서 잰 값이지 유도한 법칙이 아니다.  실자산 재측정 필요
 ② 🔴 이 편집이 이제 청크를 비운다(18개).  removed 경로를 실제로 밟는다
    ⇒ D70 과 충돌 가능: 청크가 작아지면 통째로 비워지기 쉽고 removed = GameObject 파괴다.
      델타는 정밀해지는데 in-place 품질이 나빠질 수 있다.  moto-b 실측이 유지/철회를 가른다
 ③ 하드코딩이 낡아 헐거워진 단언 둘 — cx >= 5 가 실제 하한 10 인데도 통과 (아무것도 안 지킴)
 ④ ChunkBin.cs:41 에 ContractVersion = 3 — 지시에 없던 셋째 하드코딩 자리

★★ mirror_check.py 에 __main__ 블록이 없었다.  CLAUDE.md 가 안내하는 명령이
   아무것도 출력 않고 rc=0 으로 끝나고 있었다 — 방법론 5조의 정확한 재현
★ C# 값 대조 추가 — 상수 6개 + ContractVersion 을 값까지, 파생 관계까지.  지금까지 이름만 봤다
★ Unity LassoEditApp.cs:234 의 c.x / 8 → 상수로.  안 고쳤으면 틴트가 조용히 엉뚱한 청크를 칠했다

안전장치   CONTRACT_CONSTANTS 가 매니페스트로 나가고 받는 쪽이 assert_contract_compatible 로 거부.
           옛 자산을 새 코드에 물리면 조용히 안 깨지고 즉시 거부된다
⚠️ HANDOFF.md:139 "CONTRACT_VERSION=3 은 v3 이후 불변" 과 충돌.  그 문서는 리포 밖이라 미정정.
   근거는 coords.py 독스트링에 남겼다
```

---

### 🔴 복셀로 보이던 원인 — **둘이었다** (W24② 확정)

```
① 자산이 합성본   3090 이 숫자로 증명했다.  같은 자산·같은 파이프라인으로 대조:
     합성본(occupancy_to_mesh)  375청크 · 면 109,800 · 면 법선 축정렬 **100.0%**
     실물                        376청크 · 면 359,220 · 축정렬 **5.1%**   (dragon-c 2.7%)
   ★ 109,800 = 9,150복셀 × 12삼각형.  **큐브라는 사실이 숫자에 그대로 있다**

② 🔴 `tools/build_lasso_apk.sh` 의 기본값이 **리포 밖 `$REPO/../.inplace`** 를 본다
   ⇒ **자산을 올려도 빌드가 합성본을 집어간다.**  ①만 고치면 화면이 안 바뀐다
   3090 이 "앱 담당 판단" 이라 안 건드렸다 — 맥북이 고친다

실물 자산 (PR #10)
  moto-b   parent **376청크** · patch(recolor) 71 — **changed 71 / added 0 / removed 0**
  dragon-c parent **485청크**
  ★ **removed = 0.  D75 의 대가가 recolor 경로엔 없다** (8³ 24/0/0 → v4 71/0/0)
  검증: 면 동일 · bbox 오차 0 · 점유 IoU **1.000000** — **기하를 새로 만들지 않았다.**
        v3 정점·면·색을 그대로 읽어 **자르는 자리만** 바꿨다
  ★ v4 디코더가 v3 바이트를 거부하므로, D75 직전 커밋 워크트리로 옛 리더를 만들어 읽었다 —
    **두 계약이 한 프로세스에서 만나지 않는다**
  ⚠️ 바이트 총계가 줄어든 이유 미확인 (9,763,456 → 7,646,272).  추론은 양자화 범위 축소
```

### slat 인계 — 가능 (A5000 답변)

```
편집된 SLAT 은 이미 메모리에 있다.  버리고 있을 뿐이다 — `edit_pipeline.py:557 slat_tgt`
방법   래퍼에서 `pipe.decode_slat` 를 감싸 가로챈다.  **VoxHammer 소스 수정 0줄** (D51 1줄 그대로)
격자   `coords_tgt` 는 sparse-structure 64³ 에서 직접 뽑은 정수 좌표다 — **표면 복셀화가 아니다.
       D28 정본 격자 위에 선다.**  형식도 자산과 동일 (coords (N,4) I32 · feats (N,8) F32)
비용   약 460 KB · VRAM·시간 영향 없음
⚠️ **한계**: slat 을 넘기면 정본 격자 위에서 델타를 계산할 수 있게 되지만,
   **최종 GLB 는 그 slat 을 전역 디코드해서 나온다**(D55).  전역 디코드가 만드는 마스크 밖 변화는
   사라지지 않는다.  **43.2% 가 정본 격자에 서는 것이지 값이 좋아진다는 뜻이 아니다**
```

### W26 — AR 이 살아났다 · 서버 계약이 확정됐다

```
[맥북] 🔴 카메라가 검던 원인 — 예외 없는 실패였다
  `Assets/XR` 을 복사해도 **XR 로더 할당은 별개**다
    "No active XRInputSubsystem … valid loader configuration"
    → 화면 검음 + ARCore 매니페스트 병합 미실행 → **APK 에 CAMERA 권한 자체가 없음**
  V3AppBuild.ConfigureXr() 로 ARCore 로더 할당 · InitManagerOnStart
  ⇒ 로더 True · CAMERA granted · **평면 2개 감지 · 카메라 영상 정상**
  ★ 부수 발견: `Assets/XR` 을 **디렉터리 유무로 판단하면 안 된다** —
    Unity 가 껍데기만 만들어 두면 cp 가 건너뛰어 로더가 빠진 채 빌드된다.  파일 단위로 확인·중단
  월드 스케일 = AssetScale.FootprintMeters 0.24m (v2 실측).  RootScale·VoxelMeters 가 전부 파생
  입력 분리 = 모드 배타.  Placing 이 입력 독점 → 배치 즉시 닫음 → 재배치는 버튼으로만

[3090] ★★ removed 실측 — **D75 를 면책이 아니라 정당화한 숫자**
                   8³(v3)        4³(v4)
  부모 청크         89            376
  changed           89 (100%)     242 (64%)
  removed            0             19 (5.1%)
  🔴 **8³ 에서는 자산 전체가 다시 간다(89/89) — 델타가 델타가 아니었다.**
     4³ 는 36% 를 승계한다.  대가는 GameObject 파괴 19건
  (recolor 의 71/0/0 은 D75 를 면책 못 한다 — recolor_asset 이 removed=[] 를 명시해 구조적으로 못 만든다)

[3090] ★ halo — 3090 이 자기 실수를 물리로 잡았다.  이 프로젝트에서 가장 좋은 자기검증이다
  처음 **해시 비교**로 재니 halo 0/1/2/3 = 190/188/187/187 → "halo 가 안 듣는다"
  ⇒ 삼각형이 최대 0.45복셀인데 **44복셀 떨어진 청크가 바뀌는 게 물리적으로 말이 안 된다**
     확인하니 먼 청크 116/116 이 정점·면·바이트 길이까지 같고 **순서만 달랐다**
     (자기 정점 압축이 partition_mesh 입력 순서를 바꿨다.  D61-a 를 인용해 놓고 스스로 어겼다)
  기하 기준 재측정        halo 0→ 부기72 놓침2 과잉1
                          halo 1→ 부기116 놓침0 과잉43
                          halo 2→ 부기148 놓침0 과잉75
                          halo 3→ 부기190 놓침0 과잉117
  바뀐 청크는 해시 261 이 아니라 **기하 73** (거짓 양성 188)
  ⇒ DEFAULT_HALO_VOXELS=2 는 틀리지 않았다(놓치는 쪽만 사고).  다만 이 자산은 halo1 로도 0누락
  ⚠️ 자산 1·마스크 1·편집 1건.  마스크가 청크 경계와 어떻게 맞느냐에 좌우된다

[3090] ★ 떠 있는 엔드포인트 — 셋뿐.  추측이 아니라 실제로 쏴 본 결과
  200  GET /healthz · /runs · /v2/assets/{id}/slat_coords.v{n}.json
  404  GET /v2/assets · /v2/jobs/{id} · /v2/assets/{id}/chunks/{key}.v{n}.cbin
  404  POST /v2/assets · /v2/assets/{id}/edits
  ⇒ 라쏘용 좌표 엔드포인트 하나만 서 있다.  생성·편집·폴링·청크 전송은 **없다**

[3090] ★ 스키마 — contract/ 에 전부 있다.  **새로 지을 것이 없고 계약 변경 요청도 없다**
  생성  GenerateRequest → JobStatus.manifest = ChunkManifest
  편집  EditRequest(base_version·mask·idempotency_key) → JobStatus.patch = PatchPackage
  폴링  JobStatus.state ∈ **queued · running · succeeded · failed** (넷).  생성·편집 같은 규약
        🔴 rev53~58 이 여기에 `cancelled` 를 넣어 **다섯**으로 적었다 — 오기다.
           3090 이 자기 W26 배선 문서에서 같은 오류를 내고 **계약을 열어 스스로 정정**했다:
           "계약을 안 열고 기억으로 썼다".  Chat 이 그것을 그대로 옮겼다 (§0-9 위반)
  청크  uris.chunk_uri · slat_coords_uri · staging_chunk_uri
  마스크 = EditMask mode="voxels" **복셀 좌표 목록** (청크 목록도 지문도 아니다).
        grid_source="slat_coords" 생략 불가, 서버가 안 채운다 (D28-a)
  manifest 가드 충족 — ContractInfo 에 chunk_size 4 · chunk_grid_res 16 · contract_version 4
  생성 1회 실측 **38초** (Z-Image+BiRefNet 27s + TRELLIS→청크 11s, 콜드 포함)
  ⇒ 폴링 상한 120초 · 간격 2초

🔴🔴 **배선의 선행 조건 — A5000 :8082 가 아직 v3 를 낸다**
  이번 생성에서 확인: contract_version 3 · chunk_size 8 · 헤더 바이트 3
  → v4 클라이언트가 `ChunkBinError: file=3, local=4` 로 거부한다
  **생성 경로를 그대로 Unity 에 연결하면 화면이 통째로 빈다.**
  지금 앱이 뜨는 건 3090 이 v4 로 재분할해 넣은 assets/ 덕이고, 새로 생성하면 다시 v3 다
  ⇒ A5000 의 deltacontract 를 v4 로 올리는 것이 W27 의 선행 조건
```

### 🔴🔴 W26c — AR 이 처참한 이유.  선행 프로젝트 3개를 뜯어 대조했다

> 사용자 지적: **"AR 성능(Spatial Aware)이 너무 처참하다."**
> `ai-ar-v2` · `ai-ar-prototype` · `ai-ar`(빌드 산출물 역분석) 전수 대조 결과.

```
★★★ 원인 1 — `TrackedPoseDriver` 가 **리포 전체에 0건**이다 (grep 확인)
   AF6 에서 XROrigin 은 카메라를 스스로 움직이지 않는다.  카메라 자세는
   TrackedPoseDriver 가 InputSystem 에서 받아 넣는다.  그게 없으면
   **카메라가 Camera Offset 원점에 영원히 고정된다.**
   ⇒ 영상은 나온다(ARCameraBackground 는 무관) · 평면도 감지된다 ·
     그런데 **오브젝트가 화면에 붙어 있다.**  예외도 경고도 없다

   ⚠️ 이것이 ai-ar-prototype 이 한 사이클을 태워 문서로 남긴 **"누끼"** 실패다
      (contract/UNITY_CLIENT.md §12 · §12-1).  원문:
      "이 절의 첫 판은 'M1 열림' 이라고 적었다. 성급했다.  자산이 보인 것은 맞지만
       **화면에 붙어 있었다**.  로그는 전부 초록이었고 사용자가 눈으로 보고서야 드러났다.
       ★ **AR 판정은 로그로 하지 마라.**"

   ★ prototype 이 밝힌 원인은 **세 겹**이고, 우리는 그중 1·3 을 미확인 상태로 두고 있다
     ① AddComponent<TrackedPoseDriver>() 는 **입력 액션이 빈 채로** 붙는다
        → 바인딩을 코드로 직접 써야 한다:
          <XRHMD>/centerEyePosition + <HandheldARInputDevice>/devicePosition
          <XRHMD>/centerEyeRotation + <HandheldARInputDevice>/deviceRotation
     ② `activeInputHandler = 2 (Both)` 여야 한다.  **패키지 설치로는 안 바뀐다.**
        public API 가 없어 prototype 은 ProjectSettings.asset 직렬화 속성을 직접 고쳤다
        (ProjectConfigurator.cs:347-359).  ⇒ v3 에 이 설정이 **없다** (grep 0건)
     ③ ARInputManager.OnEnable 이 **그 시점의** activeLoader 에서 XRInputSubsystem 을 찾는다
        늦게 켜면 controls=0 · 값이 영원히 (0,0,0)
   ★★ prototype 의 한 줄 진단법 — **AR 씬을 만들면 이것부터 찍어라**
        InputSystem.devices  20개 — XR 장치 0개   ← 세 겹 전체가 이 한 줄에 드러난다
                     수정 후 21개 — HandheldARInputDevice(ARCore) 추가
                     TrackedPoseDriver pos: controls=0 → 1

★ 원인 2 — `requestedDetectionMode` 를 안 정한다 (grep 0건)
   prototype 은 씬 빌더와 런타임 **두 곳에서** Horizontal|Vertical 을 못박고 로그를 찍는다

★ 원인 3 — 평면 히트의 **회전을 그대로 쓴다** (ArPlacement.cs:155 `pose = _hits[0].pose`)
   v2 와 prototype 은 **둘 다 법선을 버리고 yaw-toward-camera 만** 쓴다.  원문:
   "기울어진 평면에 붙으면 눈사람이 기울어 서고, 그러면 놓였다는 감각이 깨진다"
   ⇒ 살짝 기운 평면에서 자산이 비스듬히 선다.  지금 증상과 일치한다

★ 원인 4 — 앵커 생성 방식이 AF6 이 명시적으로 경고하는 형태다
   v3: `anchorGo.AddComponent<ARAnchor>()` (ArPlacement.cs:179)
   AF6 프레임워크 경고 문자열(ai-ar 바이너리에서 실제로 발견):
     "has failed to add itself to the anchor subsystem …
      you should use **ARAnchorManager.TryAddAnchorAsync** instead of adding the
      AR Anchor component to GameObjects at runtime"
   ⇒ 앵커가 서브시스템에 등록 안 되면 **재로컬라이즈 보정을 못 받는다** = 드리프트

★ 원인 5 — targetFrameRate 미설정.  prototype 은 60 을 명시 (ARSceneDriver.cs:98)

────────── 선행 프로젝트가 이미 답을 낸 것들 (재발명 금지) ──────────
평면 표시   ★ **MeshRenderer 를 끄지 말고 아예 안 붙인다** (prototype ARSceneBuilder.cs:164-206)
            ARPlaneMeshVisualizer.SetVisible 이 매번 SetRendererEnabled<MeshRenderer>(true) 로
            **되켠다**(:137).  그 함수가 `if (component)` 로 null 을 건너뛰므로,
            컴포넌트를 안 붙이면 면이 영원히 안 그려지고 LineRenderer 만 남는다
            ⇒ 사용자의 "면 채우지 마라" 요구와 정확히 일치하는 검증된 방법이다
            MeshFilter 는 **남긴다** — 레이캐스트가 그 메시를 친다
평면 0개    버그가 아니다.  ARCore 는 **시차로 얻은 점군**을 요구한다.  폰이 고정돼 있으면
            점군이 안 쌓이고 평면도 안 나온다.  ⚠️ 코드 문제로 오해하고 고치기 시작하면
            멀쩡한 코드를 망친다.  대기 예산 240초 · HUD 4Hz(GC 때문)
평면 선택   `ARPlane.extents` 는 **half-dimension** 이다.  실제 크기는 `ARPlane.size`.
            extents 를 크기로 읽으면 길이 절반·면적 1/4 로 조용히 틀린다
AF6 API     planesChanged ❌ 폐기 → trackablesChanged (또는 trackables.count 폴링)
            ARAnchorManager.AddAnchor(Pose) ❌ 없다 → TryAddAnchorAsync
            반환은 Task 가 아니라 Awaitable<Result<>> — GetAwaiter() 로 코루틴 폴링
드리프트     ★ **상태 플래그로는 안 잡힌다.**  prototype 실측: 카메라가 20초에 80.9m 이동했는데
            loss_events=0 · anchor_state=Tracking.  ARCore 는 "정상 추적" 이라고 보고한다
            ⇒ ARDivergenceMonitor: **물리적 타당성**으로 판정 (5 m/s · 30 m 상한)
            ⇒ 그리고 **구간 단위 주석**이지 세션 차단이 아니다 — 초기 재로컬라이즈 점프는
              거의 매 세션 일어난다
드리프트 계측 앵커를 **배치에 쓰지 말고 계측에 쓴다** — 고정 배치 pose 와 앵커 위치의
            벌어짐이 곧 세션 drift (prototype ARSceneDriver.cs:496-514)
전제 검사   "카메라가 안 움직이면 세션 공간 전체가 정지하므로 앵커도 안 움직인다.
            **'안정적이다' 가 아니라 잴 대상이 없었다.**"  ⇒ cam_travel ≥ 1.0m 를 전제로 검사
            ★ world 와 camRel 을 **나란히** 찍어라 — 둘이 45초 내내 동일하면 그게 누끼다
ARCore 설정  requirement/depth 를 Optional 로.  안 내리면 매니페스트에
            `com.google.ar.core.depth required="true"` 가 박힌다.
            ⚠️ ARCoreBuildProcessor 는 매니페스트 태그를 **추가만** 한다 —
              바꿨으면 `--clean` 빌드가 필요하다(낡은 태그가 살아남는다)
그래픽      v2 는 OpenGLES3 고정("ARCore 는 이 경로가 가장 안전") · prototype 은 Vulkan 전용.
            둘 다 자동 API 를 껐다.  v3 는 미확인
멀티유저     `ai-ar` 는 **이미지 마커(marker_A) + Photon PUN2**, 모든 pose 를 앵커 상대로 저장.
            클라우드 앵커 없음.  ⚠️ 다만 그 앵커도 **맨 GameObject** 라 보정을 못 받는다
            ⇒ W28 에서 재사용하되 ARAnchor 로 승격해야 한다
```

### 🔴 W26b — 넷째 하드코딩 자리.  두 세션이 반대 방향에서 같은 것을 찾았다

```
[3090 → A5000 방향]  v4 생성물 415청크 중 **372 실패**.  받아진 43개는 전부 인덱스 ≤7
                     GET …/chunks/10_10_0.cbin → 422 "chunk_key 형식이 아니다"
[A5000 자체 조사]    `server/api/store.py:24`
                     CHUNK_RE = re.compile(r"^[0-7]_[0-7]_[0-7]$")   ← 8³ 하드코딩
                     v4probe 509청크: 통과 114(22.4%) / 거부 395(77.6%)
                     이론 주소지정 가능 영역 8³/16³ = 12.5% 와 정합
                     호출 지점 둘 다 청크 바이트 전송 경로 (:208 staging · :345 committed)

⇒ **D75 가 센 하드코딩 3곳에 이은 넷째 자리**다 (cx>=5 단언 2 · ChunkBin.cs:41).
   격자 상수를 값으로 박은 자리는 예외를 안 내고 **조용히 78%를 거부**한다
⇒ 고치는 법: CHUNK_GRID_RES 에서 **유도**하거나 parse_chunk_key + 경계검사

🔴 **소유권 공백 — 둘 다 자기가 아니라고 했다.  Chat 이 정한다: A5000 이 고친다**
   3090: "A5000 의 chunk_key 검증이다" → handoff/to-a5000 에 요청서
   A5000: "역할표상 서버 로직은 맥북 담당이다" → 보고만
   ★ 판정 근거: `server/api/store.py` 는 **리포에 없다**(main 의 `server/` 에 `api/` 가 없다).
     A5000 디스크의 :8082 서비스 코드이고, **`contract/` 파일이 아니다.**
     역할표가 금지한 것은 `contract/` 수정이지 자기 서비스 코드가 아니다
   ⇒ 규칙 보강: **"내 담당이 아니다" 로 끝내지 마라.  파일이 누구 디스크에 있는지,
     그게 contract/ 인지를 적어라.** 그 두 줄이면 공백이 안 생긴다
```

### ⚠️ 엔드포인트 진단 — rev53 이 **호스트를 안 적어서** 생긴 혼선

```
rev53 기재  "떠 있는 엔드포인트 셋뿐 … 생성·편집·폴링·청크 전송은 없다"
A5000 반론  "전부 있습니다. 접두사가 다를 뿐입니다" — /v2/trellis/generate 등, 422(≠404)

★ 둘 다 맞을 가능성이 높다.  **다른 호스트를 쟀다**
   3090 이 잰 것 = <GEN_HOST> (자기 오케스트레이터).  slat_coords 만 200 이었고
                   나머지가 없어서 3090 이 W26b③ 에서 **직접 세웠다**
   A5000 이 든 것 = <EDIT_HOST>:8082 의 `/v2/trellis/*`
🔴 **Chat 의 기재 오류다.**  3090 보고를 옮기며 **호스트 한정사를 뗐다.**
   A5000 은 그걸 자기 얘기로 읽었고, 그 판단 자체는 합리적이다
⇒ 규칙: **엔드포인트를 적을 때 `<GEN_HOST>` / `<EDIT_HOST>` 를 반드시 붙인다.**
   D71 의 변주다 — 이번엔 오진이 아니라 **한정사 누락**이 오해를 만들었다
★ A5000 의 "422 ≠ 404 — 라우트는 등록돼 있고 검증에서 걸린 것" 은 정확한 구분이다
```

### ✅ W26b③ — 3090 라우트가 섰다 (PR #14)

```
POST <GEN_HOST>/v2/assets · /v2/assets/{id}/edits · GET /v2/jobs/{id}
GET  /v2/assets/{id}/chunks/{key}.v{n}.cbin · /manifest.v{n}.json (덤)
v4 왕복 성립  edits→queued→succeeded 2.0초 · patch changed 71/removed 0 ·
              chunk 27,144B 선언과 일치 · 정점 713 · 색 (37,99,235) = "뒷바퀴를 파랑으로" ·
              sha256 일치 · 부모 v1 과 **판본 공존** · manifest 376 (승계 305 + 변경 71)
              POST grid_source=null → 422 (서버가 안 채운다, D28-a 지켜짐)
★ 매니페스트의 contract 는 계약 상수를 그대로 싣는다 — 손으로 안 적었다
★ 생성 가드는 판본을 코드에 안 박았다 — 상류가 올라가면 저절로 통과한다

⚠️ 3090 이 스스로 신고한 약점 (좋은 신고다)
  · 생성 가드가 **헬스 선언만 본다.**  W26 때 /v2/trellis/health 는 v4 라 답했는데
    실제 생성물은 v3 였다 — **선언은 증거가 아니다**(D28·방법론 2조).  산출 바이트 검증으로 바꿔야
  · 잡 인메모리 — 재시작하면 폴링이 404.  "실패" 가 아니라 "없다"
  · 라우트 무인증 · 자동 테스트 없음(살아 있는 서버에 쏜 것뿐, 회귀 못 막는다)
  · 편집은 recolor 만.  색을 못 읽으면 COLOR_NOT_UNDERSTOOD 로 거부 —
    ★ "기본색을 칠하면 무엇을 요청하든 같은 색이 되는 서버가 된다" 는 판단이 옳다
```

### ✅ W26b② — A5000 v4 승급 완료

```
$ xxd -l 8 chunks/0_7_8.cbin
00000000: 4342 4e31 0400 0000     CBN1....      ← offset 4 = 4 ✅
manifest 4/4/16 ✅ · 최대 청크 인덱스 15 (8³ 에선 불가능) ✅ · conformance 53/53 · 골든 816
★ 사본 diff = 사고 아님.  git status 깨끗, 1,145줄 차이는 전부 v3↔v4 드리프트
★ 교체 지점을 **실측으로** 특정했다 — main.py 는 server/vendor 를 넣지만 거기 사본이 없어
  실제 해석은 `ai-ar-prototype/contract/python/deltacontract/` 였다 (`__file__` 확인).
  ⇒ 경로 선언이 아니라 **실제 로드된 모듈**을 본 것.  방법론 2조의 좋은 적용
v3 산업물   53자산 1.1GB 에 `.CONTRACT_V3_STALE` 마커(비파괴) · 프로브 3개만 격리
slat 래퍼   완료.  VoxHammer 0줄 수정 · coords(9591,4)int32 · feats(9591,8)float32 · 460,520B ·
            내부 decode 1회 유지 · 블록 종료 후 원복 확인.  한계 문구를 docstring 에 박았다
```

### ✅ W26d — 서버 쪽 선행 조건이 **양 호스트 실측으로** 닫혔다

```
[A5000] CHUNK_RE — **유도로 고쳤다.  16 을 다시 박지 않았다**
  parse_chunk_key + `i < 0 or i >= CHUNK_GRID_RES` 경계검사 → **정규형만 파일시스템에 닿게**
  ★ 정규형을 돌려주는 이유가 좋다: parse 는 '+8'·' 8' 도 8 로 읽는다.
    원문을 그대로 경로에 붙이면 **같은 청크가 여러 문자열로 접근된다**
    실측 '+8_1_1'→'8_1_1' · '16_0_0'·'-1_0_0' 범위거부 · '../../etc'·'0_7' 형식거부 · '15_15_15' 통과
  결과   <EDIT_HOST> 509/509 (수정 전 114) · **<GEN_HOST>→<EDIT_HOST> 도 509/509**
         바이트 무결성 10_4_0: 서버=디스크 19,816B · sha256 3자 일치 · version u32 = 4
         16_0_0 은 422 유지 ✅
  ★★ **스테이징 경로는 슬롯 디렉터리명(gen-…)이 아니라 job_id(j-…) 를 받는다.**
     처음에 디렉터리명으로 쳐서 509개 전부 404 였다 — **422→404 로 바뀐 것 자체가
     CHUNK_RE 수정이 들었다는 신호**였다.  W27 배선에 이 구분이 필요하다
  남은 8³ 잔재 2곳 (§0-9c 준수해 디스크·contract 여부까지 적어 왔다 — 이게 옳은 보고다)
    server/lab/view.py:31,33,34,50,54  range(8)×5   A5000 디스크 · contract 아님 · 진단도구
    server/repaint/halo_sweep.py:37    c*8 + 3.5    동일
    ⇒ :8082 서빙 경로가 아니라 배선을 막지 않는다.  v4 에선 틀린 값을 낸다 — 나중에 유도 처리
  clone 전환 완료 (tar 백업 491MB → 별도 clone → untracked 복사 → 검증 후 교체)
    ⚠️ clone 이 rev51 이라 **로컬 rev55 를 덮어썼다.** `M docs/PROGRESS.md` 가 정상이다

[3090] 생성 가드 — **사전 헬스 조회를 아예 없앴다**
  "지켜야 하는 것은 상류가 뭐라고 하는가가 아니라 **클라이언트에게 무엇을 건네는가**" —
  건네기 직전 .cbin 헤더 offset 4 의 uint32 를 계약 레이아웃 `<4sI` 로 읽는다
  ★★ **합성이 아니라 W26 때 <EDIT_HOST> 에서 실제로 받은 바이트로 재현했다**
     ① 상류 선언 contract_version = 4
     ② 실제 산출 판본별 {3: 96}
     ③ 전 가드(헬스만) 통과했을 것이다
     ④ 새 가드   거부 — "96/96 이 v4 가 아니다.  하나라도 어긋나면 전부 버린다"
  설계 판단 셋 (전부 옳다)
    · 부분 수신은 성공이 아니다 — 반쪽 자산은 **열리기까지 해서** "화면이 비었다" 보다 늦게 발견된다
    · 문턱을 손으로 안 적었다 — CONTRACT_VERSION 비교뿐.  테스트가 `== 4` 리터럴 부재를 잠근다
    · decode() 를 판정에 안 쓴다 — 맞을 때만 통과해서 **"몇 번인지" 를 못 말한다**
  test_contractguard.py 7건 신규 — "헬스 v4 · 산출 v3" 를 합성으로 잠갔다
```

### 🔴 지금의 병목은 맥북이다 — PR 3건 적체 + AR

```
PR #12 W26 배선 계약 문서            OPEN
PR #14 라우트 본체                   OPEN   ← 이게 안 가면
PR #16 생성 가드 (#14 위에 쌓음)      OPEN   ← 이것도 못 간다
⇒ 🔴 **머지가 안 되면 맥북이 3090 의 라우트를 못 본다.**  W27 은 맥북 Unity 가
  그 라우트를 부르는 웨이브다 — 코드를 못 보면 **추측해서 부른다**
⚠️ D27-c 가 경고한 자리인데 이번엔 브랜치 재사용이 아니라 **리뷰 대기**다
```

### 🔴 새 블로커 — t2i 가 라우트에 안 붙어 있다 (3090 자진 신고)

```
Z-Image·BiRefNet 이 **리포 밖 conda 환경**이라 `_source_png()` 가 NotImplementedError
⇒ W26 에서 잰 "생성 38초" 는 기존 경로의 실측이고, **새 /v2/assets 라우트엔 미연결**이다
★ 3090 이 없는 것을 있는 척하지 않았고, 가드는 이미 서 있어 이미지가 들어오는 순간 동작한다
⇒ W27 생성 end-to-end 의 마지막 미연결 지점.  편집 경로는 이것과 무관하게 돈다
```

### ★★ W26e — **생성 end-to-end 가 처음으로 성공했다** (3090)

```
POST <GEN_HOST>/v2/assets · "a yellow rubber duck" · seed 42
  t2i (Z-Image + BiRefNet)        28.1s
  상류 structure → slat → chunk   40.1s
  청크 수신 · 바이트 검증 · 저장   50.1s
자산 v1 · 청크 **989** · contract v4/4/16 · stage_detail 판본 {4: 989} (가드가 전 청크를 셌다)
GET …/chunks/0_10_3.v1.cbin → 200 · 324B · v4 · sha256 일치 · 두 자리 인덱스 683개 포함
⇒ A5000 의 CHUNK_RE 수정이 <GEN_HOST> 경유로 확인됐다 (683개가 인덱스 ≥8, 전부 수신)
⚠️ W26 의 38초는 **기존 스크립트 경로** 값이다.  차이는 청크 96→989 수신 시간(v4 격자).
   폴링 상한 120초는 유효 (실측의 2.4배).  ⚠️ 한 번 잰 값이다

★ t2i 배선 — subprocess.  **선택이 아니라 제약이다**
  Z-Image 와 BiRefNet 은 numpy/torch 판본이 충돌해 **한 프로세스에서 임포트하면 죽는다**(W2 실측).
  각자의 conda 파이썬으로 따로 띄우고 파일로만 주고받는다 (prototype 설계 그대로)
  ★ 리포 밖 `_tools/` 의 **이미 도는 스크립트를 부른다.  재구현하지 않았다** —
    "두 벌이 되면 그때부터 어느 쪽이 진짜인지 매번 확인해야 한다"
  ★ **자리표시 이미지를 만들지 않는다** — "빈 이미지를 넘기면 그 뒤가 전부 정상 동작하면서
    다른 물체를 만든다".  환경변수가 하나라도 없으면 T2I_UNAVAILABLE 로 멈춘다

🔴 W27 이 물릴 자리 셋 — 3090 이 실제로 밟아 본 것들
 ① **스테이징 경로는 job_id(`j-…`) 다.**  슬롯 디렉터리명(`gen-…`)은 404
    ⇒ 경로를 손으로 만들지 말고 **`uris.staging_chunk_uri()` 를 불러라.**
      접두사가 비대칭이고 버전이 없어서 손으로 만들면 404 만 나온다
 ② 상류 `meta` 는 **JSON 문자열 폼 필드**다 (본문 JSON 아님 — 처음엔 422)
 ③ ★ **완료 판정은 `state` 가 아니라 응답 모양**(`"chunks"` and `"to_version"`)이다.
    state 로 판정하면 **그 필드를 안 채우는 판본에서 영원히 돈다**
★ 잡은 버그: `Path("")` 는 `.` 이 되고 `.exists()` 가 참이라 **미설정 환경변수가 통과**했다.
  Path 변환 전에 원문을 보게 고쳤다 — 방법론 2조의 작은 실례

🔴 **생성물이 리포 밖 `_store` 에 쌓인다** (ASSET_STORE_ROOT).  989청크짜리를 리포에 넣을 수 없다
  ⇒ 그래서 **앱이 생성물을 바로 못 쓴다.**  지금은 손으로 골라 커밋해야 한다
  ⇒ 🔴 **이것이 W27 의 핵심 설계점이다** — 아래 참조
```

### ✅ W26e — A5000 이 **여섯 번째** 하드코딩을 스스로 찾았다

```
halo_sweep.py:42   c * 8 + 3.5  →  c * CHUNK_SIZE + (CHUNK_SIZE-1)/2.0
  ★ 구식 식은 청크 15 를 **123.5** 로 가리켰다 — VOXEL_RES 64 **격자 밖**이다.
    진단 도구가 격자 밖을 가리키면서 값은 계속 나왔다
lab/view.py        range(8) ×5  →  range(CHUNK_GRID_RES)
★★ 그리고 **열 머리글이 문자열로 박혀 있었다** — 내가 센 5곳 밖이다
     "01234567" → "".join(str(y % 10) for y in range(CHUNK_GRID_RES))
   ⇒ range 만 고쳤으면 **16열을 그리면서 머리글은 8자리인 채**로 남아,
     열 번호가 어긋난 지도를 보고 판단하게 됐다.  A5000 이 그 실패 양상을 정확히 짚었다
★ z 라벨 폭도 유도 — 원래 상수 10·12 가 각각 G+2·G+4 라 G=8 에서 **기존 출력과 바이트 동일**
검증  청크 0/1/15 중심 1.5/5.5/61.5 = 복셀 구간의 참 중심 · 최상단 61.5 < 64 ✅
      16³ 실제 렌더: 머리글 16자리 · x=15 행 · z=9 층 · 빈 층 접기 정상
⇒ **A5000 디스크의 8³ 잔재는 이제 없다.**  남은 것은 ChunkBin.cs:41 (맥북 디스크·Unity)
```

### 🔴 W27 설계 — 자산의 출처가 바뀐다.  이것이 배선의 본질이다

```
지금    Unity 가 **APK 안**(StreamingAssets) 청크를 읽는다.  빌드 타임에 구워진다
        · sha256 대조로 검증됨 (W25) · 렌더는 이미 성립
W27     Unity 가 **<GEN_HOST> 에서 HTTP 로** 청크를 받는다.  런타임이다
🔴 렌더는 검증됐고 **전송이 미검증**이다.  둘을 한꺼번에 바꾸지 마라

★ 최소 단계로 쪼갠다 — 각 단계가 따로 판정된다
 ① 전송만 교체   기존 자산을 <GEN_HOST> 에서 HTTP 로 받아 렌더.  생성·편집은 안 건드린다
   🔴 **판정을 육안으로 하지 마라.**  APK 안 사본이 이미 `SHA256SUMS` 로 잠겨 있다
      ⇒ HTTP 로 받은 청크를 **APK 사본과 바이트 대조**.  전 청크 sha256 일치 = 전송 성립
      ⇒ "같아 보인다" 는 몇 청크가 조용히 빠져도 통과한다.  W25 의 대조 장치를 그대로 쓴다
   ✅ **`asset_id = v3-moto-b` · 376청크 · contract v4** (3090 확인)
      `GET <GEN_HOST>:8083/v2/assets/v3-moto-b/manifest.v1.json` → 200
      ★ 청크 URI 는 **매니페스트의 `chunks[key].uri` 를 그대로** 쓴다 (손으로 만들지 마라)
      ★★ 3090 이 **맥북이 할 판정을 미리 돌렸다** — HTTP 매니페스트 hash ↔ 리포 사본 전량 대조
         **376 / 376 일치 · 불일치 0.**  판정 장치가 선다
      ⚠️ 생성물(duck 989청크)은 APK 사본이 없어 이 대조가 성립하지 않는다 — ①은 v3-moto-b 로
 ② 편집 왕복     라쏘 → POST /edits → 폴링 → PatchPackage → **changed 만 교체**
                 ★ 여기서 (C) in-place 를 **AR 앵커 위에서** 잰다.  앵커 pose 델타 0
 ③ 생성 수신     자연어 → POST /v2/assets → 폴링 → manifest → 청크 → 렌더
                 ⚠️ 989청크 — 직렬이면 40초대.  병렬화가 선결
⇒ ①이 안 되면 ②③은 원인을 못 가른다

🔴🔴 **순서를 바꿨다 (rev62).  편집이 생성보다 앞이다** — Chat 의 초기 배열이 틀렸다
```
편집은 생성에 의존하지 않는다.  필요한 것이 전부 이미 서 있다:
  자산 로드   ✅ ① 에서 v3-moto-b 를 HTTP 로 받았다 (376/376)
  라쏘        ✅ 실기 1,386셀 · 오염 0
  편집 라우트  ✅ 한글 "뒷바퀴를 파랑으로" → succeeded · changed 71 / removed 0
  폴링·델타   ✅ PatchPackage 규약 · 판본 공존 확인
  in-place    ✅ Editor 통과 (EntityId 89/89 · recreated 0 · apply 7.68ms)
  AR 앵커     ✅ W26c 성립
★ 그리고 **직렬 수신 문제가 편집엔 없다** — 델타는 changed **71청크**다.
  71 × 34.4ms ≈ **2.4초**.  989청크(40초대)는 생성 경로만의 문제다
⇒ 생성(③)은 편의이고, **편집(②)이 대목표 (A)(B)(C) 그 자체**다.  이걸 먼저 닫는다
```
```

### ✅ W26f③ — 한글 op 가 뚫린다 · 라우트 테스트가 결함 셋을 잡았다 (3090)

```
지시                    결과
뒷바퀴를 파랑으로        succeeded · changed 71 / removed 0
바퀴를 빨갛게 칠해줘      succeeded · changed 71   (전에는 거부됐다)
이 부분을 뾰족하게        UNSUPPORTED_OP · op=replace_region → "assemble 또는 VoxHammer 로"
뒷바퀴를 없애줘          UNSUPPORTED_OP · op=remove → "청크를 비울 수단이 없다"

★★ **이 배선이 진단을 갈랐다** — 전에는 "이 부분을 뾰족하게" 도 COLOR_NOT_UNDERSTOOD 였다.
   그건 **틀린 진단**이다.  색 문제가 아니라 이 소비자가 못 하는 op 이고,
   **고칠 사람도 방법도 다르다.**  UnsupportedOp 에 error_code 를 달아 클라이언트가
   "서버 버그" 와 "이 경로로는 못 하는 요청" 을 구분하게 했다 — D71 의 좋은 적용
★ 한국어 활용형: 빨강/빨간/빨갛게/붉은 이 같은 색인데 **어간만 넣어 "빨갛게" 를 놓치고 있었다**.
  색이 둘이면 뒤에 나온 것을 쓴다.  ⚠️ 어휘표지 이해가 아니다 — 표에 없으면 거부한다
  (COLOR_NOT_UNDERSTOOD 유지.  "기본색을 칠하면 무엇을 요청하든 같은 색이 되는 서버가 된다")

라우트 테스트 11건 — **3건이 실제 결함을 잡았다**
 🔴 `manifest.v99.json` 이 **200** 을 냈다 — 없는 판본에 v1 을 조용히 돌려줬다.
    **docstring 에 "옛 판본으로 대신 답하지 않는다" 고 적어 놓고 구현이 없었다**
    ⇒ 방법론 4조("규칙만 적고 함수를 안 주면 그 규칙은 안 지켜진다")의 정확한 재현
 🔴 그 탓에 `base_version=99` 편집도 409 가 아니라 200
 🔴 `JobStatus.state` 는 **넷**(queued·running·succeeded·failed).  배선 문서에 다섯(cancelled)이라
    적은 것은 오류 — "계약을 안 열고 기억으로 썼다".  Chat 이 그것을 그대로 옮겼다

🔴 **재시작 취약점을 고쳤다 (3090).  W27 이 여기 걸려 있었다**
  저장소 루트를 **기동 명령줄에만** 싣고 있어서, 누가 그냥 `./server/run-8083.sh` 로 재시작하면
  **리포 자산이 통째로 사라졌다**("모르는 자산이다: 'v3-moto-b'").
  ⇒ 재시작 한 번으로 남의 웨이브가 멈추는 구조였다.  이제 리포 밖 `~/.ai-ar-v3-server.env`(600)
    를 읽는다.  ★ `env -i` 맨몸으로 재시작해 수치를 다시 확인했다 — 전제를 스스로 검사했다

★ 커밋 사고 자진 신고: `assets/moto-b/v2/`(1.8MB·71파일)가 W26f 커밋에 딸려 들어갔다.
  쓰기 루트를 리포 밖으로 바꾸기 전 산출물이다.  되돌리고 `.gitignore` 에 `/assets/*/v[0-9]*/` 로 잠갔다

남은 약점 (적어만 둔다)
  · op 분류가 **LLM 호출 1회**에 달렸다.  키가 없으면 fallback_spec 이 replace_region 고정이라
    한글 recolor 가 전부 UNSUPPORTED_OP 가 된다
  · 색 표는 어휘다 — 하늘색·남색은 못 읽는다
  · 잡 인메모리(재시작 시 404 = "실패" 가 아니라 "없다") · 무인증
```

### ✅ W27① — **전송 성립.  376/376 sha256 일치** (맥북 + 실기 S22)

```
맥북    manifest 376청크 · contract 4/4 · 130ms  →  수신 376/376 · 7,646,272B · 12.92s
실기    HTTP 수신 376/376 · 실패 0 · 7,646,272B · 15.67s · 정점 207,537 (manifest 1,282ms 별도)
바이트 일치 376/376 · 불일치 0 · 로컬 사본 없음 0
★ W25 에서 APK 를 상대로 만든 대조 장치를 **네트워크에 걸었다** (tools/verify_http_transfer.py).
  육안 판정은 쓰지 않았다
★ 계약 가드가 **청크를 받기 전에** 선다 — v3 자산이면 한 개도 안 받고 거부
★ 맥북이 `Validate()` 라는 이름을 지어냈다가 컴파일 에러로 잡혔다 →
  **재구현 않고 계약이 이미 준 `AssertCompatible()` 을 따랐다.**  좋은 반사다
★ 경로를 손으로 안 만든다 — 서버가 계약 규칙으로 만들어 보낸 `ChunkEntry.Uri` 를 그대로 쓴다
★ 호스트는 GEN_HOST 환경변수로만 빌드에 주입 — 리포·코드·문서에 값이 없다 (§6)

🔴🔴 **②를 막을 것: 수신이 직렬이다.  청크당 34.4ms 왕복이 그대로 쌓인다**
   376청크 12.92s / 15.67s.  **989청크면 40초대** (2.6배).  생성 자체가 50.1s 인데
   수신이 40초 더 붙으면 90초 — 폴링 상한 120초에 육박한다
   ⇒ ②의 선결 과제다.  병렬화(클라) + 서버가 동시 요청을 견디는지(3090) 양쪽이 필요하다

⚠️ 이름 함정: **`moto-b` 는 404.  `v3-moto-b` 만 있다** (3090 이 `v3-` 접두사로 저장)
   ★ `v3-` 는 **자산 계보**이지 계약 판본이 아니다 — 이 자산의 `contract_version` 은 **4** 다.
     누군가 `v3-` 를 보고 "낡은 계약" 으로 읽으면 조용히 틀린다.  이 프로젝트가 반복해 물린 형태다
⚠️ `<GEN_HOST>:8083` — `/healthz` 는 있고 `/v2/health` 는 404 · **`/openapi.json` 이 없다**

미결
  · **clean 빌드 미실시** — OpenGLES3 전환 후 낡은 매니페스트 태그 (§5 경고 미처리)
  · **HTTP 경로 화면 확인 못 함** — 숫자는 충족(376/376·정점 207,537)이나 육안이 비어 있다.
    ⚠️ 운영원칙 7: 육안 산출물이 없으면 미검증으로 적는다
  · ★ 브랜치 사고: `w27a-http` 가 main 에서 갈라져 AR 파일이 없었고,
    `git checkout <branch> -- unity/` 로 되살리다 **방금 쓴 HTTP 변경을 덮어썼다**.
    ⇒ D27-c(웨이브마다 새 브랜치)를 지키되 **기반 브랜치를 명시적으로 확인**한다
  · dragon-c 폐기 완료 (자산 9.9MB · handoff 사본 · SHA256SUMS · 빌드스크립트·README 문구.
    assets/ 22MB → 12MB).  ⚠️ **3090 이 아직 `v3-dragon-c` 를 서빙 중이다**
  · xr 판별식 정정 — 이름 매칭이 놓쳤던 것이지 장치가 없던 게 아니다.  layout 까지 보게 고쳤다
```

### ★★★ W27② — **대목표 (A)(B)(C)(D) 가 전부 숫자로 닫혔다** (실기 S22)

```
(A) 효능      라쏘 1,376셀 → "recolor : front wheel to blue color" → **바퀴가 파랗다**
              나머지(라이더·차체·헬멧) 그대로.  handoff/w27-http/2-edit-blue-wheel.png
(B) 보존      changed 밖 **305/305 청크가 v1↔v2 바이트 동일 · 불일치 0**
              ①에서 만든 대조 장치를 그대로 돌렸다
(C) in-place  APPLY 교체 70 · **생성 0 · 파괴 0 · 실패 0** · 노드 376
              GameObject 를 다시 만들지 않고 같은 Mesh 인스턴스에 덮어썼다
(D) AR        ★★ **순수 앵커 델타 −0.37mm / 0.000°** (적용 직전 12.58mm → 직후 12.21mm)
              W26c 의 8.92mm 는 2.94m 이동이 섞인 값이라 증거가 아니었다.  이번엔 적용 전후만
왕복  POST→PATCH 수신 3.4s + 델타 70청크 수신·적용 2,376ms = **약 5.8s**
⚠️ 실기 70 vs 맥북 71 은 라쏘 셀 수 차이(1,376 vs 1,386) — 마스크가 다르면 걸리는 청크도 다르다

🔴 **완료 판정에 한 번 물렸다 — 지침이 절반만 맞았다**
   "state 가 아니라 응답 모양으로" 는 맞았는데 **모양의 위치**를 틀렸다.
   최상위에서 `chunks`/`to_version` 을 찾았지만 실제는 **`patch.changed_chunks` / `patch.to_version`**.
   state 는 이미 succeeded 였는데 판정이 안 걸려 **130초를 헛돌았다**
   ⇒ ★ 판정 규약에 "무엇이 있나" 만이 아니라 **"어디에 있나"** 까지 적는다

★ ①의 육안도 닫혔다 (1-http-load.png).  운영원칙 7 미검증 해소
```

### ✅ W27 준비 — 3090 이 맥북이 부를 것을 전부 미리 쐈다

```
base_version=2  → 409 "v3-moto-b 에 v2 이 없다. 있는 것: [1]"    base_version=99 → 409
'이 부분을 뾰족하게' / '뒷바퀴를 없애줘' → failed · **UNSUPPORTED_OP**
'뒷바퀴를 파랑으로' → succeeded · changed 71
slat_coords.v1.json → 200 · n_cells 9150 · grid_source slat_coords · voxel_res 64   (v2 → 404)
/openapi.json 200 (라우트 19개) · /v2/health 개방 · docs_url 은 계속 닫음(공개 URL, 표면 축소)
asset_id 오타 힌트: moto-b → "'v3-moto-b' 를 찾는가?" · 매니페스트에 "v3- 는 계보이지 계약 판본이 아니다" 명시
v3-dragon-c 내림(404) · JobStatus.state 넷으로 문서 정정 · manifest.v99 → 404 (테스트가 잠갔다)

★★ **운영 함정 — 브랜치를 바꿔도 프로세스는 안 바뀐다**
   처음 쐈을 때 UNSUPPORTED_OP 가 아니라 COLOR_NOT_UNDERSTOOD 가 나왔다.
   코드 문제가 아니라 **뜬 서버가 옛 코드**였다.  ⇒ **판정 전에 서버가 어느 코드인지 확인한다**
   (LLM 분류 자체는 안정적 — 3회 반복 replace_region/remove/recolor 전부 일치)

🔴🔴 **main 에 커밋 3개가 없다 (D27-c · 세 번째).**  PR #16 머지 **뒤에** 푸시한 것들이다
   빠진 것이 하필 맥북이 지금 쓰는 것: UNSUPPORTED_OP error_code · 한글 op 배선 ·
   라우트 테스트 11건 · **다중 루트 저장소** · 서비스 환경 파일
   ⇒ 다중 루트가 없으면 ASSET_STORE_ROOT 를 설정한 순간 **v3-moto-b 가 404** 다.
     지금은 옛 프로세스가 뜬 채 버티는 것뿐이고 **main 코드로 재시작하면 W27 이 멈춘다**.  PR #17 시급

★ 측정 — **번들을 논할 때가 아니다** (3090 의 절제가 옳다)
   로컬 120청크(평균 14.4KB):  직렬 keep-alive 3.15ms · close 3.33ms · 병렬 8/16/32 = 7.49/7.49/7.65
   🔴 **병렬이 직렬보다 느리다(0.4배)** · 14KB 에 서버 처리 중앙값 **7.84ms 는 과하다**
   원인 후보: 저장소가 **요청마다 자산 디렉터리를 전부 훑고 manifest 를 파싱**한다 (미확인)
   ⇒ "병목이 전송이 아니라 서버일 가능성이 크고, 그러면 병렬화도 번들도 안 듣는다"
   ⚠️ 로컬 측정이라 맥북 경로의 RTT·TLS 가 빠져 있다 — 34.4ms 내역은 **맥북이 keep-alive on/off 를
     재 주면 갈린다**
```

### 🔴 W27③ — 운전자 머리 → 아이언맨 머리 (사용자 지시)

```
★ 이것은 **recolor 가 아니다.**  `op=replace_region` 이고 지금은 UNSUPPORTED_OP 로 거부된다
★ 그리고 **`removed ≠ 0` 을 처음 밟는다** — GameObject 파괴다.
  D70·D75② 가 "in-place 품질이 갈리는 자리" 라고 지목한 곳이 여기다

경로 둘.  **VoxHammer 를 1안으로 간다** (사용자가 "VoxHammer 로 모든 편집" 을 이미 결정했다)
  1안 VoxHammer   A5000 래퍼 준비 완료 · D51 보존 수정 생존 필요 · 조건 이미지는 3090(t2i)
                  ⚠️ D55: 최종 GLB 는 slat 전역 디코드 — 마스크 밖 변화가 생기나
                     **(B) 는 승계가 지킨다**(부기로 changed 를 정한다).  값이 좋아지는 건 아니다
  2안 assemble    작업1(눈사람 머리→호박)에서 **이미 성립**했다.  절감 73.76% · 육안 인정
                  ⚠️ 스케일 인자 없음 — `donor_crop_fraction` + **정수 평행이동만**
                  ⇒ 1안이 막히면 여기로.  **둘을 동시에 켜지 마라**

단계 (실패를 가를 수 있게)
 ⓐ [3090] replace_region 을 **거부에서 dispatch 로**.  어디로 보낼지 결정
 ⓑ [3090] 아이언맨 머리 조건 이미지 (t2i)
 ⓒ [A5000] VoxHammer 실행 → 델타.  ★ D51 1줄 수정 생존 확인부터
 ⓓ [맥북] **`removed` 처리** — GameObject 파괴 + 사전 제거.  ⓐ~ⓒ 없이 먼저 할 수 있다
```

### 🔴 W27③ 가 막혔다 — 그리고 **Chat 이 결정할 것 셋** (rev64 확정)

```
[A5000] D51 **생존 확인**.  1D 인코딩(x*10^6+y*10^3+z) 그대로 · git diff 7줄뿐 · torch.isin 1곳
  합성 재현: 수정본 7,608셀(89.4%) vs 구식 element-wise **13셀(0.15%)** — rev63 기재와 정확히 일치
  ★ 지시한 순서(1번부터)를 지켜 **1·2 만 하고 멈췄다.**  이미지를 지어내지도 마스크를 만들지도 않았다
[3090] PR #17 **머지 완료**(d070e1e) · main 코드로 맨몸(env -i) 재시작 →
  manifest 200 · slat_coords 200 · v3-dragon-c 404 · 오타 힌트 동작.  **이제 재시작해도 안 죽는다**
  ⚠️ 3090 이 자기 PR 을 머지했다 — server/ 는 3090 담당이고 PR 경로였다.  밝혀 둔 것이 맞다

🔴 블로커 ① — **상류에 커밋된 base 판본이 없다**
   "이 부분을 아이언맨 머리로" → op=replace_region 분류 ✅ · dispatch ✅ · 상류 제출 ✅
   → **409 VERSION_CONFLICT: base_version=1 은 커밋되지 않았다 (committed latest=0)**
   v3-moto-b·duck 둘 다 상류 커밋본 404.  생성이 **staging 까지만 하고 commit 을 안 밟는다**
   ⚠️ duck 은 오늘 생성인데 staging 이 **이미 사라졌다** — 수명을 모른다
🔴 블로커 ② — **조건 이미지 슬롯이 없다**
   `<EDIT_HOST> POST /v2/trellis/edit` 는 application/json 만 받고 필드는
   asset_id·base_version·prompt·mask·seed·idempotency_key — **이미지 자리가 없다**
   ★ 3090 이 **만들지 않은 것이 맞다** — "만들어 두면 '조건을 준비했다' 는 착시가 생긴다".
     자리표시를 안 만드는 것과 같은 원칙이다
⚠️ 필드명도 어긋난다: 3090 은 `mask`, A5000 route 는 `mask_cells`.  이대로면 422 다

────────── ★★ Chat 결정 (rev64) ──────────
결정 1 — **커밋은 A5000 이 자기 자산으로 한다.  생성 경로에 commit 을 붙이지 않는다**
  근거: **VoxHammer 는 slat 을 편집한다.  청크 판본은 하류다.**
        A5000 디스크에 `v3-moto-b` 가 이미 있다(작업 자산 5개 중 하나).
        3090 이 v4 로 재분할한 것은 **청킹**이고 slat 은 같은 물체다
  ⇒ ★ **검증 가능하다**: 3090 이 서빙하는 `slat_coords.v1.json`(9,150셀 · fingerprint)와
    A5000 로컬 slat 을 대조한다.  **지문이 같으면 같은 물체**이고 커밋해도 안전하다
    ⚠️ 지문이 다르면 커밋하지 마라 — 그때는 생성 경로 commit 으로 간다
  ⇒ staging 수명 문제를 우회한다.  duck 은 지금 손대지 않는다

결정 2 — **조건은 텍스트로 먼저 간다.  계약을 바꾸지 않는다**
  ⚠️ 단, **A5000 이 `prompt` 로 실제로 무엇을 하는지 먼저 답해야 한다** —
    TRELLIS image-large 는 이미지 조건 모델이다.  텍스트만으로 조건이 서는가?
    · 선다면 → 텍스트로 간다.  끝
    · 안 선다면 → **A5000 이 자기 route 에 이미지 슬롯을 연다.**
      ★ 이건 `contract/` 변경이 **아니다** — `/v2/trellis/*` 는 A5000 자기 스키마다.
        3090 이 t2i 로 만든 이미지를 넘긴다 (W11 에서 성립한 경로)
      ⇒ Unity→3090 구간은 자연어 그대로.  **계약은 안 건드린다**
  ★ 추측하지 말고 **A5000 이 답한다.**  이건 결정이 아니라 사실 확인이다

결정 3 — **마스크 변환은 3090 이 한다.  A5000 route 를 정본으로 삼는다**
  근거: 3090 은 이미 상류 적응을 한다(meta 폼필드·완료 판정 모양).  A5000 이 계약 지식을
        키우면 두 곳이 계약을 알게 된다.  ⇒ **필드명은 A5000 route 것(`mask_cells`)에 맞춘다**
```

### 🔴 rev65 — **결정 1 을 정정한다.  그리고 아무도 안 맡은 입력이 하나 있었다**

```
[A5000 새 발견 ④]  moto-b 가 <EDIT_HOST> 에서 **편집 불가 상태**다
  committed.json 없음 → committed()=0 → /edit 은 409 · v1/ 디렉터리 없음(staging 만)
  청킹 계약 **v3 · chunk_size 8 · grid 8 · 89청크 · 헤더 바이트 3**
🔴 **이것이 rev64 결정 1 을 깬다.**  "slat 만 같으면 커밋해도 된다" 고 했는데 틀렸다 —
   **승계(inheritance)가 부모 바이트를 그대로 물려준다.**  부모가 v3 청크면
   안 바뀐 청크가 v3 로 나가고 v4 Unity 가 거부한다.  W26b 에서 닫은 그 블로커의 재발이다

★★ 더 깊은 문제: **재디코딩은 재현되지 않는다** (FINDINGS §1 — 152/152 해시 불일치).
   A5000 이 slat 에서 **독립적으로** v4 재분할하면 3090 이 서빙하는 376청크와 **바이트가 다르다.**
   그러면 Unity 가 로드한 자산(3090 판)과 편집 base(A5000 판)가 갈리고 **승계가 무너진다**

⇒ ★ **결정 1 (정정)**: A5000 이 **3090 이 서빙하는 바로 그 376청크를 받아 커밋한다**
     `GET <GEN_HOST>/v2/assets/v3-moto-b/manifest.v1.json` + chunks → 그대로 base_version 1
     ⇒ 재분할하지 않는다.  **바이트를 옮긴다**
     slat 은 A5000 staging 것을 쓰되 **3090 의 slat_coords 지문과 대조**해 같은 물체임을 확인
     ⇒ 그래야 Unity·3090·A5000 세 곳의 base 가 **같은 바이트**가 된다

★ 결정 2 (확정) — **텍스트 조건으로는 안 된다.  A5000 이 자기 route 에 이미지 슬롯을 연다**
  근거는 추측이 아니라 실측이다 — **D40: 조건 이미지가 프롬프트를 이긴다.**
  W11 에서 조건을 바꾸자 제거 730→471 로 **방향이 뒤집혔다**.
  A5000 판정: "조건 없이 돌리면 **원본 머리로 회귀**한다"
  ⇒ `/v2/trellis/*` 는 A5000 자기 스키마다 — **`contract/` 변경이 아니다**

🔴🔴 **아무도 안 맡은 입력: 운전자 머리 마스크**
  A5000: "handoff/lasso/ 에 있는 건 뒷바퀴 마스크(W23)와 synthetic-car 뿐.
          지시대로 제가 다시 계산하지 않았습니다 — 클라가 준 복셀이 유일한 진실이고 W11 에서 물렸다"
  ★ **정확히 맞다.  그런데 만들 사람을 Chat 이 배정하지 않았다.**
  ⇒ **맥북이 만든다** — 실기에서 운전자 머리를 라쏘하고 마스크를 핸드오프한다 (W23 과 같은 형태)

★ A5000 의 removed 예측기 — **독립 경로에서 숫자가 일치했다**
    예측기 v4 점유청크 375 · 변경후보 72(19.2%)   vs   3090/D75 기록 375 · 72(19.2%)  ✓
  ⇒ 뒷바퀴 마스크 기준 **removed 상한 42청크(11.2%) · 1,022복셀**
  ⚠️ 42 는 뒷바퀴 값이다.  머리 마스크가 오면 한 줄로 나온다
  ★ 원리: 청크의 점유 복셀이 **전부** 마스크 안이면 비워질 수 있고, 하나라도 밖이면 절대 안 비워진다
  ★ recolor 의 removed=0 은 `recolor_asset` 이 `removed=[]` 를 **구조적으로 명시**하기 때문이다.
    replace_region 은 그 보호가 없다 — D70·D75② 가 지목한 자리로 실제로 들어간다
```

### ✅ W27③ⓓ — removed 계측 + keep-alive 내역이 갈렸다 (맥북)

```
removed → **GameObject 파괴 + 사전 제거 둘 다**.  사전에 남기면 다음 패치가
  이미 파괴된 MeshFilter 에 덮어쓰고 **예외가 안 난다**
(C) 지표 확장 — "파괴 0" 은 이제 합격선이 아니다 (파괴가 정상이므로)
  삼자 일치: 보고 removed 수 == 파괴 수 == 사전에서 사라진 수 · 나머지 EntityId 유지
  ★ 위반하면 **LogError 로 올린다** — 조용히 넘어가지 않게.  방법론 4조의 적용
⚠️ 실측 미도달 — recolor 는 전부 removed 0 이었다

★★ keep-alive 로 34.4ms 가 갈렸다 (3090 로컬 3.15 vs 3.33ms 로는 못 갈리던 값)
      OFF 26.49ms/청크 → 376청크 10.0s
      ON  16.50ms/청크 → 376청크  6.2s      절감 9.99ms (38%)
  ⇒ **약 10ms 가 연결 수립, 16.5ms 가 RTT+전송**
  ⇒ 판단: keep-alive 만으론 부족하다.  **16.5ms × 989 = 16.3초** — 생성 경로는 병렬이 따로 필요하다
clean 빌드 ☑ (V3_CLEAN=1 · 3분43초 · APK 청크 sha256 통과) — 세 웨이브째 미결이 닫혔다
  ⚠️ 다만 매니페스트 태그를 **직접 확인한 게 아니다** (바이너리 XML).  "clean 빌드를 했다" 까지가 사실
```

### 🔴 rev66 — VoxHammer 를 켜는 데 남은 것은 **slat feats 하나**다

```
✅ 마스크      맥북 핸드오프 완료 · main `handoff/lasso/moto-b.rider-head.mask.json`
              476셀 · 지문 9e237b53… · y29–40 z46–55 x24–38 · **어깨 침범 0셀**
              ★ 계약의 LassoVolume 을 그대로 태운 헤드리스 실행(재구현 없음).
                z=45.5 에서 잘라 머리+헬멧만.  ⚠️ 실기 손가락이 아니다 — 속도를 택했다.
                좌표계·투영·카메라는 실기와 같은 코드이고 W22 에서 실기 REPLAY 가 골든과 전건 일치했다
✅ base 커밋   A5000 이 3090 의 376청크를 **그대로 받아** committed latest=1.
              sha256 376/376 · 디스크 재검증 376/376 · **409 VERSION_CONFLICT 닫힘**
              ★ 자기 루프 버그를 산술로 잡았다 — `while read` 가 개행 없는 마지막 줄(9_9_9)을
                건너뛰어 375/376.  **200 375 + 비200 0 ≠ 376** 이 안 맞아 발견했다
              ⚠️ 자산 자체의 작은 불일치 둘(보고만): slat 9,150 vs manifest 합계 9,149
                (복셀 6_12_8 이 대응 청크 없음) · voxel_count=0 인 청크 2개(8_7_11, 10_7_4)
✅ 이미지 슬롯 A5000 이 열었다 (`contract/` 무수정).  JSON 은 그대로 두고 multipart 를 추가 —
              기존 recolor/W27② 경로를 **안 깼다**.  세 경로 전부 실측(409 = 파싱 성공)
   ★ 3090 이 보낼 것: **PNG 1024² · 알파 불필요**(편집 경로가 rembg 무조건 실행 → 덮어씀, D43) ·
     ★★ **머리만 크롭하지 말고 "바뀐 뒤 전신"** · 단순 배경
✅ 사유 전파   3090 이 붙였다.  `UPSTREAM_VERSION_CONFLICT` — **출처를 접두사로 남겼다**
              "상류 코드를 그대로 쓰면 그게 3090 것인지 <EDIT_HOST> 것인지 화면에서 안 갈린다 —
               고칠 사람이 다르다".  상류가 JSON 을 안 줄 때는 `UPSTREAM_HTTP_500` + 본문 앞부분
✅ 저장소 스캔 **의혹이 맞았다.**  `_slots()` 가 요청마다 읽기 루트를 훑고 manifest 를 파싱했다
              STORE.chunk 0.698ms → 0.070ms · HTTP 처리 중앙값 7.84 → 3.49ms
              **병렬 8 이 0.4배(직렬보다 느림) → 1.5배로 뒤집혔다**
              ★ 무효화를 TTL 이 아니라 **읽기 루트 mtime** 으로 — "만료 전에 새 자산이 안 보이고,
                그 사이 404 가 나면 원인을 못 찾는다"
              ⇒ 서버는 병목이 아니다.  번들은 여전히 논할 때가 아니다.  8 초과는 이득 죽음(GIL)

🔴 **필드명이 내 기재와 반대였다.**  A5000 실측: route 의 필드는 **`mask`** 다.
   `mask_cells` 는 서비스·계약 어디에도 없고 `halo_sweep.py` 의 **지역 변수 이름**일 뿐이었다
   ⇒ ★ **결정 3 이 소멸한다 — 3090 은 변환할 게 없다.**  마스크는 이미 `EditMask.voxels` 로 들어와 있다
   ⇒ Chat 이 A5000 의 초기 보고를 검증 없이 옮겼다.  A5000 이 실측으로 정정했다
   정본:  "mask": { "mode":"voxels", "voxels":[[x,y,z],…], "grid_source":"slat_coords",
                    "halo_margin_voxels": 2 }
          좌표 = slat 격자 정수 0..63 · 정렬 불필요 · 중복 허용 · 활성 복셀 최소 1개(아니면 MaskEmpty)
          지문은 **halo 적용 전** 원본 셀 기준

🔴🔴 **남은 블로커 하나 — `slat.safetensors` (feats)**
   A5000 의 staging TTL(3600s)이 사본을 GC 했고, 3090 의 `slat_coords.v1.json` 은 **coords 만** 담는다
   VoxHammer 는 feats 없이는 못 돈다.  ⚠️ 재생성하면 결정 1 이 금지한 것과 같은 문제 —
   **바이트가 갈린다**(FINDINGS §1)
   ⇒ ★ **먼저 A5000 자기 v3 자산 저장소를 봐라.**  `v3-moto-b` 가 작업 자산 5개 중 하나로
     `.CONTRACT_V3_STALE` 마커만 붙은 채 **보존돼 있다**(1.1GB / 53자산).  거기 slat 이 있을 것이다
     ⇒ **지문 대조로 확인 가능**: 3090 slat_coords fingerprint `b8a621ab…` · 9,150 coords
     ⇒ 없으면 그때 3090 이 자기 store 를 뒤진다
```

### ★★ rev67 — **형태 편집이 처음으로 끝까지 갔다.**  남은 것은 마스크 전달 하나

```
[3090] "라이더 머리를 아이언맨 머리로 바꿔줘"
  0.0s mask 검증 → 3.0s op=replace_region → 6.0s **조건 이미지 생성(t2i)** →
  33.0s <EDIT_HOST> repaint → 36.0s chunk → **39.1s succeeded**
  patch v1→v2 · changed 74 / removed 0 · contract v4
  ★ 조건은 **3090 이 t2i 로 만든다.  Unity→3090 은 자연어 그대로다** (실시간 경로다)
  ★★ "조건을 못 만들면 **조건 없이 몰래 진행하지 않는다**" — D40 상 원본으로 회귀하는 길이고,
     결과만 보면 "편집이 약하다" 로 오독된다.  정확한 판단이다
  ⚠️ 다만 **뒷바퀴 마스크(1,386셀)로 돌았다.**  경로는 증명됐고 **"아이언맨 머리가 됐나" 는 미판정**

🔴🔴 **머리 마스크(476셀)가 3090 에 없다 — 그런데 main 에 있다**
   맥북이 PR #19 로 머지했다: `handoff/lasso/moto-b.rider-head.mask.json`
   3090 은 자기 `handoff/lasso/` 만 봤다.  ⇒ **`git pull` 한 줄이면 끝난다**

[3090] slat.safetensors 를 **자기가 갖고 있었다.**  보냈다
  slat 439,848B `b8b1f91c…` (좌표 9,150 = manifest voxel_count_total, D28 통과) · input.png 994,830B
  ★ **사유 전파를 켜자 상류가 무엇을 못 찾는지 정확히 나왔다.**  없던 자리에 추가(덮어쓰기 아님) ·
    받는 쪽 sha256 확인(D27) · **재생성 안 함**(바이트가 갈린다)

🔴🔴 **§7 위반 — 3090 이 만들고 3090 이 잡았다**
   `httpx.raise_for_status()` 의 예외 메시지에 **URL 이 통째로 들어간다.**
   그게 잡 상태로 올라가 **앱 화면에 공인 IP 가 찍혔다.**  실측으로 겪었다
   ⇒ `raise_for_status()` 를 안 쓰고 상태·본문만 넘긴다.  테스트가 IP·URL 부재를 잠근다
   ★ **"사유는 전파하되 주소는 전파하지 않는다"** — 사유 전파와 §6 보안이 충돌하는 자리다.
     다른 세션도 예외 메시지를 그대로 올리고 있지 않은지 봐라

[A5000] 상류 500 — **셋이었고 전부 자기 쪽이었다.  셋 다 고쳤다**
  ① `store.replayable()` 이 idempotency_key=None 을 `_safe()` 에 넘김 → 평문 500
  ② `/edit` 시그니처를 Request 로 바꾸며 **FastAPI 자동 422 를 잃었다** — 스키마 위반이 500 이 됐다
     ★ **자기가 낸 회귀를 자기 테스트로 잡았다**
  ③ 워커의 `staging_dir(…, None)` — 라우트는 202, **워커가 죽어 조용히 실패**
  ⇒ 이제 job 이 FileNotFoundError(input.png) 로 **깨끗이** 실패한다.  "조용한 실패" 가 사라졌다

[A5000] slat 을 자기 디스크에서 **전수 대조로 없음을 확정**했다
  85개 slat.safetensors 지문 대조 · 9,150 coords 0개 · 9000~9300 구간 전부 불일치
  ★ 지문 함수를 확정했다 — 3090 의 fingerprint 는 `deltacontract.mask_fingerprint()` 로 정확히 재현
  ⚠️ staging TTL 이 slat 과 input.png 을 **같이** 가져갔다.  v1/v2 디렉터리는 애초에 없었다

★ removed 상한 (머리 마스크 476셀) — 마스크 검증 통과(지문 9e237b53 일치)
  halo 1 (마스크 명시값)  변경후보 25/375 (6.7%) · **removed 상한 13 (3.5%)** · 하한 0
  halo 2 (DEFAULT)        변경후보 28/375 (7.5%) · removed 상한 28 · 하한 0
  ★ 하한 0 인 이유: VoxHammer 가 그 자리를 새 기하로 채우면 안 비워진다
  ★ 6_12_8 복셀 (25,48,32) 는 **마스크 밖** — 이번 편집에 영향 없음

[맥북] 병렬 수신 — **실기에서 4.5배**
      직렬 12.67s(33.7ms/청크) → 병렬 4 3.91s → 6 2.32s → **8 1.86s (5.0ms/청크 · 4,007 KB/s)**
      실기 S22: 12.7s → **2.83s** · 청크당 7.5ms · 실패 0 · 989청크 환산 33s → **약 7s**
  ★ 맥북 6.8배 vs 3090 1.5배는 **다른 것을 잰 값**이다 — 3090 은 서버 내부(로컬),
    맥북은 네트워크 왕복 포함.  RTT 가 지배적이라 병렬 이득이 훨씬 크다.  둘 다 맞다
  ⚠️ 동시 8 상한의 근거가 **한 번의 타임아웃뿐**이다 (서버 병목 제거 전 값).  더 올려도 되는지 모른다

[맥북] 사유 전파가 **실제로 갈랐다**
  base_version=2 → UPSTREAM_VERSION_CONFLICT "committed latest=1"  ← 그 자리에서 재시도 판단
  base_version=1 → INTERNAL 500 at <EDIT_HOST>                     ← A5000 내부 (셋 다 수정됨)
  ⇒ 사유가 없던 때는 **둘 다 "편집 제출 실패" 로만 보였다**
```

### 🔴 rev67 결정 둘

```
결정 4 — **판본 동기화는 3090 이 자동으로 한다**
  맥북 지적: "3090 에는 v2 가 있는데 A5000 커밋은 v1 까지다.  이것 자체가 다음 실패의 씨앗"
  ⇒ ★ 3090 이 형태 편집을 dispatch 할 때 **base_version N 이 <EDIT_HOST> 에 커밋돼 있는지 먼저 확인**하고,
    없으면 **그 판본 바이트를 밀어 넣은 뒤** 제출한다.  v1 에서 한 것을 자동화하는 것이다
  ⇒ 수동 단계를 만들지 마라.  "커밋을 잊어서 409" 는 반드시 재발한다
  ⚠️ recolor 는 3090 로컬이라 A5000 에 없다 — **recolor 로 만든 판본 위에 형태 편집을 걸면 바로 터진다**

결정 5 — **`idempotency_key` 는 맥북이 판단한다 (contract/ 소유자)**
  A5000 보고: 계약은 Optional 인데 구현은 없으면 못 돈다(슬롯·재생색인·job 기록이 전부 이 키로 잡힘).
  ★ **"서버가 지어내면 같은 요청 두 번이 다른 슬롯을 가져 멱등이 깨진다"** — 그래서 거절했다.  옳다
  선택지  ⓐ 계약에서 **필수로 승격** (⚠️ conformance 53 · 골든 816 영향 확인)
          ⓑ Optional 유지 + 클라가 항상 보냄 + 422 거절 (지금 동작 · 계약 변경 0)
  Chat 의 기울기: **ⓐ**.  "계약이 Optional 이라고 말하는데 구현은 필수" 는 이 프로젝트가
  반복해 물린 형태다(docstring 은 있고 구현이 없던 manifest.v99 와 같은 계열).
  ⇒ 다만 골든 영향은 재 보고 정해라
```

## 4. Plan

```
W24 ★ 청크 8³ → 16³ (D75)
    ① ☑ [맥북]  계약 변경 — conformance 53/53 · main v4
    ② ☑ [3090]  실물 자산 v4 재분할 → PR #10.  ★ removed 0 확인.  원인 ② 발견
    ③ ☑ [맥북]  Texture3D 표면 하이라이트 · 키보드 · 되돌리기 → PR #9
W25 ☑ **PR #9·#10 머지 → 빌드 경로 수정 → 실기에서 곡면 확인**  ✅ 성립 (PR #11)
W26 ☑ ① [맥북] AR 살아남 (카메라·평면감지·앵커 코드).  ★ 탭 배치 실기 미완
    ☑ ② [3090] 계약 확정 · removed/halo 실측 · 🔴 A5000 v3 블로커 발견
W26b ☑ ① [맥북] 평면 윤곽선·회전 제거  ② [A5000] v4 승급  ③ [3090] 라우트 4개
W26c ☑ [맥북] ★★ **AR 성립** — 누끼 원인 5개 전부 처리.  (A)(B)(C)(D) 한 화면 (PR #15)
W26d ☑ ① [A5000] CHUNK_RE 유도 수정 · 509/509 양 호스트  ② [3090] 가드 산출바이트 검증
W26e ☑ ① [3090] t2i 배선 · **생성 end-to-end 성공** (989청크 50.1s)  ② [A5000] 8³ 잔재 전멸(여섯째 발견)
W26f ☑ ① [맥북] **PR #12→#14→#15→#16 머지 완료**  ③ [3090] 한글 op · 라우트 테스트 · 재시작 취약점
       ▶ 잔여: **dragon-c 폐기** · clean 빌드 · xr 판별식 · PR #12 문서의 state 다섯 정정
W27 ① ☑ 전송 376/376   ② ☑ ★★ **편집 왕복 — (A)(B)(C)(D) 전부 닫힘**
    ③ ▶ **운전자 머리 → 아이언맨** (replace_region · removed≠0 첫 진입)
    ④ 생성 수신 (직렬 40초대 — 병목이 서버인지 먼저 가른다) (§3 W27 설계).  단계를 합치지 마라
    ← 최대 미지수.  Unity 가 서버를 부른 적이 **0회**다
W28 멀티유저 (M6)
```

🔴 **W27 의 in-place 는 AR 앵커 위에서 재야 한다.** 고정 화면에서 "안 사라졌다" 를 재면
그건 (C)의 절반만 잰 것이다 — 사용자에게 in-place 란 **AR 공간의 그 자리에 그대로 있다**는 뜻이다.
그래서 AR(W26①)을 서버 배선(W27)보다 **앞에** 둔다.

**실기로 갈 때 반드시**

```
· ai-ar-v2/Tools/install-and-verify.sh 패턴 — 빌드 → APK 검증 → 설치 → logcat
· 🔴 "빌드 완료" 메시지는 증거가 아니다.  APK 안의 값을 열고(unzip -p … level*),
  진짜 판정은 앱이 실제로 보낸 요청 로그다 (세션 9 발생 → 세션 11 재발)
· Editor 골든과 실기 지문을 대조하라.  카메라 FOV·해상도·aspect 가 달라 경계 셀이 갈릴 수 있고
  예외가 안 나고 조용히 틀린다
· 실기 확정: Galaxy S22 (SM-S901N · Android 14 · arm64 · ARCore 1.54).  INFRA 의 "A37 현행" 은 오기
· 폰 → 3090:8083 은 미확인.  adb reverse 로 평문 HTTP 왕복은 확인됨
```

---

## 5. 🔴 자주 밟는 함정 — 작업 전에 읽는다

```
좌표 (여섯 번 물렸다)
  D9    GLB Y-up ↔ 복셀 Z-up.  voxel = (x, −z, y)
  D9-b  to_glb 말미의 z-up→y-up 회전.  그걸 안 거친 메시는 직접 걸어야 한다
  D28   격자 정본은 slat coords.  표면 복셀화는 진단용이고 마스크 근거가 될 수 없다
  D35   좌우 대칭 자산은 X 반전을 IoU 로 못 가린다 → 마스크를 X 대칭으로
  D64   Unity 는 왼손 좌표계다.  right = up × fwd.  헤드리스가 오른손이면 x 미러.
        ★ 개수·계수 10항목이 전부 일치해도 지문만 틀린다 — 원리적으로 안 잡힌다
  정본  server/pipeline/frames.py

델타·마스크
  D34   .cbin 은 slat coords 를 담지 않는다.  역산하지 마라 (두 번 실패).  이송받는다
  D24-a recolor 에서 canonicalize() 재호출 금지 — 기하 바이트 보존이 조용히 사라진다
  해시 비교로 델타를 정하지 마라 — 재디코딩하면 152/152 해시가 전부 다르다.  점유 비교로
  좌표 스케일 금지 (6-이웃 s=2.0 → 0%) · 소수 평행이동 금지 (+0.5 에서 78% 소실)
  D68   압출(solidify)은 사실상 비활성.  채움 축 = 시선 축이면 채운 셀이 이미 후보다

측정·게이트
  D33   바닥값은 자산별·영역별.  남의 자산 값을 쓰면 3.2배 틀린다
  D37   표본이 작으면 비율 단독 판정 거부.  원시 개수를 비율보다 앞에
  D73   🔴 무채색 자산에서 hue 이동은 무의미하다.  moto-b 뒷바퀴 채도 0.028 →
        검정→주황이 hue 3.6° 로 나온다(문턱 30°).  is_achromatic 이 없었으면
        효능 탈락으로 기록되고 예외는 하나도 안 났다
        ⇒ before 유채색이면 hue · 무채색이면 다른 지표(sat_delta·ΔE 후보).  문턱은 아직 없다
  D19-b 🔴 2D 화소 수로 3D 오염을 가늠하지 마라.  색 렌더에서 146화소 다른데
        3D 셀은 787, 정점은 19,172 다.  투영이 깊이를 뭉갠다 — 5배 이상 과소평가
  D5-a  신규·제거를 쌍으로.  성분은 z범위·x중심을 병기
  D60   산출물만 보는 골든은 중간 단계를 안 지킨다.  단계별 계수까지, 그리고 비대칭 케이스를

운영
  D27-c 웨이브마다 새 브랜치를 판다.  재사용하면 머지 시점 뒤에 푸시한 것이 상대에게 안 보인다
  D27   인계는 명시적 전송.  확인은 원격에서 읽고, 수신 세션이 보고해야 완료.
        브랜치도 마찬가지다 — "리포에 커밋했다" 가 "상대가 볼 수 있다" 를 뜻하지 않는다
  D46   /healthz 200 은 "화면이 뜬다" 가 아니다.  매 웨이브 / /real /runs 응답 코드를 적어라
  D47   DebugView 는 로컬 직접 접속이 정본.  ngrok 아님 (벽시계 과금·5GB/월)
  D48   게이트 표시는 3분류 — 해당 없음 / 미결 / 미도착
  D19   판정 그림: 형태 = 깊이맵 · 색 = before/after 색 렌더 쌍 · 생성 = 깊이맵
        ★ 시선 축을 자산이 정하게 하라 — 긴 축이 시선과 나란하면 부품이 전부 겹친다
  D23   LLM 은 좌표를 만들지 않는다.  {op, target_prompt} 만
  D43   rembg 는 건너뛰지 않는다 — 편집 조건 이미지는 RGB 로 보낸다
```

---

## 6. 보안 — 커밋 전 매번

```
금지   API 키 · 공인 IP · 포트 조합 · 터널 URL · 공유 시크릿 · 실제 호스트명 · 홈 경로 · SSH 포트
방법   전부 환경변수.  GEN_HOST / EDIT_HOST / SSH_PORT / ASSETGEN_URL / DEBUGVIEW_PORT /
       T2I_OUT_DIR / ANTHROPIC_API_KEY / LLM_MODEL
값 보관  ~/.ai-ar-v3-hosts (600) · ~/.env (600).  둘 다 리포 밖
검사   gitleaks pre-commit
경고   ai-ar-v2 의 StreamingAssets/blockedit_local_key.txt 에 실제 키가 있다 — 복사하지 마라
```

---

## 7. 세션 로그 (최근만 · 전체는 ARCHIVE)

| 날짜 | 세션 | 결과 |
|---|---|---|
| 08-05 | 맥북 | ✅ **언블록 — main 이 v4.** PR #8·#7 머지. D27-c 를 CLAUDE.md 에 반영 |
| 08-05 | 3090 W24② | 머지 대기로 정확히 멈춤. 계약 판본 표시 — 숫자를 안 박고 비교, 4로 갈아끼워 실제로 켜지는지 확인 |
| 08-05 | 맥북 W24① | ★ **계약 v4 완료 — conformance 53/53.** 낡은 전제 넷 적발. mirror_check 가 rc=0 으로 아무것도 안 하고 있었다 |
| 08-05 | ★사용자 | 청크 8³→16³ 확정. VoxHammer 통일은 기각(recolor 기하 100% 보존을 잃는다) |
| 08-05 | 맥북 W23 | UI 명세 전부 구현 · 궤적 지우개 실측(1,328→1,199). 화면이 큐브인 원인 = **자산이 합성 산출물** |
| 08-05 | ★사용자 | **앱 기능 명세 확정** · W22 중단("구현하려는 것과 거리가 멀다") |
| 08-05 | 맥북 W22 | 실기 터치 라쏘 통과 — 셀 1386 · 지문 골든 동일. S22 확정 |
| 08-05 | ★사용자 | **작업 2 육안 인정** — 뒷바퀴 색 변경 확인 |
| 08-05 | 3090 W21 | in-place 24/0/0 · 보존 100%. 효능은 무채색이라 hue 3.6°(D73) → 육안으로 닫힘 |
| 08-05 | 3090 W20 | ★ 작업 2 닫힘. 기하 바이트 100% · 3D 뷰 9/10 · 자기 색 오진 정정(D71) |
| 08-05 | 맥북 W19 | ★ 라쏘 관문 닫힘. moto-b 뒷바퀴 오염 0셀 |
| 08-05 | 3090 W19 | ★★ 델타 43.2% · 승계 100% → D56 절감 근거 폐기 |
| 08-04 | 맥북 W18 | 🔴 손잡이 오류 발견·수정(D64) — 계수 전부 일치, 지문만 불일치 |
| 08-04 | ★사용자 | **(C) 를 절감 → in-place 로 재정의 (D70)** |

---

## 8. 비목표

```
· 부품 분할(OmniPart/PartCrafter) — 보류.  진단은 맞다(D69 오염 교환비를 없앤다).
  🔴 그러나 둘 다 생성기다 — TRELLIS 뒤에 붙는 게 아니라 TRELLIS 를 대체한다.
  대체하면 VoxHammer 가 떨어져 나가고 편집 엔진 재탐색 = W6~W16 재현이다.
  ⇒ 같은 이득을 싸게: 두 시점 라쏘(교집합).  새 모델·VRAM 불필요
  ⚠️ "청크를 1복셀로" 는 부품 분할과 상충한다 — 마스크가 부품 경계를 따르면
    청크를 잘게 할 이유가 오히려 준다.  그리고 removed 증가로 D70 에 역행한다

· Dragon / VoxHammer 머리 3개 갈래 — D56 으로 종결.  성과·한계는 ARCHIVE 에
  (D51 버그 수정 · 머리 3개 달성 · D55 국소 편집 미성립 = 가장 값진 negative result)
· TRELLIS.2 이전 — 해상도가 병목이 아니었다 (D49-a)
· 멀티뷰 편집→재구성 — 원리만 채택, 파이프라인 보류 (D59)
· 마스크의 voxel 단위 부피 채움 · 겹치는 편집 충돌 해소 · 영속 저장 · 본격 인증
```

---

> 더 필요하면 `DECISIONS_ARCHIVE.md` — D1~D75 원문 · 웨이브 대장 · 세션 로그 전체 ·
> Dragon 갈래 상세 · 모든 실측 수치.
