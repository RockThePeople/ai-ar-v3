# server/ 의 의존성. 새 항목을 추가하기 전에 사용자에게 확인받는다 (CLAUDE.md).
# 라이브러리 하나가 GPU 서버 두 대의 환경 재구축을 부른다.

# ── 런타임 ────────────────────────────────────────────────
numpy>=1.24            # 계약(deltacontract)의 유일한 필수 의존
trimesh>=4.0           # GLB/glTF 적재 전용. pipeline/voxelize.py 가 **지연 import** 한다
                       # → 합성 픽스처 경로는 trimesh 없이 전부 돈다

# ── 선택 ──────────────────────────────────────────────────
pydantic>=2.0          # 와이어 스키마. 없으면 conformance 4건이 skip 된다
anthropic>=0.60        # llm.py 전용. **지연 import** 한다 —
                       # ANTHROPIC_API_KEY 가 없으면 규칙 기반 폴백으로 돌고
                       # SDK 자체가 없어도 폴백 경로와 테스트는 전부 돈다

# ── 개발 ──────────────────────────────────────────────────
pytest>=8.0
